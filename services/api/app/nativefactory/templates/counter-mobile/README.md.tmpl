# {{TITLE}}

Android (Kotlin, Gradle Kotlin DSL) - `{{ANDROID_PACKAGE}}` {{VERSION}}.

Rendered by the PagentOS native factory. There is no Gradle wrapper in this tree on
purpose: the factory renders source, never a script. On a machine with a JDK 17 and
Gradle: `gradle wrapper`, then `./gradlew test assembleDebug bundleRelease`.
