"""World Model foundation (overnight plan Phase 5).

A structured, computed-on-demand snapshot of the system
(``app.worldmodel.state.assemble_snapshot``) the Cognitive Core
(``app.goals.cognitive``) can inspect before planning, and the owner can ask
about directly (``GET /v1/world``). See ``app.worldmodel.state`` for the
four-truth-kind model this package exists to keep honest.
"""
