# Real-Environment Qualification

The milestone chain M-1…M9 built the system and proved it against fakes, fixtures and
loopback. This document tracks the different question: **does it work for its owner, on the
owner's real machine, real cloud, real browser, real voice and real phone?**

Every criterion carries exactly one status, and the rules for using them are strict:

| Status | Means |
|---|---|
| `PROVEN_REAL` | Demonstrated against the real thing — real OS mechanism, real remote host, real hardware, real network. |
| `PROVEN_PROXY` | Demonstrated against a stand-in (fake, fixture, injected identity, loopback, headless client). The logic is proven; the environment is not. |
| `NOT_YET_PROVEN` | Not demonstrated at all yet. |

A proxy result is never written up as real. Where a criterion is split — logic provable
here, environment provable only on the owner's machine — it appears twice, with the proxy
part marked `PROVEN_PROXY` and the environment part `NOT_YET_PROVEN` until it runs.

Last updated: 2026-09-02 (RQ-2 closed).

> **Local Windows qualification CLOSED 2026-09-01.** The final `finalize-qualification.ps1`
> run and `verify-device-service.ps1` report proved, on the owner's real machine: service
> Running as LocalSystem in Session 0; companion in the owner's Session 1; the live pipe's
> DACL read from the actual runtime pipe handle; kernel-sourced companion admission;
> install-tree posture; no inbound agent listener; DeviceService restart → reconnect → real
> Notepad → ACK; Cloud Core restart → reconnect → real Notepad → ACK; and a final owner
> credential rotated once, host-side, console-only. The qualified runtime is FROZEN — no
> speculative changes. Next: **real cloud bring-up** (Stage 5), where "Cloud Core" moves
> from loopback to Hetzner + Tailscale. Refusal legs that require a second real user
> account or concurrent session (1.4, 1.6, 1.7) deliberately stay `PROVEN_PROXY`, and
> 2.2b (fresh-logon autostart) stays open until a natural sign-out/reboot exercises it.


> **Cloud bring-up (RQ-2) CLOSED 2026-09-02.** On the owner's actual machines: `pagentos-core` (Hetzner, cpx32, nbg1) runs the production Cloud Core on `pagentos_prod`, reachable only over the tailnet (every public port refused, proven from outside); the Windows agent was moved to it without reinstall or re-enrolment; the headline path Hetzner → Tailscale → DeviceService (LocalSystem, Session 0) → Companion (Session 1) → real Notepad → ACK is `PROVEN_REAL`, with `command_received`/`command_ack` persisted and correlated to the real `command_id`/`trace_id`; recovery is `PROVEN_REAL` for Cloud Core process restart, VPS reboot (tailnet address, `/mnt/pagentos-data` and the device row all survived), Tailscale reconnect, Windows-side network loss and DeviceService restart. The cloud/device infrastructure is a FROZEN proven baseline: no reinstall, re-enrolment, device or owner recreation, or broker redesign unless a real observed defect requires it.

---

## Stage 1 — Session-0 service/companion IPC identity (M1 security finding #1)

The finding: under a real Windows Service the pipe's ACL and name both derive from the
*creating* process, so "ACL it to the current user" stops meaning "the owner" the moment the
service becomes LocalSystem — and there was no mutual authentication beyond that ACL.
Closed by ADR-0028 with four layers: DACL, first-instance creation, kernel-sourced peer
admission, and per-connection freshness.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1.1 | Pipe DACL names the owner account explicitly, not "whoever created me" | `PROVEN_REAL` | 2026-09-01 final run: the effective SDDL captured from the REAL pipe handle at creation (`ipc_pipe_created` audit) under the installed LocalSystem service names SYSTEM and the owner's SID — the two halves genuinely different accounts. Runtime proof, not source inspection. |
| 1.2 | Service refuses to share a pipe name another process already holds | `PROVEN_REAL` | `The_service_refuses_to_share_a_pipe_name_someone_else_already_holds` — a real pipe is created first by another process; the service records a listen failure and never serves on that name. **Corrected 2026-09-01:** this row previously credited `FILE_FLAG_FIRST_PIPE_INSTANCE` specifically. Independent verification showed the test passed with that flag deleted, and measurement then showed why: three mechanisms overlap (single instance, non-default security descriptor, the flag) and removing any one still refuses. The outcome is proven; attributing it to one mechanism was not, so the claim no longer does. |
| 1.3 | Companion refuses a pipe not owned by a service account | `PROVEN_REAL` | `The_companion_refuses_a_pipe_that_is_not_owned_by_a_service_account` — a real fake pipe, owned by a standard user, read through the real `GetSecurityInfo` path, refused, and not one frame written to it. **Corrected 2026-09-01:** this row previously said "refused by the production policy" while the shipped companion was in fact running developer trust (see 1.12); the mechanism was real, the claim about the binary was not. |
| 1.12 | The **shipped** companion runs at service-trust strength, not developer trust | `PROVEN_REAL` | `The_shipped_companion_defaults_to_service_trust_not_developer_trust` and `A_runtime_built_without_a_policy_refuses_an_owner_owned_pipe` — the entry point's own policy builder and the runtime's own default are both asserted, because the defect was that neither the class nor its test was wrong; the wiring was. Developer trust now requires `--dev-trust` and is logged as a warning. |
| 1.13 | Service and companion derive the same pipe name from the owner | `PROVEN_REAL` | `The_service_and_the_companion_derive_the_same_pipe_name_from_the_owner`. Under Session 0 the two halves are different accounts; a name derived from "the current user" would leave them on different pipes with nothing in either log saying why. |
| 1.4 | Peer running as another account is refused (wrong SID) | `PROVEN_PROXY` | Policy refuses `SidMismatch`; end-to-end over a real pipe with an injected identity. A second real user account is an owner action. |
| 1.5 | Peer in Session 0 is refused | `PROVEN_PROXY` | `A_peer_in_session_zero_is_refused_even_with_the_right_sid`. Reproducing a real Session-0 client requires the installed service. |
| 1.6 | Peer in a different interactive session is refused | `PROVEN_PROXY` | `A_peer_in_a_different_interactive_session_is_refused_when_the_session_is_pinned`. A second concurrent logon is an owner action. |
| 1.7 | Forged companion (right user, wrong binary) is refused | `PROVEN_PROXY` | `The_right_user_running_the_wrong_binary_is_refused`, plus refusal when the image cannot be read at all. Real proof needs the pinned installed binary. |
| 1.8 | Replayed frame within a connection is refused | `PROVEN_REAL` | `A_replayed_exec_response_does_not_satisfy_a_later_request` — a real captured frame re-sent over a real pipe; the service refuses it and the pending request still times out rather than being answered. |
| 1.9 | Stale credentials from a retired connection are refused | `PROVEN_REAL` | `A_frame_from_an_earlier_connection_is_refused_after_reconnect`, and `A_companion_hello_that_does_not_answer_this_challenge_is_refused` staged over a live pipe. |
| 1.10 | Refusals are audited with their reason and tell the peer nothing | `PROVEN_REAL` | `A_refused_peer_is_written_to_the_audit_trail_and_told_nothing` and `An_admitted_companion_is_written_to_the_audit_trail` — a real `AuditLog` on disk, asserted for the row, the reason and the refused SID, and asserted that the peer reads end-of-stream rather than any frame. **Corrected 2026-09-01:** this row previously cited the source line. Independent verification found no test ever passed an `AuditLog` to the server, so `_audit?.Write` never executed in the suite — confident evidence text, zero executing assertion. Now asserted. |
| 1.14 | The service's composition root builds the policy the install configures | `PROVEN_REAL` | `IpcWiringTests` — what `Program.BuildAdmissionPolicy` returns for a configured SID/binary/session, that both missing-configuration warnings actually reach stderr, and that the pipe name derives from the owner. Added because the Critical in 1.12 was a composition-root defect that no criterion covered. |
| 1.15 | A second connection cannot displace the connected companion | `PROVEN_REAL` | `A_second_connection_cannot_displace_the_connected_companion` — the intruder's connect fails while the companion keeps working. |
| 1.16 | The pipe-owner inspector reads a real owner and accepts a trusted one | `PROVEN_REAL` | 2026-09-01 final run: the companion read the installed service's ACTUAL SYSTEM-owned pipe through the real `GetSecurityInfo` path and connected — the acceptance half that no test process could stage. |
| 1.11 | Service runs as LocalSystem in Session 0 with the companion in the owner's session | `PROVEN_REAL` | 2026-09-01 final run: service Running as LocalSystem, SessionId 0; companion in the owner's Session 1, from the pinned binary — the exact separation the whole of Stage 1 exists for. |
| 1.17 | Only SYSTEM and Administrators can write to the installed tree | `PROVEN_REAL` | 2026-09-01, owner's machine: `verify-device-service.ps1` checked 201 objects under `C:\Program Files\PagentOS\agent` — no other principal holds write authority and **no empty DACLs remain**. The 425 files the earlier hardening had stripped were repaired by the installer itself, with no manual ACL reset. |
| 1.7b | The pinned companion binary is admin-protected and readable by SYSTEM | `PROVEN_REAL` | Same run: owner `S-1-5-32-544`, 3 inherited ACEs. This is what makes `CompanionImagePath` pinning mean something. |
| 5.2 | The agent owns no inbound listening socket | `PROVEN_REAL` | Same run, against the live companion process (pid checked explicitly, not a vacuous pass). |

## Stage 2 — Windows Service installation

**CLOSED 2026-09-01.** This stage cost nine real-machine failures (logged below) before the
final run went clean end-to-end; the log stays because the pattern — every defect in a code
path the tests did not execute — is the lasting lesson.

| # | Criterion | Status | Notes |
|---|---|---|---|
| 2.1 | Service installs, starts, restarts, reconnects unattended | `PROVEN_REAL` | 2026-09-01 final run: installed, Running as LocalSystem/Auto, and a forced restart reconnected and executed a real command with no owner intervention. (A full machine reboot has not been exercised yet; the Auto registration is real, the reboot proof will land incidentally.) |
| 2.2a | Companion runs in the owner's interactive session, from the pinned binary | `PROVEN_REAL` | 2026-09-01: session 1, `MAIL\alpak`, `C:\Program Files\PagentOS\agent\companion\PagentOS.SessionCompanion.exe`. |
| 2.2b | Companion auto-starts at a *fresh logon* | `NOT_YET_PROVEN` | The logon task is registered and has started the companion on demand, but a genuine sign-out/sign-in has not happened yet. Running now ≠ starts at logon; stays open until a natural logon proves it. |
| 2.2c | The service admits the companion on SID + session + binary | `PROVEN_REAL` | 2026-09-01 final run: `ipc_companion_admitted` audit from the installed runtime — kernel-sourced pid/session/image against the configured SID and pinned binary, with the two halves genuinely different accounts. |
| 2.3 | `desktop.open_application` executes in the interactive session from the Session-0 service | `PROVEN_REAL` | 2026-09-01 final run: broker command → Session-0 service → companion → REAL Notepad in Session 1, pid verified alive then closed, ACK and audit recorded. Proven twice (after a service restart and after a Cloud Core restart). |
| 2.4 | The installer is idempotent — a rerun after a partial or failed install succeeds | `PROVEN_REAL` | Claimed twice, disproved twice, then proven the hard way: nine distinct real failure states (see the log), each resumed by rerun — journaled `RetryFromStaging` recovery included — with enrollment, identity and key material preserved throughout. |
| 2.5 | Only SYSTEM and Administrators hold write authority over the installed tree | `PROVEN_REAL` | Same evidence as 1.17: the final `verify-device-service.ps1` allowlist sweep over the real tree — no other principal holds write authority, no empty DACLs. |
| 2.6 | Persisted machine material is usable by LocalSystem and closed to ordinary users | `PROVEN_REAL` | The half no test could reach is now real: the installed LocalSystem service read the device key and rewrote state across restarts, while non-elevated denial was already proven directly. Two domains held: machine material SYSTEM+Administrators only (key SYSTEM-read); owner material owner-scoped; the agent uses no DPAPI (checked, not assumed). |
| 2.7 | DeviceService restart → reconnect → real command → ACK | `PROVEN_REAL` | 2026-09-01 final run, unattended. |
| 2.8 | Cloud Core restart → agent reconnect → real command → ACK | `PROVEN_REAL` | 2026-09-01 final run, unattended. (Local broker; the same proof must be re-earned from the real Hetzner/Tailscale endpoint in Stage 5 and is NOT carried over.) |

### The install attempts so far, and what each disproved

Recorded in full because the pattern matters more than any single bug: nine real-machine
runs, nine defects, and **every one of them was in a code path my tests did not execute**.
None was found by the gate; all were found by the owner running the thing.

| Attempt | Failure | Cause | What it disproved |
|---|---|---|---|
| 1 | `sc create` exit 1639 | PowerShell 5.1 does not escape a native argument containing quotes, so `binPath` split at "Program Files". Only visible elevated — a non-elevated `sc.exe` fails at `OpenSCManager` first. | That the installer worked at all. |
| 4 | Service starts, then SCM 1067 | `UnauthorizedAccessException` on `device.key` in `DeviceIdentity.LoadOrCreate`. The key was created by an owner-context enrollment run and protected with a DACL naming **only that user**, so LocalSystem could not read its own identity. Service-owned material protected as if it were owner material. | That enrollment produced material the service could use. Nothing about the broker, the IPC layer or enrollment itself was wrong — the process died before reaching any of them. |
| 5 | Credential rotation committed, replacement lost | The wrapper ran `--rotate --json`, the child exited 0, and stdout carried **two** JSON documents: this application logs to stdout, and `bootstrap()` emits `identity_owner_credential_minted` — so the mint itself guaranteed the contamination. `ConvertFrom-Json` refused it and the wrapper threw *after* the rotation had committed. The replacement died with the child process. | That a wrapper checking the exit code is enough. The exit code was 0 and the operation had succeeded; the loss was entirely in the protocol between the two processes. |
| 2 | Access denied rewriting `appsettings.json` | Hardening with `/T` and `(OI)(CI)` grants left every pre-existing file with a protected empty DACL — 425 objects on the owner's machine — denying everyone including SYSTEM. | The idempotency claim. The tests behind it covered the create/reconfigure *decision* and never touched a hardened tree. |
| 3 | `Property 'Count' cannot be found` (`PropertyNotFoundStrict`) | A function returning an empty collection unrolls to `$null`; `.Count` on it throws under StrictMode, which the libraries set and dot-sourcing propagates. Reproduced: 0 → `$null`, 1 → `String`, 2+ → array. | The claim that the partial install was *fully recoverable*. It was not: recovery aborted before it began, and a rerun with exactly one component to restore would have failed too. |
| 6 | `Move-Item` access denied on the live service tree; companion left down; pipe server later found dead inside a "Running" service | NTFS refuses to rename a directory holding a running process's mapped images — the deploy never stopped the runtime first. Separately, the pre-fix `/T` icacls (reintroduced for ProgramData) emptied the log file's DACL and one failed log write from a `finally` killed the pipe server's `ExecuteAsync` while the host stayed Running. | That copy-over-live deployment was a deployment at all, and that service `Running` means health. Replaced by the journaled transactional engine (`Deployment.ps1`): verified stop before any move, same-volume renames, symmetric restart of both halves in `finally`, health = service AND companion AND pipe. Loggers may never throw. |
| 9 | Cloud Core started and answered healthy, then `dev-broker.ps1` died on `$health.dependencies` (`PropertyNotFoundStrict`) | The health schema is `{status, version, checks}` — `checks` is a map of subsystem→check; a top-level `dependencies` array **never existed**. The script inherited StrictMode from its dot-sourced libraries without declaring it, and read an imagined property. Orchestration display defect; the broker itself was healthy. | That direct access is acceptable for optional properties. `Test-ObjectProperty`/`Get-OptionalProperty` are now the explicit accessors (required properties stay direct so their absence fails loudly); the checks display warns on absent/empty rather than defaulting silently; StrictMode is declared, not inherited. Also fixed while proving the fix on the real machine: an elevated broker hides its command line from non-elevated queriers, so "already running" is now decided by process match OR a live port listener, and a marker file records which DATABASE each started broker serves — finalize reuses a running Cloud Core only when that is proven, never from health alone. |
| 8 | Transactional deployment COMMITTED (service + companion + pipe healthy), then broker-registration restore died on `ECDsa.ImportFromPem` | Windows PowerShell 5.1 runs on .NET Framework, which has no `ImportFromPem` (.NET Core 3+). Fourth member of the parses-fine-fails-at-the-call family (native quoting, StrictMode cardinality, `??`). | That a script may handle key material at all. ADR-0030: key handling only through the agent's own .NET implementation — new load-only `identity` verb (one JSON doc, distinct not-enrolled/key-missing exits, never mints); a gate lint fails any script calling .NET-Core-only crypto APIs; a PS5.1 gate suite enrolls a REAL key via the real exe against a loopback listener and proves the verb returns it byte-for-byte. `finalize-qualification.ps1` became phase-resumable so the committed deployment is never redone to reach step 2. |
| 7 | Finalize denied reading `state.json` — elevated administrator, access denied | `state.json` was rewritten at enrollment by the old binary's atomic tmp→move, so the fresh file carried only INHERITED ACEs; the earlier pre-fix `/T` run stripped inherited ACEs tree-wide, leaving a protected **empty** DACL. `device.key` survived because its ACEs were explicit — the exact signature of the bug class, timeline-proven from the owner's read-only inspection. | That ACL correctness can live in an installer repair instead of the **write primitive**. Now: every `AgentState.Save`/`IdempotencyStore` write re-applies explicit SYSTEM+Administrators protection after the move (asserted across replace), and `Restore-MachineStateAcl` repairs exactly the named state files — no recursion, no takeown, no new principals (ADR-0029). |

The corrective work is not only the three fixes. It is that the installer's top-level flow now
lives in tested functions (`Invoke-InstallRecovery`), that the cardinality pattern is linted
out of every installer script by the PowerShell parser, and that the tests assert which engine
they ran on — `installer-strictmode.tests.ps1` fails if it is not Windows PowerShell 5.1 with
StrictMode in force, so it cannot pass vacuously on a more forgiving host.

Read-only inspection of the owner's real tree with the fixed code (2026-09-01): the call that
threw now returns an empty array, the posture check reports 51 violations in a 61-object
sample, and 425 children are queued for the inheritance reset an elevated rerun performs.
That is evidence the code recognises the state — **not** evidence the repair succeeds, which
only the owner's elevated rerun can show.

## Stage 3 — Owner identity bootstrap and recovery

| # | Criterion | Status |
|---|---|---|
| 3.1 | Owner credential minted once, never in logs, source control or plaintext config | `PROVEN_REAL` — the final credential was displayed exactly once, in the local elevated console at rotation, and is used only via the DPAPI-stored session; the 2026-09-01 history investigation proved no credential-shaped value ever entered Git (root file stores hash only, never tracked), and the gate now enforces the shape permanently. |
| 3.2 | Host-side recovery rotates the credential without the API | `PROVEN_REAL` — four real rotations on the owner's actual root (including recovery from a lost-intermediate incident), `rotations` advancing, `created_at` stable, sessions revoked each time. |

## Stage 4 — GitHub private repository and CI

| # | Criterion | Status |
|---|---|---|
| 4.1 | Private repo exists and receives the branch | `NOT_YET_PROVEN` (blocked on `gh auth login`) |
| 4.2 | CI runs the full gate on a clean runner | `NOT_YET_PROVEN` |

## Stage 5 — Tailscale + Hetzner production deployment (RQ-2, CLOSED 2026-09-02)

Nothing here may be marked `PROVEN_REAL` from anything but the actual Hetzner host over
the actual tailnet. The local Cloud Core restart proof (2.8) is **not** carried over: it
was loopback, and the thing under test here is the network.

| # | Criterion | Status | Notes |
|---|---|---|---|
| 5.1 | Cloud Core runs on the real VPS | `PROVEN_REAL` | 2026-09-02: deployed to `pagentos-core` (Hetzner cpx32, nbg1). All 13 subsystem checks `ok` — db, redis, object_store, temporal, broker, artifacts, voice, memory, selfhealing, evolution, security, identity, mobile. Image built on the host from the checked-out source per ADR-0032. |
| 5.2a | This PC is on the tailnet, and still opens no inbound public port | `PROVEN_REAL` | 2026-09-01, owner's machine: Tailscale `Running`, `100.92.148.30` / `mail.tail0e6789.ts.net`. Same `verify-tailnet.ps1` run re-checked the posture rather than assuming it — the agent's own pids own **zero** listening sockets, and no enabled inbound firewall rule names PagentOS. |
| 5.2b | Windows PC ↔ Cloud over the private network | `PROVEN_REAL` | 2026-09-02: `http://100.90.158.26:8001/v1/system/health` answered from the Windows machine over the tailnet; `tailscale ping` reports a DIRECT connection via `2.28.67.130:41641`, ~47 ms, not a DERP relay. |
| 5.3 | No public application port on the VPS | `PROVEN_REAL` | 2026-09-02, probed from the Windows machine against the PUBLIC IP: 8001, 22, 5432, 6379, 9000 and 7233 all refused/filtered. On the tailnet address only 8001 answers — PostgreSQL, Redis, MinIO and Temporal are closed there too, because they publish no host port at all (`ss -tlnp` shows the API bound to 127.0.0.1 and 100.90.158.26 only, never 0.0.0.0). Public SSH stays closed; administration is Tailscale SSH. |
| 5.4 | The agent moves to the cloud endpoint without reinstall or re-enrollment | `PROVEN_REAL` | 2026-09-02: existing device row restored into `pagentos_prod` from the public identity document (same device id, capabilities preserved, no private key transported); installed service repointed to `http://100.90.158.26:8001` by the transactional switch. Device shows `online` at the cloud broker. | `switch-agent-broker.ps1` changes only the broker URL (health-probed first, atomic replace, rollback). The device keeps its id and P-256 key; the cloud DB row is rebuilt from the public half by `restore-device-row.sh`. |
| 5.5 | **Hetzner Cloud Core → Tailscale → DeviceService → Companion → real Notepad → ACK** | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet: command POSTed to the Hetzner broker, executed by the LocalSystem/Session-0 service via the Session-1 companion, real Notepad pid verified alive, ACK read back from the same broker. **Audit sub-row closed 2026-09-02:** `command_received` and `command_ack` persisted in the Windows audit JSONL and correlated to the exact real `command_id` and `trace_id` of that ACK (the earlier failure was the verifier reading `at`; the writer emits `ts`). |
| 5.6 | Cloud Core process restart → agent reconnects → command succeeds | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet; recovered with no owner action. |
| 5.7 | VPS reboot → agent reconnects → command succeeds | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet. Additionally proven across the reboot: tailnet address unchanged (non-ephemeral node), `/mnt/pagentos-data` re-mounted from fstab, device row survived in `pagentos_prod`. |
| 5.8 | Tailscale reconnect (link down/up) → command succeeds | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet (`tailscale down` / flagless `up` on the host). |
| 5.9 | Temporary network loss → recovery with no owner intervention | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet (tailnet down/up on the Windows side). |
| 5.10 | Windows Service restart against the cloud broker → command succeeds | `PROVEN_REAL` | Owner-reported real matrix from `qualify-cloud.ps1`, 2026-09-02, on the actual Hetzner host over the tailnet; re-earned across the real network, not carried over from 2.7. |
| 5.11 | Persistent state really lives on the Hetzner volume, not the boot disk | `PROVEN_REAL` | 2026-09-02: the guard EARNED ITS KEEP — the 100 GB volume was attached and formatted but **not mounted**, and `/mnt/pagentos-data` existed as an empty directory on the boot disk, so the deploy would have put PostgreSQL and the identity root on a disk a rebuild discards. Now mounted from `/dev/disk/by-id/scsi-0HC_Volume_106767183` with an fstab entry (`nofail`, so a missing volume can never hang boot); `findmnt` confirms data on `/dev/sdb` while root is `/dev/sda1`. |

## Stage 6 — M12 Realtime Voice Foundation (CURRENT; pre-registered, nothing proven yet)

Rules as everywhere in this file: `PROVEN_REAL` only on the owner's real Windows machine
with a real microphone/headset, real Turkish speech, the real network and the real Hetzner
Cloud Core (ADR-0034, `ACCEPTANCE_TESTS.md` §M12). The simulator and fakes gate the code;
they prove nothing here. Latency rows require the benchmark harness's numbers from the
real run, not impressions.

| # | Criterion | Status | What will count as proof |
|---|---|---|---|
| 6.0 | The realtime provider is live on the real Cloud Core: key present inside the running api container, provider selected in `/v1/system/health`, one real client-secret mint from the Hetzner host | `PROVEN_REAL` (2026-09-02) | Owner's release run on pagentos-core (ADR-0042): env key PRESENT, compose wired, api recreated, migration 0011 applied, container Healthy, key PRESENT in container, health providers `['openai-realtime']`, real mint from Hetzner OK. Re-read independently the same evening: `RELEASE` = fb9d52e (HEAD), health `ok`, providers `['openai-realtime']`, simulator disabled (`environment=prod`). Model `gpt-realtime-2.1`; local probe proved every M12 session layer live (ADR-0038 addendum). 2026-09-03: released again to c6f27c5 after the owner's 422 (contract drift, ADR-0045): the release qualification now proves the served `contract_version` equals the tree's (2) and that `marin` and `cedar` are each minted from the host and echoed unchanged by the provider; `/v1/voice/realtime/contract` answers 401 (owner-gated) from Windows where it answered 404 before. Nothing about audio is claimed here. |
| 6.1 | Primary conversation runs on a native speech-to-speech provider chosen by capability | `NOT_YET_PROVEN` | Session record shows the selected provider and its declared capabilities; no STT→LLM→TTS chain in the conversation path. |
| 6.2 | Natural Turkish conversation, simultaneous listen/speak as far as the provider permits | `NOT_YET_PROVEN` | Owner session transcript + owner evaluation (final gate). Vendor Turkish support is UNVERIFIED in docs — measured, never assumed. |
| 6.3 | Barge-in: owner interrupts, playback stops immediately | `NOT_YET_PROVEN` | Harness `barge_in_to_stop_ms` from the real run; target < ~150 ms where achievable; stop-first ordering in the client event log. |
| 6.4 | "dur" stops immediately; "devam" resumes correctly; "tekrar oku"; "ikinci maddeyi tekrar oku"; "biraz daha yavaş/hızlı"; "özet geç"; "detaya gir"; "burayı atla" | `NOT_YET_PROVEN` | Each intent spoken by the owner, resolved by the intent resolver, and the resulting narration/session state change observed. |
| 6.5 | Semantic end-of-turn: normal Turkish hesitation is not cut off | `NOT_YET_PROVEN` | False-barge rate on the real hesitation set (`VOICE_TURKISH_EVAL_SET.md`), recorded by the harness. |
| 6.6 | Turkish pronunciation/prosody; mixed TR/EN terminology; ı İ ğ ş ç ö ü | `NOT_YET_PROVEN` | The M12 terminology and phonetics sets spoken back; owner evaluation. |
| 6.7 | Microphone switching; headset/laptop/phone mics; AEC; noise suppression; noisy room | `NOT_YET_PROVEN` | Real device switch mid-session; a noisy-room run with the harness. |
| 6.8 | Network interruption → voice-session recovery | `NOT_YET_PROVEN` | Tailnet down/up during a session; reattach observed; no owner intervention. |
| 6.9 | Conversation continuity across desktop/web/mobile | `NOT_YET_PROVEN` | `attach` from a second client with state carried; mobile leg is M15. |
| 6.10 | Long-running tool: natural preamble, session alive, mid-task redirection changes the plan | `NOT_YET_PROVEN` | Real research request; preamble spoken; "Sadece OpenAI kısmına bak" produces `plan_changed` on the same plan. |
| 6.11 | Five latency metrics recorded against the real environment | `NOT_YET_PROVEN` | Harness JSON report from the owner's machine: mic→uplink, EOT→first audio (< ~500–700 ms target), barge-in→stop, tool preamble, tool done→speech; no unexplained gaps. |
| 6.12 | Modes explicit; VoiceIdentity never the sole root of authentication | `NOT_YET_PROVEN` | Code + a real session where identity augments, never replaces, device trust + owner session. |
| 6.13 | Provider credential never leaves Cloud Core; only the ephemeral per-session credential reaches a client | `NOT_YET_PROVEN` | Real session: client receives only the ephemeral value; audit rows carry ids/timings only. |
| 6.14 | Subjective owner evaluation of voice quality | `NOT_YET_PROVEN` | The owner says so, after the real session. |

Revised acceptance target after the first real session (owner feedback 2026-09-02,
`VOICE_OWNER_FEEDBACK.md`, ADR-0043/0044). Voice character and noise processing are
qualified TOGETHER in one session; a row passes only on the owner's word for the
subjective parts and on the session's benchmark for the counted parts.

| # | Criterion | Status | What will count as proof |
|---|---|---|---|
| 6.15 | Voice character perceptually close enough to the owner's Arbor target (closest supported voice + style profile + pacing) | `NOT_YET_PROVEN` | Owner's A/B verdict between the candidates (`marin`, `cedar`) with the Arbor profile applied; the winner recorded as the configured voice. |
| 6.16 | Normal room noise (fan, keyboard, mouse, desk knock, air conditioner, chair, hum) no longer causes distracting activations | `NOT_YET_PROVEN` | Noise matrix scenarios 1–6, 9–10: false speech-start and false-turn counts from the session benchmark near zero; owner confirms no distracting activations. |
| 6.17 | Background human speech (TV, another person) does not frequently command the system | `NOT_YET_PROVEN` | Scenarios 7–8 counted honestly; an owner-directed strategy evaluated, not assumed; residual rate reported, not hidden. |
| 6.18 | Speech preservation: the owner's Turkish stays natural and complete under the final processing (no clipped onsets, no swallowed consonants, no pumping, quiet and far speech still heard) | `NOT_YET_PROVEN` | Scenarios 12–14: missed-owner-speech and interrupted-utterance counts; owner confirms naturalness. |
| 6.19 | Echo: the assistant's own speaker output never becomes owner speech, in headset and open-speaker modes; barge-in still works while it speaks | `NOT_YET_PROVEN` | Scenario 11 in both modes: no loop, barge-in latency within target. |
| 6.20 | Semantic end-of-turn unchanged by the noise pipeline: normal hesitation still does not cause premature responses; barge-in stays fast | `NOT_YET_PROVEN` | Hesitation set and barge-in metric re-measured under the final configuration. |
| 6.21 | Browser processing verified by read-back on the owner's microphone; AGC chosen from measurement, not assumption | `NOT_YET_PROVEN` | Applied MediaTrackSettings/capabilities from the owner's browser recorded in the microphone profile; AGC on/off benchmark result recorded. |

## Stage 9 — M13 Real Browser + Research (pre-registered 2026-09-03; nothing proven yet)

Real only: the owner's actual Windows machine, actual Chrome, live Internet, the real
Hetzner Cloud Core over Tailscale. The local dev-topology proof (`scripts/e2e-m13-research.ps1`)
is recorded as `PROVEN_PROXY` where it runs the real browser and live sources but not the
real cloud.

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 9.1 | Installed agent advertises `browser.chrome` and the `browser.*` operations after the installer update; worker self-check passes on the owner's machine | `PROVEN_REAL` (2026-09-03 owner run of verify-device-service.ps1 after the engine-based installer: 6b.1 worker starts as the owner from the installed tree, Chrome available; 6b.2 service and worker agree on browser.chrome + 24 operations) | `verify-device-service.ps1` report; `GET /v1/devices` capabilities from Hetzner. |
| 6b.3 | The worker executes the installed release from the installed venv: self-check from a neutral cwd reports `module.file` inside `<root>\browser\.venv`, version/contracts/worker sha256/package digest equal to the installed source, and the site-packages copy is byte-identical to the source | `PROVEN_REAL` | **PROVEN_REAL 2026-09-04 (owner run):** the installer's staged and installed self-checks and the smoke's pre-Chrome check all proved release 0.3.0 with the module inside the installed venv and the package digest equal to the checkout. verify-device-service.ps1 6b.3 (owner, unelevated). Background: 2026-09-04 the installer reported success while the live worker stayed 0.1.0 (ADR-0050 item 16); 6b.1/6b.2 (verifier) measured that a worker starts and that the manifest agrees, not which copy runs. Local proof 2026-09-04: the stale venv was reproduced from the same staging path and refused by the new assertion; the fixed build was accepted; the dev-chain harness ran a packaged worker end to end. |
| 6b.4 | The companion's live worker is that release: newest `browser_worker_started` audit row names a running pid whose executable is `<root>\browser\.venv\Scripts\python.exe`, whose command line names the installed data dir, and whose hello carried the installed version and a module inside the installed venv | `PROVEN_REAL` | **PROVEN_REAL 2026-09-04 (owner run):** the installer committed only after the post-swap live-worker health check (INSTALL VERIFIED names the live pid and release 0.3.0), and the smoke's search/lifecycle session ran on that worker (`worker_pid` in the lifecycle identity, worker image under the installed tree, parent = the installed companion). verify-device-service.ps1 6b.4; the installer's health check enforces the same after every deployment (rollback + INSTALL FAILED otherwise). |
| 9.2 | Hetzner → Tailscale → DeviceService → Companion → worker → real Chrome: `browser.session_open` + `browser.fetch_evidence` of a public page succeed with metadata | `PROVEN_REAL` (2026-09-03 owner run of `real-browser-smoke.ps1`: session, example.com, inspect/extract, search, policy refusal, close — all correlated; installed owner-session worker) | Command rows with `command_id`/`trace_id`; companion audit; Chrome window observed. |
| 9.3 | First use case end to end: plan → device selection → discovery (APIs + `browser.search`) → ≥ 8 live sources fetched through Chrome → evidence → dedup/ranking → synthesis → report | `PROVEN_PROXY` (108 discovered, 12 fetched through real Chrome, 7 findings; cloud leg pending) | `GET /v1/research/{task_id}` stats; the report JSON. |
| 9.4 | Every `source_fact` traces to a fetched source with URL, title, publisher, date, retrieval time, excerpt, device command | `PROVEN_PROXY` (every source carries url/title/publisher/date/retrieved_at/excerpt/device_id/command_id) | Report `sources` cross-checked against `research_evidence`. |
| 9.5 | Durable artifact (JSON + Markdown + PDF/DOCX) with citations preserved; visible in the inbox | `PROVEN_PROXY` (artifact READY, pdf+docx renders, `[eN]` markers in the canonical body) | Artifact id, renders, citation markers in the PDF. |
| 9.6 | Episodic research memory written with provenance; no raw page text in memory | `PROVEN_PROXY` (episodic memory keyed `research:{task_id}`; no excerpt text in the value) | Memory id; inspect endpoint. |
| 9.7 | Website error vs browser error observed for real (one unavailable/blocked source recorded as a fetch failure without aborting the job) | `PROVEN_PROXY` (a 60 s page timeout recorded as a fetch failure without aborting the run, 2026-09-03 09:49) | `fetch_failed` ≥ 1 with `page_kind`/error class recorded. |
| 9.8 | Recovery on the real chain: Cloud Core restart or Tailscale interruption mid-job → job resumes, no duplicate evidence | `PROVEN_PROXY` (Cloud Core process killed mid-fetch and restarted; job resumed, evidence rows unique) | Workflow history; evidence row count unchanged. |
| 9.9 | Device selection: explicit Turkish target ("ev bilgisayarımda araştır") resolves to the enrolled machine; `no_capable_device` when the device is offline | `PROVEN_PROXY` (`ev bilgisayarımda araştır` resolved to the enrolled device; revoked devices excluded) | `POST /v1/devices/select` results. |
| 9.10 | Owner verdict: concise executive summary, 3–7 findings that matter, sources traceable | `NOT_YET_PROVEN` | Owner feedback recorded in the memory entry. |
| 9.11 | Owner's enrolled Chrome/Edge session drives a real authenticated site (contract v2, explicit research grant) | `NOT_YET_PROVEN` | Deferred: requires the owner's browser-session authorisation (OWNER_ACTIONS item 5). |
| 9.12 | `browser.search` on the installed worker uses Google as the primary provider and returns organic results with provider evidence (`requested_provider=google`, `provider=google`, `fallback=false`), DuckDuckGo only as a recorded fallback | `DEFERRED` (optional path) | **Not a Research blocker (owner decision, 2026-09-04):** DuckDuckGo is the production provider; Google stays available explicitly. **Google success not proven.** Owner run 2026-09-04 (`-Mode search`, installed worker 0.3.0, schema 2): `requested_provider=google`, Google genuinely attempted in the installed owner-session worker, `captcha interstitial detected; not answered`, `provider=duckduckgo fallback=True fallback_reason=google:captcha`, DuckDuckGo returned usable organic results. Google organic results remain to be observed; see 9.14 for what that run DID prove and 9.15 for the owner-handoff path. OWNER_ACTIONS item 12: `real-browser-smoke.ps1 -Mode search`; command_id/trace_id correlated; installed worker executed it. |
| 9.13 | Browser lifecycle invariant on the owner machine: one research job = one worker + one PagentOS-profile Chrome process + one window; every command proves reuse (`session_uid`, `browser_pid`, `worker_pid`, bounded `tab_count`); clean exit leaves no PagentOS-profile Chrome; a hard-killed worker takes its Chrome tree with it | `PROVEN_REAL` | **PROVEN_REAL 2026-09-04 (owner run of `real-browser-smoke.ps1 -UpdateAgentFirst -Mode lifecycle`):** installed/live worker release 0.3.0, `browser.search` schema 2, lifecycle identity present, all twelve operations reused one session (`session_uid_same=True`, `browser_pid_same=True`), one PagentOS Chrome process and window, tabs bounded and back to one, session close succeeded, PagentOS-profile Chrome processes left 0, owner's own Chrome windows before=1 after=1, `REAL BROWSER SMOKE: PASS`. OWNER_ACTIONS item 13: `real-browser-smoke.ps1 -UpdateAgentFirst -Mode lifecycle`. **Root causes of the 2026-09-03 incidents (both proven by experiment under a desktop window monitor, not inferred):** (1) `browser_agent.detect` ran `chrome.exe --version` to read the version; on Windows that starts the full browser with the default profile, the window outlives the caller (probe: 1 Chrome for Testing root, 2 visible windows, 11 processes still alive after the probe exited), and with the owner's Chrome running it opens a new window in the OWNER's Chrome - this ran on every worker start, self-check, companion restart and test fixture (the "Chrome for Testing" windows were the browser test suite's worker fixtures, 9 visible windows in one run). (2) A second `launch_persistent_context` on the dedicated profile while a PagentOS Chrome already holds it fails within 0.1 s AND opens a new window in that Chrome; an orphaned worker Chrome plus any retry source added one window per attempt. **Fixes:** detection reads the PE version resource and never starts a browser (`tests/unit/test_detect_no_launch.py`); single owned research browser with orphan reaping, OS-level launch lock (named mutex), launch-rate circuit breaker with durable fault, Windows Job Object kill-on-close, ownership record, `browser_lifecycle_violation` instead of retried launches, bounded tabs, companion/installer cleanup of profile-bound Chrome only; browser tests are headless-only outside `live` and reap their own Chromium (`services/browser/tests/conftest.py`). **Headless regression evidence (2026-09-03):** `tests/browser/test_lifecycle_e2e.py` - 20 sequential ops on one `browser_pid`/`session_uid`, tab budget refused before opening, second session id refused, foreign holder reaped then exactly one recovery launch, breaker trips and blocks clean launches durably, timeout + cancellation keep one browser, second worker process refused by the lock, `taskkill /F` on the worker removes the Chrome tree; whole suite run under the window monitor with 0 visible Chrome for Testing windows and 0 processes after. **Local real-chain evidence (harness run 15, 2026-09-03 21:49-21:58):** `scripts/e2e-m13-research.ps1 -Synthesis deterministic` PASS on every step (dev stack, agent build + self-check, Cloud Core, enrolment, companion online, selection, browser smoke, Google-primary search smoke, real research through Chrome + live Internet 303 s, artifact + memory, recovery with a Cloud Core restart mid-job 156 s) while a 2 s process/window sampler (220 samples) saw at most 1 PagentOS-profile Chrome root and 1 window, the owner's own Chrome constant at 1 window, 0 Chrome for Testing processes, and 0 PagentOS Chrome after the run; the installed DeviceService (running) and Session Companion (restarted through its logon task after the owner's containment, IPC pipe present) were verified afterwards. |
| 9.14 | Provider abstraction on the owner machine: Google is the primary requested provider, Google is actually attempted in the installed owner-session worker, CAPTCHA/interstitial detection, deterministic DuckDuckGo fallback, fallback reason/evidence recorded | `PROVEN_REAL` | Owner run 2026-09-04: worker 0.3.0, schema 2, `requested_provider=google`, `provider=duckduckgo`, `fallback=True`, `fallback_reason=google:captcha`, attempt detail `captcha interstitial detected; not answered`, DuckDuckGo organic results; no retry loop, no bypass. |
| 9.15 | Owner verification handoff on the owner machine: on a Google interstitial the PagentOS Chrome window comes to the front, the page stays as it is, the SAME session (`session_uid`, `browser_pid`) stays alive while it is polled, and then EITHER the owner completes it and the pending search is retried exactly once, OR the timeout expires and the owner's chosen fallback is recorded as `provider=duckduckgo, fallback=true, fallback_reason=google:verification_timeout, verification.outcome=timeout` with organic results and a clean session close | `DEFERRED` (optional path) | **Not a Research blocker (owner decision, 2026-09-04):** the handoff is implemented, headless-proven and preserved; it is exercised only when the owner asks for the Google path. Partially observed 2026-09-04 (owner run, 600 s): interactive mode entered, interstitial detected, `WAITING_FOR_OWNER_VERIFICATION`, window brought to the front, the same session stayed alive while `browser.wait for=verification_cleared` polled, no new browser lifecycle - then the smoke failed building the fallback payload (duplicate `interstitial` key, ADR-0050 item 18), so the fallback path itself was never exercised. Fixed in worker 0.4.0 / search schema 3 with fixed-key evidence builders; headless proxies: `tests/browser/test_google_ui_e2e.py` (captcha and consent: pending, cleared, resume, timeout fallback, repeat fallback, owner declines, interstitial named exactly once) and `scripts/tests/browser-smoke-evidence.tests.ps1` (9 cases, duplicate keys structurally impossible). Owner action: item 14 with a short `-HandoffTimeoutSec`. 
| 9.16 | Research on the owner machine with the production policy: DuckDuckGo discovery (`requested_provider=duckduckgo`, `provider=duckduckgo`, `fallback=false`), one persistent owner-session Chrome for the job, real source pages opened and extracted (not snippets), dedup across repeated coverage, publication-date awareness,every source keeping title/URL/publisher, executive summary first, why each finding matters, durable command/trace evidence, clean session close, and NO deployment when the installed release already matches | `PROVEN_REAL` | Second owner attempt 2026-09-04 reached ranking again (238 discovered, 12 fetched/ranked, 0 quarantined) and failed on `KeyError: 'label'` - a model statement without its provenance label read by raw key (ADR-0050 item 21). Fixed with named, versioned entity schemas (required/optional/derived), per-item quarantine for statements and detail sections, and a boundary audit that forbids raw indexing of producer payloads anywhere in the research package; research policy version 3. First owner attempt 2026-09-04 (`f6eb5021`) reached ranking with 243 candidates discovered and 12 fetched/ranked, then failed on a typed-data defect (a model answered `finding.importance` with Turkish prose; ADR-0050 item 20). Fixed with declared field contracts, per-candidate quarantine and a deterministic-synthesis fallback; research policy version 2. OWNER_ACTIONS item 15: `scripts/research/owner-research.ps1`. Local proof 2026-09-04: dev-chain harness (packaged worker, DuckDuckGo policy) and unit/integration suites. | **PROVEN_REAL 2026-09-04 (owner run 17:28-17:33 UTC, `research-1.json`):** `OWNER RESEARCH: PASS`, findings=5, sources=5, distinct publishers=5, PagentOS Chrome before=0 after=0, deployment skipped (installed 0.4.0 matched), 240 discovered, 33 fetched, 28 refused by reason (off_topic 9, interstitial 11, date_uncertain 3, duplicate_event 1, outside_recency_window 4), one Cloud Core release to policy 4. Backlog (non-blocking, ADR-0050 item 24): some accepted findings were broad AI developments rather than agent-specific ones; semantic intent scoring is later work. Third owner attempt 2026-09-04 reached `ready` with 12 fetched/ranked items and produced ZERO findings: an off-topic/old/interstitial mix was ranked as evidence and an empty report was published (ADR-0050 item 22). Fixed with the pre-synthesis quality gate (topic, recency, page validity, duplicates), gate verdicts persisted onto evidence rows, and a findings contract with a floor of 3 attributable findings enforced on every provider; research policy version 4. |
| 9.17 | Deployment is separate from execution: with the installed worker already at this checkout's release, an owner run performs no install, no staging/swap and no service restart, and says so; a genuine difference is named and deployed only then | `PROVEN_REAL` | **PROVEN_REAL 2026-09-04:** the owner run recorded `deployment.checked=true, deployed=false, installed_release_current=true, contract_compatible=true, checkout_release=0.4.0, installed_release=0.4.0` - no install, no staging/swap, no service restart. Proven inside OWNER_ACTIONS item 15's run (the evidence JSON records `deployment.installed_release_current=true, deployed=false`). Headless proxy: `scripts/tests/agent-release-currency.tests.ps1` (7 cases). |
| 9.18 | Evidence contract fault isolation on the owner machine: a candidate or finding that violates its declared field contract is quarantined with its reason (entity, field, expected type, observed value class, stage), its raw evidence is retained, the run continues on the valid remainder, and the run fails only when fewer than 3 valid items remain | `PROVEN_REAL` | **PROVEN_REAL 2026-09-04:** the owner run completed on the contract layer with the quarantine machinery live (`quarantined=0`, nothing malformed reached synthesis; the fault-isolation legs remain covered by the headless suites). Proven inside OWNER_ACTIONS item 15's run (the evidence JSON carries any `quarantined` counts). Headless proxies 2026-09-04: `tests/unit/test_research_contracts.py` (49 cases incl. the incident value, Turkish prose/quotes/dates/numbers-in-text, missing scores, malformed structures and the number-as-title inverse) plus synthesis/evidence pipeline tests. |
| 9.19 | Research quality gate on the owner machine: off-topic, out-of-window, undated, interstitial and duplicate pages are refused BEFORE synthesis with named reasons whose counts appear in the report, the surviving evidence is deduplicated, and the report carries 3-5 findings that each cite evidence and say why they matter - or the run fails as `insufficient_valid_findings` rather than publishing an empty answer | `PROVEN_REAL` | Proven inside OWNER_ACTIONS item 15's run (the evidence JSON carries `stats.rejected_by_reason` and the findings). Headless proxies 2026-09-04: `tests/unit/test_research_eligibility.py` (46 cases), `tests/unit/test_research_regression_20260904.py` (11 cases built from the real failed run: the Granite model card, both arXiv abstracts, the "Bir dakika lutfen..." interstitial, duplicate coverage, and a fluent summary with no findings), and the incident cases in `tests/unit/test_research_browser_activities.py`. | **PROVEN_REAL 2026-09-04 (owner run 17:28-17:33 UTC, `research-1.json`):** `OWNER RESEARCH: PASS`, findings=5, sources=5, distinct publishers=5, PagentOS Chrome before=0 after=0, deployment skipped (installed 0.4.0 matched), 240 discovered, 33 fetched, 28 refused by reason (off_topic 9, interstitial 11, date_uncertain 3, duplicate_event 1, outside_recency_window 4), one Cloud Core release to policy 4. Local proof 2026-09-04: the dev-chain harness against real Chrome and the live web (`scripts/e2e-m13-research.ps1`) - 251 discovered, 24 fetched, 19 refused (off_topic 10, interstitial 8, outside_recency_window 1), 5 evidence, 5 findings each citing a distinct source, plus recovery after a Cloud Core restart mid-job. Three defects that only a live run could show were fixed first (ADR-0050 item 23). |

## Stage 10 — M16 Activity Ledger + Self Explanation + Voice Narration (pre-registered 2026-09-04; nothing proven yet)

Real only: the owner's real Windows machine, microphone and headset, Turkish speech, the
web voice shell against the real Hetzner Cloud Core. The headless proxies (simulated
realtime session over the real control plane, the real 2026-09-04 research run as
evidence) are gates, never acceptance. No seeded or demo events exist.

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 10.1 | "Son yaptıklarını anlat" is answered from the activity ledger: the briefing names the real research qualification, its counts and its refusals, every sentence a known fact with an evidence reference; nothing invented | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** the voice UI answered evidence-backed self explanation; the owner heard the real research task identity, the discovered/fetched/evidence/rejected counts, installed browser worker 0.4.0, the research policy and that deployment was skipped - all of it from the ledger, none of it seeded. The automated acceptance for this row was brittle (it matched a Turkish sentence prefix); it now verifies STRUCTURE - cited ledger event ids, the research job they belong to, resolvable evidence kinds and numbers equal to the run's own record (ADR-0051 addendum 4). Re-verifiable any time with `owner-explain.ps1 -VerifyOnly`. `explain-1.json` → `activity.tool_calls[activity.explain]` with `level=executive`, `facts ≥ 5`, `uncertainties = 0`, `evidence_count ≥ 1`, `speech_head` starting "Efendim, son ara…"; the `research.qualified` ledger event carries the SHA-256 of the owner's `research-1.json`. Headless proxy 2026-09-04: `tests/unit/test_explain_engine.py` (16, incl. the exact owner sentence and the no-verdict/no-evidence cases), `tests/unit/test_voice_explain_tools.py`. |
| 10.2 | "Araştırmayı detaylandır" reads the actual findings (title, summary, why it matters, source) and "Teknik anlat" the technical evidence (versions, counts, ids) from the same briefing artifact | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** "detaylandır" and "teknik anlat" produced the detailed and technical explanations in the real session. The intents are now normalised whichever tool the provider routes them to. a tool call (`narration.control` or `activity.explain` - the provider chooses the route, the record carries the normalised intent) with `intent=detail`/`technical`, `action=jump_level`, `speech_chars > 0`; the artifact body sections `# Ayrıntı` / `# Teknik`. |
| 10.3 | "Dur" stops speech at once (client stop-first ordering unchanged) and the narration cursor is placed at the first sentence not fully spoken, from the transcript the client reported - never stored | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** "Dur" stopped active speech and the narration cursor was persisted. `activity.client_events`: `spoken(final=0)` immediately before `barge_in_start`, `aligned=1`, `action=paused`, `spoken_chunks`; no `text` key in any audit row; ledger `voice.narration.paused`. Headless proxy: `tests/unit/test_narration_align.py` (7), the spoken-never-audited test; "Dur" idempotence (a second "dur", or one after the answer finished, is a quiet state change, never an error) in `tests/unit/test_voice_explain_tools.py` and the web client's `dur.test.ts`. |
| 10.4 | "Devam et" resumes from that cursor - the same semantic point, not a replayed offset | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** "Devam et" resumed from the paused semantic position. `activity.tool_calls[narration.control]` with `intent=resume`, `narration_state=READING`, `speech_head` equal to the sentence after the last fully spoken one. |
| 10.5 | The ledger is backfilled only from canonical rows (research runs/reports, voice audit, releases, incidents), idempotently, each event referencing its source; a re-run records nothing new | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05** by `owner-explain.ps1 -VerifyOnly` against the real Cloud Core: the backfill examined 33 canonical rows (research_failed 2, research_completed 2, research_quality_gate 4, voice_session 25), created NOTHING and skipped all 33 - `total_created: 0`, which is the criterion. Recorded in `explain-1.json`. Superseded evidence note: | The backfill runs inside the owner command and its report is written to the evidence file; the run that would have captured it failed later, at the session step, so no durable copy reached the repository. `owner-explain.ps1 -VerifyOnly` (about fifteen seconds, no talking) records it. `explain-1.json` → `backfill` report from the real Cloud Core; a second backfill in the same run reporting zero new events. Headless proxies: `tests/unit/test_ledger_service.py`; real dev database `tests/integration/test_ledger_explain_over_real_runs.py` (backfill of the actual research runs, second run creates zero, the briefing states their counts from their rows). |
| 10.6 | The completed session is recorded end to end (explanation, pause, resume) in the ledger and the session activity record; the session closes cleanly | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05** by `owner-explain.ps1 -VerifyOnly` reading the owner's completed session `b09d5cb5` (started 2026-09-04T20:56:28Z): 8 tool calls and 161 client events; the ledger carries `voice.explained` x14 and `voice.narration.paused` x7; session `state=closed`. Superseded evidence note: | The session completed on the owner's machine, but the harness failed before it read the durable record, so the end-to-end evidence was never captured. `owner-explain.ps1 -VerifyOnly` reads the completed session and closes this row without another voice test. `GET /v1/ledger/events?subsystem=voice` with `voice.explained` and `voice.narration.paused`; session `state=closed`. Final line `OWNER EXPLAIN: PASS`. |
| 10.7 | While the assistant speaks, another person's distant speech in the room does NOT stop it; "Dur" (and bekle / sus / kes / bir dakika) stops it at once; a real owner interruption is honoured | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** background human speech in the room did not stop the narration, and explicit "Dur" stopped it immediately. `explain-1.json` → `activity.noise`: `rejected_background_speech ≥ 1` during the distant-voice step, `explicit_stop_command ≥ 1` or an accepted interruption for "Dur", `false_interruption = 0`; the owner's verdict that it kept speaking over the other voice. Headless proxy: the web client's `interruption.test.ts`. |
| 10.8 | Spoken answers respect the listening budgets: executive two to four sentences (~10-20 s), detailed cut at a sentence boundary (~30-60 s), technical concise; only "hepsini oku" reads everything; the cursor keeps section/item position across pause/resume | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** the concise executive briefing and the detailed and technical levels were heard as intended. `activity.tool_calls[activity.explain].speech_chars ≤ 420`; `intent=technical` call `speech_chars ≤ 700`; the owner's verdict on length. Headless proxies: `test_explain_engine.py` (budgets), `test_voice_explain_tools.py` (full read, level routing). |
| 10.9 | "Son yaptıklarını anlat" summarises owner-relevant activity - never the previous explanation or voice bookkeeping - and says whether anything needs the owner | `PROVEN_REAL` | **PROVEN_REAL 2026-09-05 (owner's real session, reported by the owner):** the briefing summarised the research qualification rather than the previous narration. The executive briefing names the research qualification while newer `voice.explained` / `voice.session.*` events exist in the ledger. Headless proxy: `test_the_explanation_itself_never_leads_the_next_explanation`. |

### Stage 10 status after the 2026-09-05 `-VerifyOnly` run

**Owner decision, 2026-09-05:** accept the direct real-session evidence and close these as
`PROVEN_REAL`: Activity Ledger, Self Explanation, Voice Narration, concise executive
narration, detail/technical narration, background-human-speech rejection, explicit `Dur`,
narration cursor persistence, semantic `Devam et`. Each rests on its own durable evidence,
not on the provenance re-check.

The structural-provenance re-check MECHANISM is recorded separately as
`NOT_YET_PROVEN / DEFERRED`, because the only completed owner session predates the
deployed provenance block. That is a gap in the verifier's own coverage and is explicitly
NOT a failure of the capabilities it verifies. No production release is to be performed to
exercise it. When a future legitimate Cloud Core change requires a normal release, the
already-committed provenance implementation ships with it, and the next ORDINARY real
Voice/Self Explanation session after that release closes the mechanism with no dedicated
qualification.

All ten rows are `PROVEN_REAL`. 10.1-10.4 and 10.7-10.9 were closed on the owner's real
session; 10.5 and 10.6 were closed by re-reading that same completed session, with no new
voice session and nothing spoken.

**One acceptance MECHANISM remains unexercised, and it is not a product gap.** The
structural-provenance re-check (ADR-0051 addendum 4) cannot run against session
`b09d5cb5`: the code that emits the `provenance` block on a tool-call record landed in
commit `3603555` at 2026-09-04T21:12Z, and the session started at 2026-09-04T20:56Z -
sixteen minutes earlier. The deployed Cloud Core (`547a5a3`) does not contain that code
either. So the three provenance checks fail on that session for a reason no harness fix can
remove, while every behavioural check in the same run passes. `explain-1.json` records this
as `diagnosis: provenance_not_recorded_by_the_build_that_ran_this_session`.

To exercise it: deploy a Cloud Core built from a commit at or after `3603555`, then let it
ride along with the next ORDINARY voice session. It needs no qualification of its own and
the owner is not to repeat the long test for it.

## Stage 11 — M17 Cognitive Foundations (pre-registered 2026-09-05; nothing PROVEN_REAL yet)

Everything below is `PROVEN_PROXY` at best: automated tests and dev-database runs. None of
it has been heard by the owner in a real Turkish voice session, so none of it is
`PROVEN_REAL`. The proxies are gates, never acceptance. The one thing this stage does NOT
need is another long owner qualification: these rows ride along with the next ordinary
voice session.

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 11.1 | "Ne öğrendin?" is answered from compiled lessons with the incidents that produced them; a lesson with no evidence is never spoken as fact | `PROVEN_PROXY` | Unit: `test_experience_engine.py` (8), `test_experience_compiler.py` (11), `test_experience_routes.py` (10); the Phase-9 branch in `test_explain_engine.py`. Real: the owner hears a lesson naming a real incident. |
| 11.2 | "Kendi üzerinde ne geliştiriyorsun?" / "Canlıya alınmayı bekleyen ne var?" answer from the opportunity backlog, and a SHADOW_READY candidate is named as ready-but-not-deployed | `PROVEN_PROXY` | Unit: `test_evolution_backlog.py` (216), `test_evolution_boundaries.py`, the evolution branches in `test_explain_engine.py`. Real: the owner hears the shadow candidate and that it awaits approval. |
| 11.3 | The lab cannot reach production by any path: no lab-issued authority holds a production grant, `OWNER_APPROVED` needs an owner-session capability, `LIVE` needs an owner-approved release, root policies are immutable from lab code | `PROVEN_PROXY` | Unit: `test_evolution_authority.py`, `test_evolution_boundaries.py`, `test_evolution_guards.py`, plus the exhaustive illegal-transition matrix. This row is structural: it is proven by the code refusing, not by an owner watching it refuse. |
| 11.4 | Every backlog transition writes exactly one ledger event; no status maps to nothing | `PROVEN_PROXY` | Unit: `test_every_lifecycle_status_writes_exactly_one_ledger_event`, plus the quarantine/superseded/test-failure regression tests (2026-09-05). |
| 11.5 | The world model never reports a source-only fact as installed or runtime truth, and says "I don't know" rather than answering from a stale index | `PROVEN_PROXY` | Unit: `test_worldmodel.py` (19), `test_selfmodel_query.py` (26). Two real defects of this class were found by independent review on 2026-09-05 and fixed with regression tests. |
| 11.6 | The self model indexes only files inside the checkout - a junction or symlink out of the tree is never read or parsed | `PROVEN_PROXY` | Unit: `test_selfmodel_indexer.py` (24), including the containment regression. |
| 11.7 | "Diagnostic Observer'da sorun ne?" and "Bu özelliği neden geliştirdin?" are answered with module provenance, separating known fact from inference | `PROVEN_PROXY` | Unit: `test_selfmodel_routes.py` (13), `test_explain_engine.py`. Real: the owner hears the distinction stated aloud. |
| 11.8 | Goals progress through the cognitive loop with evidence-checked success criteria; the Critic can send a step back; every state change is in the ledger | `PROVEN_PROXY` | Unit: `test_goals_service.py` (26), `test_cognitive_loop.py` (12), `test_goals_routes.py` (15). |
| 11.9 | Every new subsystem publishes truthful UI state (ADR-0052) with no content, no transcript and no audio sample; `progress` is null where unknown | `PROVEN_PROXY` | Unit: `test_uistate.py` (11) plus the per-subsystem publisher tests. The renderer does not exist yet by decision. |
| 11.11 | The Evolution Engine drives a real candidate from IDEA to SHADOW_READY through the real service, records every transition in the ledger, and stops there: not deployed, not approved, wired to nothing | `PROVEN_PROXY` | The Acceptance Wording Guard, built from two real recorded incidents. `services/api/lab/candidates/acceptance_wording_guard/evidence/lifecycle_run.json`: seven `evolution.*` ledger events, six UI states, four evidence refs all `verified: true`, composite 0.577, ending at `evolution.owner_approval_required` with `approved_by: null`. It lives outside `app/`, outside the wheel (`packages = ["app"]`), has no `__init__.py`, and no production module imports it - asserted on the import graph via `ast`, not by grep. Unit: `test_lab_acceptance_wording_guard.py` (42). |
| 11.12 | An in-process caller cannot forge production authority by subclassing, `dataclasses.replace`, pickle, or rebuilding the object around its constructor | `PROVEN_PROXY` | The critical finding of the 2026-09-05 security review, reproduced and then closed. `test_evolution_authority.py`: subclass refused, forged instance refused for every production action, pickle round-trip cannot promote, and a genuine owner authority still works. In-process containment against arbitrary code is explicitly NOT claimed (ADR-0053 addendum 1). |
| 11.10 | No subsystem added in M17 starts a background loop at application startup | `PROVEN_PROXY` | `app/main.py` wires routers only; asserted by inspection and by the absence of any scheduler registration. Re-check on any change to startup. |

### Stage 11 CLOSED — M17 is PROVEN_REAL (2026-09-05)

The owner's third M17 voice run passed: `OWNER EXPLAIN: PASS`, all six cognitive paths
reached by voice and answered from real durable data. `PROVEN_REAL` and closed for:

* Memory + Experience Compiler
* Goal Engine (answering "no active goals", which is the true state)
* World Model (four truth kinds, uncertainty and staleness stated)
* Self Model / Code Intelligence
* Evolution Engine foundation
* production-authority knowledge and boundary
* voice access to all of the above

Not to be reopened without a genuine regression. It took three attempts, and the first two
failed on the CHECKER rather than the product — the record of that is below, because the
defect class it exposed (acceptance depending on metadata the system never wrote down) is
the same one this milestone was built to remove.

### Stage 11 after the owner's first M17 voice run (2026-09-05) — FAILED, and why

The run is worth recording in full, because four of its five findings were defects in the
CHECKER rather than in the product.

Real session `0b186069` (closed cleanly, 5 tool calls, 102 client events):

| Question | what really happened |
|---|---|
| ne öğrendin | answered from 4 real lessons, 19 facts, 4 uncertainties |
| hangi hedeflerin var | "Kayıtlı bir hedefim yok." — correct, no goal exists |
| ne görüyorsun | **`activity.explain` FAILED**, `error_class=internal_bug` |
| kendi kodun | answered, 222 real modules |
| gece ne geliştirdin | answered, the real SHADOW_READY candidate |
| canlıya alabilir misin | **no tool call was made at all** |

Yet the harness reported memory, goals, the self model and evolution as "not reached by
voice". Four separate causes, now fixed:

1. **Routing metadata, not routing.** Every call recorded `query_kind=""`, because the
   field was read from the narration intent resolver — which resolves controls like "dur"
   and returns `None` for a question. The engine knew the kind all along and never wrote it
   down. Briefings now carry a `cognition()` record (query_kind, the subsystem that
   answered, counts, evidence kinds, entity ids) and the durable row reads it from there.
   Nothing infers a subsystem from generated Turkish.
2. **The World Model crash was real**, and was ours: the branch shadowed the briefing's
   `facts` accumulator (a dict) with a local list, so `provenance()` called `dict(<list>)`
   during persistence. `Briefing` now refuses a non-mapping `facts` where it is built.
3. **The sixth question never reached the tool.** "Bunu canlıya alabilir misin?" reads as a
   request for permission, and the model answered it conversationally. Whether this system
   may deploy is a fact about policy and never the model's to assert; the tool contract and
   persona now name authority questions explicitly and forbid answering from belief.
4. **The harness inherited M16 acceptance** — research-job provenance, the listening
   budget, `teknik anlat`, the interruption counters, `dur`, `devam et` — and failed M17 for
   the absence of steps nobody performed. M17 now asserts only its own six capabilities,
   names exactly which question produced no answer, and distinguishes "never routed" from
   "routed and the tool failed".

**Structural re-verification against the live production database (Cloud Core `b6b64ef`):
ALL SIX PASS.** Each routes to the intended kind, is recorded against the intended
subsystem, survives persistence, carries facts or an explicit uncertainty, and cites its
OWN subsystem's records — `world_fact`/`world_snapshot`, `code_module`/`code_index`,
`evolution_opportunity`, `authority_policy`/`root_policy`. No cognitive answer is required
to cite a research job; research provenance belongs to research answers.

`goals` cites nothing and carries one uncertainty. That is the honest shape of an empty
subsystem, and it is a PASS.

### Stage 11 status after the 2026-09-05 production bring-up

M17 is DEPLOYED (Cloud Core `871d9c3`, migrations 0016 and 0017) and every one of the
owner's six questions was answered from the REAL production database, verified by running
the real explain engine inside the deployed container against the live rows:

| Question | kind | answered from | spoken |
|---|---|---|---|
| Son yaşadığın hatalardan ne öğrendin? | `learned` | 4 compiled lessons, from production's own incidents and ledger | 482 chars |
| Şu anda hangi hedeflerin var? | `goals` | the goal engine, which is empty | "Kayıtlı bir hedefim yok." |
| Kendi sisteminde şu anda ne görüyorsun? | `world_state` | 18 observations across ALL FOUR truth kinds, 2 uncertainties | 171 chars |
| Kendi kodun hakkında ne biliyorsun? | `self_code` | 222 real indexed modules, 3813 symbols | 92 chars |
| Gece kendi üzerinde ne geliştirdin? | `evolution` | the real SHADOW_READY Acceptance Wording Guard | 162 chars |
| Bunu canlıya alabilir misin? | `can_deploy` | the authority module itself | 299 chars |

**"Kayıtlı bir hedefim yok" is a PASS, not a gap.** No goal has been created, and inventing
one to make a qualification look better is precisely what this milestone forbids. The check
is that the Goal Engine is reached and answers honestly, which it does.

Everything above is `PROVEN_PROXY` in the strict sense the matrix uses - it is real durable
data, but the owner has not heard it. The one thing structural verification cannot prove is
REACHABILITY: that the owner can ask these questions out loud and be understood. That is
what `owner-explain.ps1 -M17` is for, and it is the only thing it is for.

Not deployed and not approved: the Acceptance Wording Guard remains `SHADOW_READY`.

## Stage 12 — M18 Holographic Core, Active Eye, ambient presence (CLOSED 2026-09-06: 18 rows PROVEN_REAL from the owner's runs, reconciled from the durable record; 12.13 media playback and 12.19 display-off deliberately deferred to their own owner runs)

**M18 CLOSED 2026-09-06.** Every row the milestone's owner runs could prove is `PROVEN_REAL`
from the durable record; the structural rows (12.2, 12.3, 12.9, 12.11, 12.14, 12.15, 12.18, 12.20)
are proven by code refusing, as registered. Two rows are NOT closed and are not claimed: 12.13
(an owner-selected media action plays the named item - never exercised for real; its own short
owner run, when the owner wants it) and 12.19 (display-off - deliberately separate, gated twice).
The record also settles the earlier monolithic failures: every `FAIL` the long harness printed
on 2026-09-06 was a qualification-harness or session-correlation defect (a readiness filter, a
closure scope, a `.Count` unroll, a watcher that started after the transition) while the durable
evidence proved the underlying capability independently - see ADR-0064.

M18 closes from combined durable evidence, not from one monolithic run (owner directive,
2026-09-06): `scripts/core/reconcile-m18.ps1` evaluates each row from the Cloud Core's own
record - sessions, tool calls, receipts, ledger rows, routine firings - and its output for
the owner's final run is kept at `docs/evidence/m18-reconciliation-2026-09-06.json`. A
`PROVEN_REAL` cell below names that evidence first and keeps the earlier proxy evidence
after it. What is still `PROVEN_PROXY` was, at registration, `PROVEN_PROXY` at best: This stage is unusual in how far proxy is from
real: every claim here is about the owner's actual room, their actual camera and their
actual production system, and a fixture cannot be wrong about any of those. A green test
suite means the code is honest about what it was told; it says nothing about whether the
camera saw anyone.

Two rows are deliberately structural — proven by code refusing rather than by the owner
watching it refuse — and are marked as such.

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 12.1 | The Core renders on the owner's machine and shows genuine state: listening, thinking and speaking transitions correspond to what actually happened | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-presence-reconciliation-2026-09-06.json`, `docs/evidence/m18-reconciliation-2026-09-06.json`): the owner watched the Core through six real runs; the bus events behind it were the sessions' own (mic_speech_start, first_audio), the eye's and the presence engine's, never synthesised (rows 12.22, 12.24-12.27). Earlier: Web: `tests/uistate/visual.test.ts`, `render.test.tsx` — every contract state produces its own visual and no other state's. Real: the owner watches the Core during a real voice turn. |
| 12.2 | Silence is drawn as four different facts — reported-idle, never-told, a claim that aged out, unreachable — and none is a calm breathing core | `PROVEN_PROXY` | Web: the silence block in `visual.test.ts`. Real: the owner sees the difference when Cloud Core is briefly unreachable. |
| 12.3 | Channels do not displace one another: `owner.likely_asleep` does not blank a thinking core, and a deployment in flight is not drawn as the agent's own work | `PROVEN_PROXY` | Web: `tests/uistate/ambient.test.ts`. Structural. |
| 12.4 | Research, memory and evolution state actually appear on the Core while those subsystems run | `PROVEN_PROXY` | API: `test_experience_uistate_signal.py`, `test_research_uistate.py` — the stamped event is read back off a real publisher. Two subsystems could not publish at all before 2026-09-05 and nothing noticed, because both failures were silent. Real: the owner sees memory and research light the Core during a real run. |
| 12.5 | The real camera can be enabled and the indicator is truthful; `untold`, `disabled` and "a state this build cannot read" are three different renderings | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): eye.enable verified by voice (call_m2380sVmVCiowg1o, browser ACTIVE / track live, runtime eye_enabled=true) and the owner watched the indicator. Earlier: Web: `tests/uistate/ambient-render.test.tsx`. Real: the owner enables the camera and sees the indicator agree with the machine's own camera light. |
| 12.6 | A real presence transition is detected from the owner actually leaving and returning | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-presence-reconciliation-2026-09-06.json`, `docs/evidence/m18-reconciliation-2026-09-06.json`): the presence-only run (OWNER M18 PRESENCE: PASS): eye.enabled 15:48:53Z -> present 0.56 (5 camera signals) 15:49:13Z -> away 0.41 15:51:56Z after the owner left -> present 0.68 15:53:07Z on return -> eye.disabled owner-m18-presence:end. The two unknown rows in between carry reason conflicting_signals - the engine said so rather than guessing. Earlier: API: `test_presence_engine.py` (fusion, sustained evidence, conflicting signals). Real: only the owner leaving the room can prove this. |
| 12.7 | Disabling the Active Eye stops perception immediately — the camera light goes out and camera-sourced observations are refused | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): eye.disable verified by voice (call_3EHcf6U3WzqmwE0N, browser DISABLED / track ended, runtime eye_enabled=false); the presence assertion went UNKNOWN reason eye_disabled. Earlier: API: `test_presence_routines_wiring.py::test_the_eye_disable_path_stops_camera_observations_immediately`. Real: the owner says `Gözünü kapat` and watches the light. |
| 12.8 | No raw camera archive is created: nothing on disk or in the database holds an image, a frame, or anything derived from one beyond the seven structured fields | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): every presence row of the run carries only to_state / confidence / reason / signal_sources / signal_count; no image-shaped value anywhere. Earlier: API: `test_presence_observations.py` — refusal by key shape and independently by value shape, and a route-level test that a refused observation never reaches the ledger. Real: an inspection of the machine and the database after the run. |
| 12.9 | Perception is never authentication: no presence observation grants, influences or substitutes for an owner session | `PROVEN_PROXY` | API: `test_presence_never_grants_or_influences_authority` — a confidently established presence state, then an unauthenticated client still refused on every presence route. **Structural**: proven by the code refusing. |
| 12.10 | Presence is stated as an inference with its confidence, never as a fact; a stale observation degrades to unknown rather than to "still present" | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-presence-reconciliation-2026-09-06.json`, `docs/evidence/m18-reconciliation-2026-09-06.json`): after each voice disable the assertion went UNKNOWN with reason eye_disabled (stale=true, confidence 0.0), never 'still present'; between observations the engine wrote unknown with reason conflicting_signals rather than a guess. Earlier: API: `test_presence_states.py`, `test_routines_presence_link.py`. Web: `ambient.test.ts` (the publisher's own `ttl_s` beats the client default). |
| 12.11 | A brief movement at 03:00 does not produce a morning greeting, and evaluating a greeting does not deliver one | `PROVEN_PROXY` | API: `test_presence_greeting.py` (both gates independently), `test_presence_greeting_delivery.py` (asking twice still says yes; only a recorded delivery starts the cooldown). Real: the owner wakes up and is greeted once. |
| 12.12 | A routine can be created and a short test alarm fires; the wake volume ramps and never jumps to full | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): routine 6153c724 triggered 15:28:53Z and executed: dispatch succeeded over the device path, started=true, ramp 0.05 -> 0.40 over 10 s, max 15 s; the owner heard it ramp and stop (twice that day). Earlier: API: `test_routines_dispatch.py` (the alarm routes to `desktop.alarm_start` with the firing id, the ramp is re-asserted at dispatch). Windows: `AmbientCapabilityTests` — the ramp starts low and climbs monotonically, a start at or above 0.5 is refused, an end above 0.85 is clamped and reported, the generated samples never exceed the level, the first audio the owner hears is at the start level, an alarm nobody stops stops itself. **Real** needs the next Windows agent install: the installed agent predates the alarm pair. |
| 12.13 | An owner-selected media action executes and plays the item the owner named | `PROVEN_PROXY` | API: `test_routines_dispatch.py` — opens an isolated browser session then navigates to the owner's exact URL, byte for byte; never navigates if the session open fails; a missing URL is refused without touching a device. No CAPTCHA/anti-bot handling exists anywhere in the dispatcher. Real: the owner names a video and it plays on the real chain. |
| 12.14 | A routine asks the Presence Engine rather than its caller, and an unknown presence skips it with a reason naming why | `PROVEN_PROXY` | API: `test_routines_presence_link.py`, `test_presence_routines_wiring.py`. |
| 12.15 | No routine fires without something asking: there is no background timer, and every transition writes exactly one ledger event | `PROVEN_PROXY` | API: `test_routines_service.py`. **Structural.** |
| 12.16 | The Activity Ledger records the whole run: presence transitions, eye enable/disable, routine firings | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): the run's ledger: voice.session.created/closed, voice.explained, voice.state_answered, eye.enabled/disabled with action ids, action.receipt x5, presence.state_changed, routine.created/armed/triggered/executed. Earlier: API: the per-subsystem route and service tests. Real: the owner asks what happened and hears it. |
| 12.17 | A real SHADOW_READY candidate is visible in the Core, and `Bunu canlıya alabilir misin?` explains the authority model and deploys nothing | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-presence-reconciliation-2026-09-06.json`, `docs/evidence/m18-reconciliation-2026-09-06.json`): the SHADOW_READY candidate was visible on the Core in every run and the candidate set was asserted unchanged before/after the alarm run (authority.nothing_deployed); the spoken half was proven in M17. Earlier: M17 proved the spoken half (Stage 11, `PROVEN_REAL`). The Core half is `ambient-render.test.tsx`: the release band renders no button, form or input. Real: the owner sees the candidate and asks the question. |
| 12.18 | Evolution cannot promote itself, and owner authorisation cannot be minted by Evolution-generated code | `PROVEN_PROXY` | API: `test_evolution_authority.py`, `test_evolution_boundaries.py`, `test_evolution_authorize.py`. **Structural**, and carried forward from Stage 11.12. TWO bypasses have now been found in this kernel, both by someone building against it rather than reading it: the subclass forge (2026-09-05) and, on 2026-09-06, `advance()` classifying a transition by its target alone — so a lab actor could divert an opportunity that was mid-deployment into `quarantined` with only its default `propose_candidate` grant. Both closed with regressions. A third is a reasonable expectation, not a surprise. |
| 12.19 | Display power automation turns a monitor off and never shuts down, reboots, hibernates or suspends the machine | `NOT_YET_PROVEN` | Built and deliberately unreachable, twice: `desktop.display_off` on the companion is advertised and routed only behind `PAGENTOS_AGENT_DisplayPowerEnabled` (default false), and Cloud Core refuses a routine's `display_action` behind `DISPLAY_ACTION_QUALIFIED=False`. A test reads `DisplayPowerController.cs` and fails if any shutdown/suspend/hibernate/logoff API name appears. **This row has its own separate qualification**, excluded from the main M18 run: a wrong inference here interrupts unrelated owner work. |
| 12.20 | No test launches a browser, and no camera fixture contains real owner imagery | `PROVEN_PROXY` | `services/browser/tests/test_test_isolation_guards.py` exists because the browser risk was once realised on this owner's desktop. The Core's own suite is `react-dom/server` only, by decision. |
| 12.21 | Voice connects FROM `/core`: the owner opens one page, and the realtime session, the microphone, the speaker and the narration state are owned there — `/voice` is a diagnostics view of the same session, not a second one | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): session d9867f4a created from /core (client_kind web, new since the baseline, named by the bus), router calls on it; one web session of the run. Earlier: Web: the voice store's single-instance tests (one rig, one `getUserMedia`, a second `connect()` while one session is live is refused, not stacked). Real: `owner-m18.ps1` v2 `voice.connected_from_core` (a new web session since the baseline that asked `activity.explain`) and `voice.single_session` (no two web sessions of the run open at once, from the sessions' own started/ended instants). |
| 12.22 | The Core's voice states are real: listening is the client's own speech gate opening, speaking is the first generated audio arriving, interrupted stops the motion at once — nothing is synthesised for effect | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): the session's own mic_speech_start and first_audio events (6 each) in d9867f4a. Earlier: Web: `visual.test.ts` — the overlay drives from controller snapshots and the playback analyser, and an `interrupted` snapshot zeroes the pulse. API: `_UI_STATE_BY_EVENT` maps the session's timing events to `agent.listening` / `agent.speaking`. Real: `voice.owner_spoke_core_listened` (`mic_speech_start` on the session), `voice.core_reacted_to_speech` (`first_audio` on the session). |
| 12.23 | A cognitive request spoken at the Core reaches its subsystem through the existing Realtime path, and the engine's own `query_kind` + `subsystem` are on the record — never inferred from prose | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): activity.explain last_activity -> ledger (voice.explained) and state.now world_state -> worldmodel (voice.state_answered), both from the Core. Earlier: Carried from Stage 11 (`PROVEN_REAL` from `/voice`). Real from `/core`: `voice.cognitive_request_reached_subsystem`, `voice.answered_through_realtime` (a succeeded tool call with spoken characters). |
| 12.24 | `Gözünü kapat` spoken at the Core disables perception durably and the local camera loop stops: the ledger row's reason is `voice:<phrase>`, not the control's `owner_stop` | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): eye.disable verified with reason voice:gozunu kapat on the ledger row, action_id call_3EHcf6U3WzqmwE0N, session d9867f4a. Earlier: API: the realtime service applies `disable_eye` deterministically on the `EYE_DISABLE` intent. Web: `EyeControl` stops the local loop when the bus says `eye.disabled` (`stopLocalOnly`). Real: `eye.disabled_by_voice`, `eye.disabled`. The first real run (2026-09-06 morning) DID disable the eye at 09:46:44 — the harness reported it as never having happened because it gated the disable on a second presence state. Harness defect, fixed and pinned. |
| 12.25 | "Camera enabled" and "a current, valid presence observation exists" are two different facts on the Core: the camera being open never implies the owner is present | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): eye.enabled 15:12:19Z, then presence.state_changed -> present (confidence 0.48, 5 camera signals) at 15:12:39Z; before it owner.presence was an uncertainty with the eye on. Earlier: API: `owner.presence` stays `no_observations_yet` with the eye on and nothing observed (`test_worldmodel`); the presence service heartbeats a held state at half its TTL so a live claim is not shown as unknown (`test_presence_heartbeat.py`). Web: `presence-memory.test.ts` replays the first real run — a seated owner who fidgets is present; an empty room is absent at ≤ 0.2. Real: `presence.current_observation`, `world.presence_fact`, `core.presence_event_published`. |

| 12.26 | A spoken mutation is acknowledged only from a verified receipt: `Gözünü kapat` → the real capability executes, the runtime is read back, and the assistant says `Gözümü kapattım efendim.` (or `zaten kapalı`, or `kapatamadım; işlem doğrulanmadı`) — never *"öyle olmuş gibi düşün"* | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): five eye receipts in the window, every one verified; the sentences spoken were the receipt's own. Earlier: Contract: `docs/M18_ACTION_CONTRACT.md` §1, §5 (ADR-0063). API: `test_voice_eye_tools.py` (verified / already / unverified / safety-net-same-turn), `test_actions_receipt.py` (the speech table contains no banned phrase). Real: `owner-m18-eye.ps1` `eye.disable_receipt_verified`, `eye.confirmed_only_after_ack` (the session's `first_audio` after its `tool_done`). The owner's second real attempt (2026-09-06) is the defect evidence: the eye closed, the assistant made it up. |
| 12.27 | The symmetric enable path exists and is stateful: `Gözünü aç` / `Kamerayı aç` / `Beni tekrar izle` reopen the real local camera through the same contract; DISABLED→ENABLING→ACTIVE→DISABLING→DISABLED, idempotent, never two streams or two loops | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): session 1ce36ca0: eye.enable gen:1 -> eye.disable gen:2 -> eye.enable gen:3, all verified, a new live track each time (the eye test finished by itself at 15:10:26Z). Earlier: Web: `tests/eye/store.test.ts` (enable → ACTIVE → disable → DISABLED → enable → ACTIVE with exactly two camera opens; join-in-flight; counters {1,1,1}). API: `test_voice_intents.py` (every phrase → `EYE_ENABLE`), `test_voice_eye_tools.py` (enable verified only when the client observed `ACTIVE`; permission_denied spoken as such). Real: `eye.enable_receipt_verified`, `eye.reenabled_read_back`. The owner's second attempt: `Gözünü aç` did nothing. |
| 12.28 | Current-state questions are answered from live state, result-first: `Kendi sisteminde şu anda ne görüyorsun?` → runtime + World Model facts each with source / observed_at / age / confidence / stale; stale is said as stale; 1–3 sentences; no bookkeeping narration | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): state.now answered 'Cloud Core saglikli, ses bagli, goz kapali. Bir modul canliya alinmayi bekliyor' - result-first, from the live composer. Earlier: API: `test_voice_state_tool.py` (facts carry provenance; stale spoken; eye scope sentences; `activity.explain` with a `world_state` question returns the same `speech` as `state.now`; no "kayıt"). Real: `state.live_path_reached`, `state.spoken_result_first`, `state.facts_carry_provenance`. The owner's second attempt: "kayıtlara bakmalıyım…". |
| 12.29 | One router, three classes: QUERY / ACTION / CONTROL from `resolve_intent` alone; `Canlıya al.` is an ACTION that is refused with the authority sentence and recorded; `Bunu canlıya alabilir misin?` stays a QUERY; no second Turkish table anywhere | `PROVEN_REAL` | **REAL 2026-09-06** (owner runs; `docs/evidence/m18-reconciliation-2026-09-06.json`, `scripts/core/reconcile-m18.ps1`): intents on the sessions carry klass=action / capability=eye.* for the eye phrases and klass=query for the questions; every voice-attributed eye row is explained by a receipt (0 unexplained). Earlier: API: `test_voice_intents.py` (the nine owner utterances → klass / intent / query_kind / capability), `test_voice_eye_tools.py` (`release.promote` refused, `action.receipt` recorded). **Structural**: the web client has no phrase table (grep-asserted in review). |

**Sixth real attempt, 2026-09-06 evening — `owner-m18-eye.ps1` fourth run (session
9df439af), contract v2 deployed by the run itself:** steps 1 and 2 `PROVEN_REAL` for the
first time — `eye.enable` verified (browser ACTIVE, Logi C615 track live, runtime
`eye_enabled=true`, `Gözümü açtım efendim.`), `eye.disable` verified (DISABLED, track
ended, `eye_enabled=false`, `Gözümü kapattım efendim.`). Step 3 failed three times with
`request:stop_local > superseded:stop > state:ENABLING->DISABLED`: the Core's eye control
stops the local loop whenever the bus still says `eye.disabled` and the loop reports
running; on a second enable the bus still carries step 2's `eye.disabled` and the loop
starts before the durable enable is written, so a stale bus state cancelled a newer
transition (step 1 escaped only because the older `eye.disabled` had decayed). Fix:
generation-owned transitions — a bus stop is a dated request applied only to an ACTIVE
loop older than the event, never to an enable in flight; late callbacks of an older
generation are ignored; every enable is a fresh stream with the track id in the trace;
proven by a repeated-sequence race test. `eye.no_hidden_mutation` FAIL in the same run was
the CHECK's defect: it read the receipt's timestamps off the ledger row's top level, built
no windows, and flagged rows 71 ms before their receipts; fixed with the production rows as
the fixture, and eye rows now carry the `action_id` / `session_id` of the voice action that
wrote them (contract v3), so correlation is by identity first.

**Fifth real attempt, 2026-09-06 evening — `owner-m18-eye.ps1` third run (session
3eb6fee7), diagnosed from the production record:** `state.now` answered result-first
("Cloud Core sağlıklı, ses bağlı, göz kapalı. Bir modül canlıya alınmayı bekliyor"). Every
eye tool call reached the Cloud Core with NO `observed_after` → `capability_missing`,
`failed`, "işlem doğrulanmadı": the provider delivers `eye__disable` (vendor spelling), the
browser compared it against `eye.disable`, so its local action never ran. The camera that
closed at 13:57:33 was closed by the OLD utterance safety net (`eye.disabled`,
`voice:gözünü kapat`, 7 ms after the tool call) — a hidden second mutation path; `Gözünü
aç` never reached the browser; the harness said "current web session: none yet" because the
session predated its baseline (clock skew 0.6 s, not time). Fixes: `tool-names.ts`
normalises the vendor spelling before the local action (pinned with `eye__enable` /
`eye__disable`); the safety net no longer mutates (resolved and audited only); receipts
carry `session_id`, `observed_at`, the media track's `readyState` and the browser's
`action_trace`; nine browser error classes each with its own sentence; "kapandı ancak işlem
kaydını doğrulayamadım" when the physical action succeeded and the record could not be
verified; `GET /v1/state/now`; the harness correlates by the session id the Core publishes
on the bus (`Get-BusVoiceSessionIds` / `AcceptIds`), watches the three receipts
(`Get-EyeReceiptSteps`), proves no unreceipted voice mutation (`Test-HiddenEyeMutation`),
and gates on the deployed contract VERSION (`action_contract_version`), releasing once.

**Fourth real attempt, 2026-09-06 later afternoon — `owner-m18-eye.ps1` second run:** proven
real by the owner: clean working tree, transactional Cloud Core release succeeded,
production advertises `eye.enable` / `eye.disable` / `state.now`, `/core` reachable, the
closed-session leftover no longer accepted as current state. Then the harness crashed
before the owner's part: `Get-NewSessions is not recognized` — both wait callbacks were
`GetNewClosure` blocks, which run in a fresh module scope that sees no dot-sourced
function. Fixed as a pattern: the wait owns fail-fast and progress (`ConnectWaitSec`,
`RouterWaitSec`, `ProgressEverySec`, `CoreProbe`); callbacks receive their inputs as
arguments; the wait's and selector's locals are prefixed so dynamic scope cannot shadow a
callback's variables (tests 12e/12g/12h caught that on the way); no closure remains in any
harness; `scripts/tests/harness-symbols.tests.ps1` parses every harness and library with
the AST and fails on any invoked command nothing declares and on any `GetNewClosure`.
`core.real_state` had failed only because nothing had been published since the release
restarted the process; the route now reports `current_age_s` against the server clock and
the lifespan publishes a truthful system `agent.idle` at startup (not yet released; the
harness reports a null current as exactly "nothing since the Cloud Core started"). Proven
automatically, twice, against production without the owner: no release (tools present),
web shell ready in 3 s, real state truthful, bounded stop after 35 s with the exact reason,
no Enter prompt, evidence written — and the run left `next dev` orphaned on port 3000, so
the harness now stops the whole tree and refuses a foreign listener (`Get-ListeningProcess`).

**Third real attempt, 2026-09-06 afternoon — `owner-m18-eye.ps1` first run, diagnosed from
the production record:** the harness reached `/core` and then waited; `Gözünü aç` did
nothing. The deployed Cloud Core's health manifest listed only the M17 tools
(`activity.explain`, `clock.now`, `narration.control`, `plan.redirect`, `research.start`,
`voice.intent`): the action contract was merged, never released, so no router existed in
production for the harness or the model to reach. The old safety net still wrote
`eye.disabled` (13:09:54Z, 13:13:53Z) but no `eye.enabled` row exists in that window, so the
camera was never durably on when `Gözünü kapat` was said. The `agent.listening` the harness
read at startup belonged to session a71096ca, closed at 13:14:08Z — closing published
nothing. Sessions a2ac0716 (never closed) and a71096ca overlapped — a reload left the first
open. Fixes: the harness checks the deployed tools first and releases once; a session that
ends publishes `agent.idle`; a reload posts a keepalive close; the wait prints what it sees
and fails fast with the missing evidence. The camera lifecycle the owner asked to see proven
runs in `tests/eye/browser-frame-source.test.ts` against the real `BrowserFrameSource` with
`getUserMedia` stubbed: one stream per real transition, tracks ended on disable, no orphan,
permission refusal surfaced.

**Second real attempt, 2026-09-06 midday — the owner's observations (defect evidence, no
file was produced):** from `/core`, `Gözünü kapat` really disabled the eye and the assistant
answered *"öyle olmuş gibi düşün"*; `Gözünü aç` / `Kamerayı aç` / `Beni tekrar izle` did
nothing; `Kendi sisteminde şu anda ne görüyorsun?` was answered with bookkeeping narration.
Rows 12.26–12.29 and `docs/M18_ACTION_CONTRACT.md` are the response; the long run (item 19)
is not repeated until the short one (item 20, `owner-m18-eye.ps1`) passes.

**First real run, 2026-09-06 morning — what was real before the harness crashed:** the Cloud
Core release succeeded (`ROUTINES_VERSION` current), the Windows agent update was verified
and the alarm pair advertised, `/core` answered, the alarm routine triggered on evaluation.
Those are kept as evidence; nothing is redone for its own sake. What the run also showed:
`owner-m18.ps1` v1 crashed on `The property 'Count' cannot be found on this object` — a
one-element `dispatch_results` array unrolled by a parenthesised call inside an
if-expression (`scripts/tests/owner-harness.tests.ps1` pins the exact form, and the fix);
the seated owner was read as `away` at 0.75 for eleven minutes on 1,102 observations
(ADR-0062: the client measured whole-frame mean motion; rewritten); the Core showed
"Sahip durumu bilinmiyor" for ten of those minutes (a held state was never republished;
now heartbeats). And `/core` could not hear the owner at all, because voice lived on
`/voice` (ADR-0061). The second attempt is `owner-m18.ps1` v2, twelve points from `/core`.

## Stage 13 — M18.2 Voice visual continuity + research result narration (7 rows PROVEN_REAL from the owner's run of 2026-09-06, reconciled from the record; 13.3 has its own short follow-up check)

**The owner's real run (2026-09-06 19:49–19:55Z, session be6d49ce) is reconciled in
`docs/evidence/m18-2-reconciliation-2026-09-06.json` by `scripts/core/reconcile-m18-2.ps1`.**
The core rows are real: the speaking lifecycle (7 turns ended after `response.done`, two
audible over 6 s), the findings-first spoken result (936 characters, no crawler words, three
findings), the real run (task deabbd44 READY in 43 s of a 120 s QUICK budget, the voice
call 77 s end to end), the challenge policy (openai.com challenged twice, cooled, two
shortlists skipped without navigation) and the waves (3 waves, 8 fetched of 46 discovered).
Row 13.3 was not observed: the session closed by `page_unload` 30 seconds after the result
and the fresh session's next utterances carried no technical word — the harness's 120 s
step window was an arbitrary timer the requirement never asked for. The follow-up has its
own check, `scripts/core/owner-m18-2-followup.ps1` (item 23b), which reuses the completed
report and re-runs nothing. Attributed, not hidden: two of the evening's three QUICK runs
failed `insufficient_valid_findings` (1–2 evidence pages after the cooled domain consumed
the shortlist). Fixed and merged as ADR-0074: the shortlist is rebuilt every wave,
round-robin over domains with a 40 % share cap, cooled domains occupy no slot; the budget
is the ceiling (a wave never starts unless it can finish inside 120 s; a run with a
publishable answer stops at the soft 90 s); one or two verified sources produce a READY
*thin* report ("Kısa araştırma bütçesinde yalnızca N kaynak doğrulanabildi; bulgular
sınırlı." with an offer of a broader run) and zero evidence still fails truthfully;
`test_research_regression_20260906.py` replays all three runs by their real counts and
shows both failed ones now end READY-thin with the findings they had.

Two product defects the owner observed on `/core` after M18 closed. Both were traced to a
mechanism before any change (ADR-0066, ADR-0067). `PROVEN_REAL` needs the owner's short
`/core` check (Test A, Test B in `docs/OWNER_ACTIONS.md`); the rows are written so that the
Cloud Core's record can prove most of them (the session's `first_audio` / `audio_done`
timing events; the research call's terminal result and `speech_head`).

**Implemented 2026-09-07, all rows `PROVEN_PROXY` pending item 23.** The trace behind 13.2
found more than the wording: a spoken `research.start` never started the real pipeline (it
fabricated a local plan and returned "running" forever; whatever the owner later heard came
from `activity.explain` reading the LAST REST-started run's statistics). Now the tool
starts the same run as the REST route (task + device selection inside the tool call, the
Temporal start as a route-awaited follow-up), the announcer completes the originating
session's call with `spoken_result` when the durable report lands, and a failure to start
is a truthful failed call, never a silent "running". Proxy evidence: web
`tests/voice/speech-lifecycle.test.ts` (16), `voice-overlay.test.ts`; api
`test_research_result.py`, `test_voice_research_start.py` (6), `test_voice_research_completion.py`,
`test_explain_*` (research executive from findings; `research_problems` / `rejected_pages`);
`test_realtime_bench.py` (`audio_done`). Gates: web 678, api 1132 across the affected suites.
The owner check `scripts/core/owner-m18-2.ps1` gates on action contract v4 (the deployed
Cloud Core runs v3 and is released once by the run).

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 13.1 | SPEAKING is a lifecycle, not an analyser frame: from the first audible playback until the final assistant audio of that response actually completed; a natural pause inside the answer leaves the state SPEAKING with a calmer Core; barge-in / `Dur` / cancel end it at once | `PROVEN_REAL` | **REAL 2026-09-06** (session be6d49ce; `docs/evidence/m18-2-reconciliation-2026-09-06.json`): 7 completed turns, every one ended after `response_done` — e.g. first_audio 9844 → response_done 13914 → audio_done 15895 ms (audible 6051 ms), and a 6387 ms turn (99581 → 104371 → 105968); the owner watched SPEAKING hold through the pauses. Earlier: Web: controller lifecycle tests (first audio → speaking; `response_done` with the provider buffer still playing → still speaking; `audio_stopped` for that response → listening with one `audio_done`; silence release and drain cap fallbacks; interruption immediate; a previous response's `audio_stopped` ignored). Real: Test A — a multi-sentence answer; the session records `first_audio` then `audio_done` (basis provider) after the last sentence, and the owner sees SPEAKING held through the pauses. |
| 13.2 | The default spoken research answer is the findings and why they matter — a concise conclusion, up to three findings each with its importance, an optional offer — never crawler statistics | `PROVEN_REAL` | **REAL 2026-09-06** (call call_gqSiq7eQWlrztrHF on session be6d49ce): the spoken head "Efendim, 'Son üç gündeki OpenAI ile ilgili gelişmeler' konusunda araştırmayı tam…" (936 characters, no crawler words) over a report with three findings; the owner heard findings, not telemetry. Earlier: API: `spoken_result` deterministic from the validated report; a no-diagnostics invariant (no "elendi", "interstitial", "dedup", "aday", counts of pages); insufficient evidence said truthfully. Real: Test B — a real research question; the research call's terminal `speech_head` starts with the conclusion; the owner hears findings, not telemetry. |
| 13.3 | Diagnostics stay available and are spoken only on request: `Teknik anlat.`, `Hangi sayfalar elendi?`, `Araştırma sırasında ne sorun oldu?` | `NOT_YET_PROVEN` | API: the technical level / the diagnostics question kinds return `ResearchDiagnostics` with evidence refs; the executive level never does. Real, first attempt (2026-09-06): not observed — the session closed by `page_unload` 30 s after the result. Real, second attempt (2026-09-07, the follow-up-only check): the routing, the diagnostics-only-now, the conciseness and no deployment were PROVEN on a new session — and the model ALSO started a second research (1 `research.start`, 1 new task): a real defect, fixed as **ADR-0075** (merged): the one router classifies research turns (`research_class` on every resolved intent: new / retry / technical_explanation / followup — "Teknik anlat.", "Hangi sayfalar elendi?", "Kaynakları söyle.", "Birinci bulguyu detaylandır.", "Neden önemli?" pinned; "Araştırmayı yeniden yap." is the only re-run), the explanation binds to the completed job by identity (session linkage → plan → the latest READY report within 14 days; two candidates within 6 h with no linkage → one clarifying question, no crawl) and names it (`research_job_id` / `research_artifact_id` on the call and under `provenance`), and the relay refuses `research.start` on such a turn as a succeeded call carrying `{"status":"refused","reason":"research_followup_turn",…}` with "Yeni bir araştırma başlatmadım efendim…" (`test_voice_research_followup.py`: the five phrases → 0 crawls, the report row byte-identical, the diagnostics from that report; the re-run → exactly one new task). Contract v7. Real, third attempt (2026-09-06 21:04Z and 21:13Z, on v7): no silent crawl — but the fresh session's "Teknik anlat." was answered with the clarification "iki tamamlanmış araştırmam var … hangisini anlatayım?" six times in a row (nothing could take the spoken answer) and a page reload had dropped the conversation's link, so identity failed (`job_identity_reused`, `report_reused_not_recomputed`). Fixed as **ADR-0076** (merged 2026-09-07; `test_research_focus.py` 26 tests, the whole unit suite 4,102): a durable, owner-level, ID-based research FOCUS with a bounded stack (set on completion, on the spoken result, on a click in the cockpit/research page, on a voice selection, on "bir önceki"), a deterministic reference resolver (current / previous / ordinal / topic-unique / selection; ambiguity → one answerable clarification; missing → "Hangi araştırmayı kastediyorsunuz efendim?"; never a crawl), follow-up tools `research.explain/sources/finding_detail` that run only against the resolved id, the relay refusing `research.start` before any task row for every reference/follow-up turn, and identity metadata (time · mode · sources) on every research item. Contract v8. Its own check, third form: `scripts/core/owner-m18-2-followup.ps1` — the owner clicks one completed research (focus X by id), says "Bunu teknik anlat." (explained job == X) and "Bir önceki araştırmayı anlat." (explained job == the previous focus Y); zero `research.start` that ran, zero new tasks, both reports' artifacts unchanged, at most one clarification, no deployment from the first sentence on, within the technical budget. |
| 13.4 | The Core's lifecycle during research is truthful: RESEARCHING through discovery/fetch/rank, THINKING or RESEARCHING with progress at synthesis, SPEAKING for the entire presentation, then LISTENING/IDLE after the last audio; the constellation may stay while presenting | `PROVEN_PROXY` | API: `test_research_uistate.py` per stage. Web: overlay tests (speaking + outputLevel 0 keeps kind speaking). Real: the owner watched Test B on `/core` (2026-09-06) and reported the lifecycle as correct; the bus history is not durable, so the record can only bracket the presentation with the session's own `first_audio`/`audio_done`. |
| 13.5 | The result-first policy holds across the spoken answers of this stage: answer, why it matters, action where appropriate, details on request; no "kayıtlara bakmam gerekiyor", no "şu sayfaları eledim" unless asked | `PROVEN_REAL` | **REAL 2026-09-06**: the spoken head opens with the conclusion and names no records or pages (reconciliation row 13.5); the owner's judgement during Test B. Earlier: API: persona + `spoken_result`/`state.now` wording tests. |
| 13.6 | A spoken `Araştır` is a real run: the tool starts the same pipeline as the REST route, the announcer completes the originating session's call with `spoken_result`, and a run that cannot start is a truthful failed call, never a silent "running" | `PROVEN_REAL` | **REAL 2026-09-06** session a4455670: `research.start` → succeeded, spoken head "Efendim, 'Son 3 gündeki yapay zeka ajan gelişmeleri' konusunda araştırmayı tamamladım…"; `activity.explain` technical afterwards ("iki yüz elli dört aday keşfedildi…"). API: `test_voice_research_start.py`, `test_voice_research_completion.py`. |
| 13.7 | Research modes are explicit and QUICK is the conversational default: typical completion 60–90 s, hard budget 120 s; STANDARD 2–3 min; DEEP only on the owner's explicit words, never chosen silently | `PROVEN_REAL` | **REAL 2026-09-06** (task deabbd44, plan mode `quick`): `research.completed` elapsed 43.5 s of a 120 s hard budget; the voice call 77 s end to end. Two other QUICK runs that evening ended in a truthful `insufficient_valid_findings` at 58 s and 81 s (attributed; fixed by ADR-0074 — replayed by their counts they now end READY-thin inside the budget). **Proxy 2026-09-07** (ADR-0068): `test_research_policy.py` — the per-mode table (QUICK target 90 s / hard 120 s, 2 discovery queries, 25 candidates, 10 sources, waves of 4 up to 3; STANDARD 150/210; DEEP 360/600), `derive_mode_from_utterance` maps only explicit words ("kapsamlı", "derinlemesine", "detaylı" → DEEP; "geniş", "karşılaştırmalı" → STANDARD), `decide_next_wave` stops at the hard budget on a fake clock; `test_voice_research_start.py` (the voice tool derives the mode from topic+scope; REST `research_mode`). API: a `ResearchPolicy` per mode; `research_start` maps only explicit words to DEEP/STANDARD; a QUICK run respects its hard deadline on a fake clock and still answers truthfully. Real: Test B timing — the first real run (a4455670) discovered 254 candidates and took minutes; the next must finish inside the QUICK budget. |
| 13.8 | Challenges are left alone: CAPTCHA / unusual-traffic / bot verification / uncompletable consent walls / interstitial and redirect loops are detected quickly, the reason recorded, the page abandoned with zero retries (one only on clear transient evidence), the domain cooled for the run, and the next ranked source taken | `PROVEN_REAL` | **REAL 2026-09-06** (task deabbd44): openai.com answered two interstitials (`access_denied`), was cooled after the second, and 20 shortlisted pages were skipped without a navigation ("skipped: domain cooled"); the run completed on other sources. **Proxy 2026-09-07** (ADR-0068): `test_research_challenge.py`, `test_research_browser_activities.py` — a challenge is classified from the fetch result (data, never an exception), retried zero times by construction; the transient retry dropped to 2 attempts; the second challenge on a domain cools it (`DOMAIN_COOLDOWN_THRESHOLD=2`) and its remaining candidates are skipped with no device dispatch; a once-challenged domain sorts last. API: a challenged page abandoned in one attempt with its reason; a domain challenged twice cooled with its remaining URLs skipped without navigation; counts only in `ResearchDiagnostics`. Real: Test B diagnostics on `Teknik anlat.` naming challenged pages and cooled domains without having spent the budget on them. |
| 13.9 | Two-stage pipeline, ranked before navigation, fetched in waves with an early stop: discovery → candidates → duplicate-story dedupe → quick ranking (primary > academic > technical > news > community; recency, relevance, diversity, obtainability) → wave 1 of 4 → enough strong findings → synthesize, else wave 2; snippets rank, never evidence | `PROVEN_REAL` | **REAL 2026-09-06** (task deabbd44, ledger `research.completed`): 3 waves, 8 pages fetched of 46 discovered, 3 evidence, 3 findings, 5 duplicate stories dropped. **Proxy 2026-09-07** (ADR-0068): `test_research_policy.py` (`decide_next_wave`: evidence-enough → stop, waves spent, hard budget, source ceiling), `test_research_browser_activities.py` (fetches in waves of `wave_size` at `concurrent_fetches`; findings capped at `final_findings_max`; the caller's `max_sources` and the mode's ceiling combine as the minimum); `test_research_result.py` (snippets never become evidence). API: ranking prefers primary over secondary and drops duplicate stories; enough evidence after wave 1 stops before wave 2; a snippet never becomes evidence for a claim. Real: Test B's diagnostics show waves and fetched counts far below the candidate count. |
| 13.10 | Owner-facing progress on the Core during research is coarse and Turkish — "Kaynaklar aranıyor", "N güvenilir kaynak incelendi", "Bulgular doğrulanıyor", "Sonuç hazırlanıyor" — never crawler counters on the bus | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0068): `test_research_uistate.py` — the bus carries only the fixed Turkish phrases ("Kaynaklar aranıyor", "N güvenilir kaynak incelendi" per fetch, "Bulgular doğrulanıyor", "Sonuç hazırlanıyor"), never the raw topic or a crawler counter; the Core's readout prints a published label verbatim (`StateReadout`). API: `test_research_uistate.py` labels + progress. Real: the owner watching `/core` during Test B. |

## Stage 14 — M18.3 Fullscreen Living Core, durable Wake Alarm, ambient display control (pre-registered 2026-09-07; nothing PROVEN_REAL yet)

Spec `docs/M18_3_LIVING_CORE_WAKE_ALARM_SPEC.md`, ADR-0069. Three short owner runs (A visual,
B wake alarm, C ambient display) close this stage; everything else is deterministic and runs
before the owner is asked. The M18 rows 12.13 (media playback) and 12.19 (display-off) are
absorbed here: 14.7 is the media row and 14.11–14.14 are the display rows, each with its own
real evidence. The rule of Stage 12 holds: a `PROVEN_REAL` cell names the durable record
first (receipts, ledger rows, routine firings, the device audit), never a yes/no from the owner.

**Update 2026-09-07 (ADR-0078, end-to-end proxy).** Rows 14.5, 14.6, 14.9, 14.10, 14.11,
14.16 and 14.18 are now proven through the REAL application object in one run
(`services/api/tests/unit/test_alarms_wiring.py`): created by voice through the relay
(the canonical router classifies the words, the receipt speaks, the rows are durable and
in Europe/Istanbul), armed on the device by the `RoutineClock`'s own tick, fired at the
instant through the routine engine (once; a second process's tick fires nothing), the
display woken before the media, the alarm profile opened, the media verified, the ramp,
one receipt per physical step, the greeting on a later tick with duck and restore and a
single-use audio token redeemable exactly once through the open route, `Alarmı kapat.`
by voice stopping the medium that is playing, the test alarm cleaning itself up, the
session record saying so; `display.off` / `display.status` by voice reaching the device
and the registry. Writing it surfaced two production defects the component suites could
not see (the wake sequence was never on the route's live sources; the model's
`when_text` argument was refused by the relay's blocklist) - both fixed, contract v10. A
device that rang its fallback after the cloud gave up (network down at the instant) is
now recorded on reconcile rather than lost. The owner runs A/B/C remain the only
`PROVEN_REAL` evidence: A is visual, B is audible, C is physical; nothing else is left.

**Update 2026-09-07 (ADR-0079, ambient hardening).** Rows 14.12 and 14.14 are now backed by
the fifteen-scenario matrix through the REAL policy layer (`test_ambient_scenarios.py`: a
real fusion engine on structured camera observations, the real status registry through
the heartbeat ingest, the real holdoffs, the real alarm service; the device the only fake)
and by `test_ambient_hardening.py` (quiet hours in the owner's timezone, keep-on, the
camera grace, holdoffs restored from the ledger after a restart, the explicit-wake and the
alarm-wake holdoffs — the latter had never been wired — the owner's own words setting the
policy over the model's booleans, and the explanation surface). AMBIENT DISPLAY
ENGINEERING COMPLETE; the physical run (item 26) is `READY_FOR_OWNER_PHYSICAL_TEST`.

| # | Criterion | Status | Evidence required |
|---|---|---|---|
| 14.1 | `/core` in Minimal mode is a full-viewport Living Core: the Core covers 60–80 % of the usable viewport by aspect ratio, near-black ground, no page scroll, controls as fading overlays, fullscreen only on the owner's gesture with a visible exit, a PWA manifest; the old small wireframe is gone | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0070, merged): `stageSizeFor` targets portrait 0.74 / square 0.78 / landscape 0.72 / ultrawide 0.66 and is clamped to 60–80 % (`tests/uistate/stage.test.ts`); Minimal mode is `position: fixed; inset: 0` with no page scroll; the six-control cluster fades to 25 % after 4 s through a pure reducer and returns on pointer/key/focus; fullscreen is one callback bound to one button with a labelled exit; the manifest is standalone with SVG icons; the served document carries `<meta name="pagentos-core-build" content="living-core-1">` (`core-build-marker.test.tsx`, pinned to the harness). Web gate: 776 tests, tsc, lint, build. Real: A — the owner sees it on their own screen. |
| 14.2 | The gold/amber identity is layered and dense — outer field, containment shell, topology shell, orbital layers, data/circuit layer, particle transport, processor structures, energy chamber, nucleus — an original PersonalAgentOS language, not a film asset | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0070): nine layers mounted per tier — outer field (instanced points), containment shell, topology shell, three orbital layers at incommensurate rates in two directions, a merged circuit-trace buffer, two instanced particle flows, instanced processor fragments, energy chamber, nucleus with one additive halo; one gold family with exactly two exceptions (ember for fault, cool for the eye); no post-processing library; `docs/M18_3_LIVING_CORE_VISUAL_IDENTITY.md`; the 2D fallback shares the identity (server-rendered preview handed to the owner). Real: A — scale, depth, colour reviewed by the owner. |
| 14.3 | Real state drives the Living Core, unchanged truth rule: idle breath only; listening pulls inward; owner speech from the measured level; thinking/tool/research/memory/eye/waiting/shadow as published; SPEAKING semantic with RMS on amplitude only; NEW `alarm.*` wake surge and `display.*` on the ambient strip; contract v3 both sides | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0070): contract v3 on the client (ten new states with Turkish labels and the spec's TTLs); `alarm.*` is its own `wakeSurge` channel that never sets `kind`, never touches a core motion channel and does not accelerate the rings (ring speed stays the thinking channel — refused and recorded); the surge is non-zero only while sounding and zero while unreachable; `display.*` has no field on `VisualIntent`; the release claim is read by vocabulary so a ringing alarm cannot blank a live deployment; a v2 server renders with a one-line lag note (`visual.test.ts`, `contract.test.ts`, `truth.test.ts`). Real: A — speaking, listening, eye and the research constellation watched live. |
| 14.4 | High / Balanced / Low stay bounded, a hidden tab draws nothing, reduced motion is still, no WebGL → the 2D view in the same identity; cognition never depends on the renderer | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0070): `sceneBudgetFor` pinned in `quality.test.ts` — high 36 drawables / 398 instances / 160 particles, balanced 33 / 206 / 80, low 22 / 32 / 0 (no orbital fragments, no circuit, no field); a hidden tab keeps the demand frameloop and draws nothing; reduced motion is still; the 2D fallback carries the same identity. Real: A — smooth on the owner's GPU at the chosen tier. |
| 14.5 | A wake alarm is created by voice in the owner's timezone (`Yarın sabah 07:30'da beni uyandır.`, `Her hafta içi 07:15'te …`, `Saat 08:00'e alarm kur.`, `90 saniye sonra test alarmı kur.`), acknowledged only from its receipt, and `Sabah alarmım kaçta?` is answered from the record; `Alarmı iptal et.` cancels it | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071, merged): `resolve_intent` pins every phrase (the alarm intents are checked before the generic stop words, so "Alarmı durdur" is not the narration stop); `tr_time` parses "yarın sabah 07:30", "her hafta içi 07:15", "saat 08:00", "90 saniye sonra", "beş/on dakika" in Europe/Istanbul (a bare number is never a clock); the `alarm.create/cancel/snooze/stop/status` tools return receipts with the §6 speech table (`test_alarms_voice.py`, `test_alarms_tr_time.py`, `test_alarms_speech.py`; 269 new tests). Real: B — the test alarm spoken, its `alarm.scheduled` ledger row and receipt on the session. |
| 14.6 | The alarm fires without a voice session, a focused tab or a microphone: the clock fires an ARMED alarm at its instant; a restart or reconnect never fires it twice; the device's own armed fallback rings once when the cloud does not reach it | `NOT_YET_PROVEN` | **Proxy 2026-09-07**: API (ADR-0071) — `RoutineClock` (10 s, single-flight, health `checks.routine_clock`) fires an ARMED row on a fresh process's first tick; two ticks, two processes and a redelivery produce one firing and one ring (`test_routines_clock.py`, `test_alarms_service.py`); the sequence disarms the device first and, if the device had already rung its fallback, stops it by id before the music. Windows (ADR-0072) — `ArmedAlarmStore` arms/consumes/expires on a manual clock, rings once across a restart, an overdue arm under two hours rings at once, older ones expire with an audit line. Real: B — the firing is a routine firing at the scheduled instant (the record's `triggered_at`), the device audit shows the arm consumed. |
| 14.7 | The named YouTube item really plays in the worker's dedicated `alarm` profile, verified by the media element (`currentTime` advancing, not paused); a challenge / bot check / consent wall / autoplay block / missing element is a truthful failed receipt and the tone with its ramp — never a bypass, never a click, never the owner's own Chrome | `NOT_YET_PROVEN` | **Browser half proxy 2026-09-07** (ADR-0073, merged): `browser.media_play/volume/status/stop` on a session opened with `profile: "alarm"` + `session_kind: "media"` (either without the other is refused); the alarm profile is derived beside the research profile and refused inside any real Chrome tree; `verified` requires `currentTime` to advance ≥ 0.5 s while not paused; each reason (`no_media_element`, `autoplay_blocked`, `challenge`, `consent_wall`, `navigation_failed`, `error`) is a successful command carrying the reason, zero retries, no click; the in-page scripts were executed for real under Playwright's bundled Node (`test_media_scripts_in_node.py`); 398 browser tests; no test launches a browser; two independent reviews found no blocker. API: media failure → tone fallback with the failure receipt (Track C). Real: B — `media.play` verified with the owner's URL after the one-time consent in the alarm profile's own window (or the truthful fallback, named). Absorbs M18 row 12.13. |
| 14.8 | The wake volume ramps from a low start to the owner's target over 10–30 s and never touches the Windows master volume; nothing is left changed after the alarm | `NOT_YET_PROVEN` | **Proxy 2026-09-07**: Browser — the in-page ramp steps `video.volume` every 250 ms from the level actually reached, lands exactly on the target and clears its own interval; a duck issued mid-ramp cancels the wake ramp (proven under real Node). Windows — structural: no CoreAudio endpoint-volume or session-volume API name appears in the companion sources; the tone scales its own samples. API: the ramp figures on the receipt (Track C). Real: B — the owner hears the rise; the receipt carries start/end/ramp. |
| 14.9 | "Günaydın efendim. Saat yedi buçuk. Alarmınız çalıyor." plays from background narration — TTS in the persona's voice, a single-use audio token, `desktop.play_audio` — with the music ducked and restored around it; no microphone and no realtime session are involved | `NOT_YET_PROVEN` | **Proxy 2026-09-07**: API (ADR-0071) — the alarms package is structurally forbidden from importing anything under the realtime sessions (`test_alarms_structure.py`); the audio token is 256-bit, single-use, five minutes; `spoken_clock_tr` ("yedi buçuk", "yediyi çeyrek geçiyor", "sekize çeyrek var"); the greeting URL is built on the origin the device dialled (recorded from its handshake) and the TTS is `cedar` with an explicit fallback and an offline Fake when no key is set. Windows (ADR-0072) — the Device Service validates the URL against its configured broker origin (default deny), the companion fetches with a 10 s timeout, checks size and sha256, plays PCM16 WAV through the tone's own render path scaling samples by level. Real: B — the greeting receipt, the duck/restore receipts, and the owner hears it over the music. |
| 14.10 | `Alarmı kapat.` stops the music/tone at once; `Beş dakika ertele.` snoozes and re-arms; a one-shot test alarm cleans itself up (media session closed, device disarmed, routine resolved) on every terminal state | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071): stop (media and tone by id), snooze (a fresh `at` routine, re-armed), and cleanup on all three terminal states with an `alarm.cleaned_up` row (`test_alarms_service.py`, `test_alarms_sequence.py`). Real: B — STOPPED with its receipt; nothing armed afterwards (device status), no media session. |
| 14.11 | `display.off`, `display.wake`, `display.status` are governed capabilities on the DeviceService / SessionCompanion path with receipts (action_id, requested_state, reason, terminal_status, observed_after, evidence_refs, error_class); direct owner commands (`Ekranları kapat.` / `Ekranları aç.`) use the normal action authority and are spoken only after read-back | `NOT_YET_PROVEN` | API (ADR-0071, merged): receipt tests per outcome (verified / already / refused / failed); a device `recent_input` / `alarm_active` refusal is `execution_status=refused` with its own sentence. Windows (ADR-0072): the observer's state read back after each request. **Two answers to "may this system darken a display", on purpose:** the live path is `app/ambient` (owner commands, the owner's display test, and the automatic policy — this row and Stage C), while the M18 routine action kind `display_action` stays refused behind `app.routines.dispatch.DISPLAY_ACTION_QUALIFIED = False`: a routine descriptor is not an owner command or a presence decision, and it keeps its own gate until someone needs it. Real: C — the `display.off` receipt and the `display.on` observation on the heartbeat. Absorbs M18 row 12.19's positive half. |
| 14.12 | Keyboard or mouse input wakes the displays immediately, with Active Eye disabled, the camera failed, the owner unrecognised or presence stale: the activity is recorded (`owner.input_active`), and a stale AWAY/LIKELY_ASLEEP cannot re-darken the screen inside the input holdoff | `NOT_YET_PROVEN` | API: `decide()` is `none` for the whole holdoff with the assertion still AWAY; the device `recent_input` refusal → refused receipt + holdoff. Windows: the companion refuses `display_off` inside its own window. Real: C — eye disabled, screen off, one key, screen on within seconds, `owner.input_active`, no `display.off` for the holdoff. |
| 14.13 | DISPLAY OFF != SYSTEM SLEEP: no lock, sleep, hibernate, log-off, reboot or shutdown API anywhere in the display path; display actions change no topology, resolution, orientation or primary display; multi-monitor results report the displays targeted, the request accepted and the observed result | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0072, merged): the source-reading guard now covers every display file (`DisplayPowerController`, `DisplayWake`, `DisplayStateObserver`, `MonitorInventory`) and forbids shutdown / suspend / hibernate / logoff / `LockWorkStation` / `ExitWindowsEx` / `SetSuspendState` in both spellings; the wake sends a zero-delta pointer move and never a key (the input struct has no keyboard member); `monitors` from `EnumDisplayMonitors` reports index/primary/geometry only; 385 agent tests. **Structural.** Real: C on the owner's monitors. Absorbs M18 row 12.19's negative half. |
| 14.14 | Automatic display-off is conservative and owner-configurable: only sustained AWAY (`away_after_s`) or sustained, confident LIKELY_ASLEEP (`asleep_after_s`, `asleep_min_confidence`) with `auto_off_enabled`; UNKNOWN, stale, eye disabled, low confidence, an alarm context, a holdoff or a display already off never turn a display off; `LIKELY_ASLEEP` never comes from one frame; every decision that acts is a receipt and a ledger row | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071): `app/ambient/policy.decide` is pure and swept line by line against the ENABLED policy — `UNKNOWN`, stale, eye disabled, below-confidence, every holdoff, an alarm context and a display already off never yield `display.off`; sustained AWAY / sustained confident LIKELY_ASLEEP do (`test_ambient_policy.py`); a decision that acts is a receipt and a ledger row; the policy row persists with `auto_off_enabled=false` by default. Real: the separate longer observation after C, when the owner wants it. |
| 14.15 | The owner's ambient commands persist with receipts: `Uyurken ekranları kapat.`, `Ben yokken ekranları kapat.`, `Otomatik ekran kapatmayı aç/kapat.`, `Ben geri geldiğimde ekranı aç.` | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071): the intents are pinned; `ambient.set_policy` returns a receipt and writes `ambient.policy_changed`; `PUT /v1/ambient/policy` starts the command holdoff (`test_ambient_service.py`, `test_alarms_voice.py`). Real: B or C — one spoken policy change on the record. |
| 14.16 | At alarm time the displays wake before or with the audio, and a failed display wake never suppresses the alarm audio; the wake works with the eye disabled | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071): a failed `display.wake` port is recorded and the sequence still reaches the audio step; six media-failure modes fall back to the tone with the failure receipt; both paths failing is FAILED + critical `UiState.ERROR`; no presence, eye or camera dependency in the sequence (AST-checked per file) (`test_alarms_sequence.py`). Real: B — the `display.wake` receipt precedes `media.play`/`alarm.start`. |
| 14.17 | Privacy unchanged: no raw camera frame or video is persisted for sleep/away detection; the input observer reads only an idle tick count; presence stores state, confidence, observed_at and source | `PROVEN_PROXY` | **Proxy 2026-09-07** (ADR-0072): the input source is `GetLastInputInfo`'s tick count only — no key or pointer content API in the companion (source-read); the heartbeat status carries seven closed keys (`SerializationTests` cross-check the fixture, the code and the schema); the M18 perception guards are unchanged. **Structural.** |
| 14.18 | Test mode is the production path: a test alarm is the same table, routine, arm and sequence with a short offset; the display test issues the real `display.off` receipt; nothing is simulated | `NOT_YET_PROVEN` | **API half proxy 2026-09-07** (ADR-0071): `is_test` changes only the offset, `max_play_seconds` (120) and the cleanup; the same table, routine, arm and sequence; `POST /v1/ambient/test-display` arms a moment the clock turns into the real `display.off` receipt (reason `owner_test`, a 5 s device holdoff because asking IS recent input). Real: B and C are those tests. |

## Stage 15 — Voice routing qualification: the Owner Utterance Corpus (2026-09-07, ADR-0080)

`PROVEN_AUTOMATED` here is the owner's own name for a `PROVEN_PROXY`-class mark whose
stand-ins are the synthetic utterance (text injected at the boundary right after
transcription) and the fake device: the router, the relay, the tools, the tables and the
speech text are all real. Nothing in this stage claims a sound was heard.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 15.1 | Every corpus case enters at the canonical boundary (`POST /events` utterance), is resolved by the ONE router, dispatches the contract-expected tool through the relay, and is judged on intent, class, target identity, tool status and speech | `PROVEN_AUTOMATED` | `tests/unit/test_owner_utterance_corpus.py`: 345/345 (corpus v1), 0 wrong routes, 0 unexpected clarifications, report HEALTHY. |
| 15.2 | Turkish variation is covered deterministically: punctuation, case, missing diacritics, polite/short forms, deictics, number/time formats, weekly recurrence, ASR-split numerals | `PROVEN_AUTOMATED` | Sources canonical / paraphrase / asr_noise / regression / generated in the corpus; `_variants` per case; the alarm product set. |
| 15.3 | Negative routing: a research follow-up never starts a research (the relay refuses `research.start`, dispatched to prove it), no wrong route creates an alarm or research row, no tool touches a device capability outside the case's policy | `PROVEN_AUTOMATED` | Forbidden side effects = 0 across every run, before and after the fixes (`test_corpus_has_no_forbidden_side_effect_anywhere`). |
| 15.4 | Focus: with two same-title researches, "bunu"/"az önceki"/"son" bind the newer job and "bir önceki" the older, by id | `PROVEN_AUTOMATED` | `research_focus_b` context; target ids asserted per case. Reload / reconnect / same-title survival stays in `test_research_focus.py` (ADR-0076). |
| 15.5 | The spoken result is structurally valid: non-empty, no banned completion phrase, recorded with its chars, and the client's first_audio/audio_done for the turn on the session record | `PROVEN_AUTOMATED` (speech text) / `PROVEN_PROXY` (audio pipeline) | Harness step 6; ADR-0066 lifecycle events. No sample is synthesised or heard. |
| 15.6 | A regression becomes a durable state and an EvolutionOpportunity: `voice.qualification` ledger row, one opportunity per case (idempotent), state REGRESSION_FOUND / SELF_HEALING / OWNER_AUDIO_TEST_REQUIRED / HEALTHY from rows alone; a healthy word never outranks failing numbers | `PROVEN_PROXY` | `test_voice_qualification.py` (10) through `/v1/voice/qualification`; Cockpit panel `voice-qualification.test.tsx`. |
| 15.7 | The nightly bounded run writes the report and can record it on the Cloud Core with the DPAPI owner credential | `PROVEN_PROXY` | `.github/workflows/nightly-voice-corpus.yml` (02:15 UTC, report kept as a run artifact, summary on the run page); `scripts/core/voice-routing-qualification.ps1` on the owner's machine (syntax-gated; `-Post` not yet exercised against the real host). |
| 15.8 | The nine routing defects the first run found are fixed with regression tests and their utterances kept in the corpus | `PROVEN_AUTOMATED` | ADR-0080 §2; `test_voice_corpus_regressions.py` (37), `test_alarms_tr_time.py` (+6); neighbouring suites 469 passed. |
| 15.9 | Physical owner audio (microphone → transcription → the same path → audible answer) | `READY_FOR_OWNER_TEST` | Items 23b and 25 — now a thin final layer over proven routing. |
| 15.10 | The RESPONSE half (owner directive 2026-09-07, VOICE_SPEC §7a): the assistant's spoken answer for a category-balanced sample of the corpus, synthesised by the real TTS, transcribed by the real STT on an isolated byte path, normalised the router's way and compared (WER + content words), with the three honest marks | AUDIO GENERATION `PROVEN_AUTOMATED` · LOOPBACK SPEECH SEMANTICS `PROVEN_PROXY` · PHYSICAL OWNER HEARING `NOT_CLAIMED` | `docs/evidence/tts-loopback-2026-09-07-190306.json` (the M18.4/M19 baseline): OpenAI tts-1 → whisper-1, 40 of the 44 distinct spoken sentences of corpus v1 (413 cases; alarm 9, ambient 8, evolution 8, research 7, display 3, eye 2, presence 2, authority 1): 38 matched, 2 degraded, 0 mismatched, 0 error, mean WER 0.018; 226.1 s of WAV (mean 5.65 s, min 1.4 s, max 21.4 s), TTS latency mean 2.0 s, STT mean 1.7 s. The two degraded: `dep.1` — "Core'daki" is spoken as "kor" (the English word in a Turkish sentence); `ev.q.version` — the version "0.1.0" read as "sıfır nokta bir nokta sıfır" was garbled by the TTS on this run. The first run (`…-185857.json`: 39/1/0/0, WER 0.043) found that a recogniser's suffix apostrophes ("sekiz'e") were charged as word errors; fixed in the comparison, regression test added. `test_voice_loopback.py` (23) on the fakes; `-Post` not yet exercised: the production Cloud Core does not know `voice.tts_loopback` until it is released with this vocabulary. |

## Stage 16 — M18.4 self-evolution, self-healing and the zero-downtime update foundation (2026-09-07, ADR-0081 + addenda 1–3)

Spec: `docs/M18_4_SELF_EVOLUTION_SPEC.md`. `PROVEN_AUTOMATED` as in Stage 15.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 16.1 | Durable signals (incidents, recurring failed receipts, recurring research failures, open generation gaps) become deduplicated `EvolutionOpportunity` rows on a clock, each with a priority (P0–P3) and a promotion class derived from the risk table, never chosen | `PROVEN_REAL` | 2026-09-07: the clock-driven supervisor on the Hetzner Cloud Core opened two opportunities from the production ledger (a P1 for recurring `eye.enable` failures, a P2 for recurring `insufficient_valid_findings`), deduplicated across three later scans (`m18-4-qualification-2026-09-07-152224.json` phase E). Proxy: `test_evolution_supervisor.py`. |
| 16.2 | The owner's pause switch is a ledger row read back; a paused scan opens nothing; a failing source is a recorded fact, not a crash | `PROVEN_REAL` | 2026-09-07 on production: pause → scan `skipped_paused` with health `ok` → resume → `scanned` (run 3 phase E). The voice path: corpus category `evolution`. |
| 16.3 | The supervisor holds LAB authority only and never drives a lifecycle transition | `PROVEN_PROXY` (structural) | `test_the_supervisor_acts_with_lab_authority_only`. |
| 16.4 | The owner's voice over self-evolution routes through the ONE router and acts through receipts: pause / resume / cancel / hold; rollback and promotion by voice refused with the reason recorded; the four questions answered from rows | `PROVEN_AUTOMATED` | corpus category `evolution` (68 cases, 413/413 HEALTHY); `test_voice_evolution_tools.py`. |
| 16.5 | "Which version is running" is a fact: sha (or `unknown`), app version, contract versions, last-known-good, uptime, on health and `/v1/release/*` | `PROVEN_REAL` | 2026-09-07: `health.release` on production names c109302, contracts 12/3/1/1, last-known-good, uptime; `/v1/release/components` lists the agent (0.1.0, 29 capabilities, connected) and the web as unknown from the server (run 3 phase F). |
| 16.6 | Every migration `upgrade()` is expand-only unless it declares a contract phase or a reviewed widening | `PROVEN_PROXY` (structural, whole tree) | `test_migration_compatibility.py` (24 migrations). |
| 16.7 | Blue/green: idle colour up → verified → DEVICE handoff (device upstream → idle colour, the active colour drains, the sessions on the idle colour) → switch HTTP → verified through the edge (a bounded settle wait) → drain → old stopped; rollback the switch in reverse, devices first; refusals 73/75/76/79 before the switch; a live colour without the drain route falls back to the legacy switch out loud | `PROVEN_REAL` | 2026-09-07 run 7 (`m18-4-qualification-2026-09-07-173803.json`, 50/50): release ae88edd 246 HTTP probes / 0 dropped (the one-time legacy switch from c109302, presence gap 61.49 s named); controlled-failure rollback 141/0 with the devices taken back first (1/1 after 1 s, presence gap 2.23 s); explicit rollback 49/0 (handoff 1/1 after 1 s, gap 2.23 s); roll-forward 112/0 (handoff 1/1 after 0 s, gap 1.12 s); the agent online through the edge after every switch (ADR-0081 addendum 3). Run 8 (`…-175914`, the first from a drain-capable colour): release 1072d0d with the handoff BEFORE HTTP (1/1 after 0 s, presence gap 1.12 s; 245 probes, one slower than 3 s during the image build), rollbacks 139/0, 47/0, roll-forward 114/0 (handoffs 1/1, gaps 2.23–2.25 s). Run 9 (`m18-4-qualification-2026-09-07-182258.json`): Run 9 (`m18-4-qualification-2026-09-07-182258.json`), 50 checks, 0 failed, with the latency-aware prober: the release of 338ffec 239 HTTP probes / 0 dropped / max latency 2189 ms / 0 slower than 3 s, the device sessions on the new colour BEFORE HTTP (1/1 after 1 s, presence gap 2.26 s); the interrupted promotions reconciled with 0 dropped (presence gaps 0 s / 1.2 s); the controlled-failure rollback 140/0 (devices back first, gap 2.23 s); the explicit rollback 49/0 (handoff 1/1 after 1 s, gap 2.25 s); the roll-forward 117/0 (handoff 1/1 after 1 s, gap 2.24 s); the reconcile unit RECONCILE OK from its journal; release == HEAD, last-known-good exported. Earlier: addendum 2's runs (first cutover 4.1 s once; 247/0, 234/1). Proxy: `cloud-release-bluegreen.tests.ps1` 45/45; `test_broker_drain.py`. |
| 16.8 | A broken release is rolled back automatically by a supervisor outside the app, the incident recorded, a fix produced, staged and promoted; a bad candidate rejected with production untouched — real processes | `PROVEN_PROXY` | `tests/integration/test_selfhealing_e2e.py` (CI integration job, real processes on loopback). |
| 16.11 | The closed loop: incident → opportunity → the real pipeline's phases moving the opportunity idea → researching → design_ready → building → testing → evaluating → shadow_ready → owner_approval_required, dated; a failed gate parks it quarantined with the gate named; no promotion past the owner's line | `PROVEN_PROXY` (real processes, SQLite; the production image lacks the supervisor script) | `tests/unit/test_evolution_closed_loop.py`; `POST /v1/selfhealing/opportunities/{id}/heal`. |
| 16.12 | The first real blue/green cutover, the controlled-failure rollback, the explicit rollback and roll-forward, the supervisor and pause/resume on the real Cloud Core, each measured by a prober through the canonical edge | `PROVEN_REAL` | 2026-09-07 runs 14:58Z / 15:11Z / 15:22Z; run 3: 23/23 (`m18-4-qualification-2026-09-07-152224.json` + probe logs). Run 2 found and run 3 confirmed the fix of a real rollback bug (the script removed its own working directory). |
| 16.13 | Interrupted promotion recovery: a promotion SIGKILLed after the idle colour is up, and after the switch (the edge already on the candidate, RELEASE unwritten), is rebuilt to the last COMPLETED promotion — the candidate drained, stopped, its tree kept aside, never live | `PROVEN_REAL` | 2026-09-07 runs 5–7 on the host (`…-170850`, `…-172512`, `…-173803`): after_idle_up three times, after_switch twice; 0 dropped probes across crash + reconcile; device gaps 0 s / 5.68 s. Proxy: the suite's three interruption points, the emergency fallback (81), the idempotent reconcile. |
| 16.14 | Reboot with a colour active: the reconcile runs at boot after docker and rebuilds the same state | `PROVEN_PROXY` | run 7 phase H: `infra/systemd/pagentos-bluegreen-reconcile.service` installed, enabled, run once on the consistent host — RECONCILE OK in its own journal, nothing changed. The reboot itself is not performed autonomously on production (owner-only). |
| 16.9 | Availability is a measurement or "no measurement" | `PROVEN_PROXY` (ledger half) | `/v1/release/slo`: windows counted from the ledger, `availability: null`, `measurement: "none"`. Probes: `NOT_YET_PROVEN`. |
| 16.10 | Windows agent staged update (candidate manifest verified before the swap, Cloud Core sees the candidate or the engine rolls back); browser worker staged update (drain, verified candidate, routing, retire); web live update | `PROVEN_PROXY` (agent + worker; ADR-0081 addendum 3), web `NOT_YET_PROVEN` | `scripts/tests/agent-update.tests.ps1` 16/16; `BrowserWorkerSwapTests` 12/12 over the real fake worker (agent suite 397/397); the real runs are the owner's elevated update (items 26/27). |

## Stage 17 — M19 Digital Operator (2026-09-07/08, ADR-0082 + addenda 1–4)

Spec: `docs/M19_DIGITAL_OPERATOR_SPEC.md`. The lab (`devices/windows-agent/tests/PagentOS.Agent.Tests/Operator/`) runs real Notepad / Explorer / PowerShell windows on the owner's machine and, since this stage, on the GitHub `windows-latest` runner too (every effect read back: UI Automation values, window rects, exit codes).

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 17.1 | Every operator family member (32 names: app/window/keyboard/pointer/ui/screen/file/terminal) is an interactive-family device command executed by the companion with a re-observed result; the family is advertised only with `OperatorEnabled` on both halves; no other desktop path exists | `PROVEN_REAL` (mechanisms, this machine + the runner) / `PROVEN_PROXY` (the deployed 0.1.0 agent lacks the family until the owner's elevated update with `-Operator`) | lab 78/78 (`NotepadLifecycleTests`, `ExplorerFixtureTests`, `TerminalRunnerTests`, `AdvertisementTests` incl. the real service→pipe→companion chain); agent suite 475/475; `DEVICE_PROTOCOL.md` §6i |
| 17.2 | The focus guard: input never lands in a window other than the expected one; a mid-typing focus switch stops the stream with `focus_mismatch` naming typed/confirmed/uncertain characters; secrets are never typed (`secret: true` refused before any window is touched; password fields masked) | `PROVEN_REAL` | `FocusGuardTests`, `TypingGuardTests` (a real second Notepad brought to the front mid-stream: A + B == the typed prefix, B ≤ one batch — Windows assigns injected input to the thread that retrieves it, recorded in ADR-0082 addendum 2), `PayloadGuardTests` |
| 17.3 | The security review's findings closed: resolve-then-contain path roots (an NTFS junction inside a root is refused), the per-batch typing guard, a per-application argument policy (browsers: URLs and `--new-window` only; powershell: none) | `PROVEN_REAL` | independent review (security-reviewer, 1 high / 2 medium) → `PathConfinementTests` (a real `mklink /J`), `TypingGuardTests`, `ArgumentPolicyTests` (21) |
| 17.4 | The governed terminal: only allowlisted read-only commands run, headless, with a timeout; composition characters, aliases and non-allowlisted commands are refused before a process exists; cancel and timeout are typed | `PROVEN_REAL` | `TerminalRunnerTests`, `CancellationAndTimeoutTests` (`hostname` → the machine's name; `Remove-Item` → `permission_denied` with `ProcessesStarted == 0`) |
| 17.5 | OperatorTask: OBSERVE → PLAN → ACT → OBSERVE AGAIN → VERIFY POSTCONDITION; a failed postcondition after ≤ 2 retries ends `failed/postcondition_failed`; cancel between steps; timeout; a modal names the dialog; the trail is receipts + ledger rows | `PROVEN_AUTOMATED` | `test_operator_task.py`, `test_operator_tools.py` through `create_app` (the real relay, a fake device port), `test_operator_wiring.py` (the live port registered by `create_app`) |
| 17.6 | Object focus: windows and apps in the durable `object_focus` stack; "Bunu kapat" resolves the current window by id, "Önceki pencereye dön" the previous; no current window → a clarification, never a guess | `PROVEN_AUTOMATED` | `test_operator_focus.py`; migration 0025 expand-only (`test_migration_compatibility.py`); corpus deixis cases |
| 17.7 | The owner's voice: the corpus category `operator` (123 cases: apps, windows, typing, deixis, shell, cancel/status while a task runs, ASR noise, negatives) routes through the ONE router with 0 forbidden side effects; "Buraya şifremi yaz" refused; "Chrome'u aç" never reaches the browser worker; every existing category unchanged (536/536) | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py` 536/536, forbidden side effects 0 |
| 17.8 | Response / TTS: every successful case carries non-empty Turkish speech and the audible-turn record; the TTS → STT loopback proxy (real OpenAI tts-1 → whisper-1, isolated bytes, 40 distinct spoken answers): 38 matched, 2 degraded, 0 mismatched, mean WER 0.018 | `PROVEN_AUTOMATED` (speech structure) / `PROVEN_PROXY` (loopback semantics; physical hearing not claimed) | corpus harness assertions; `docs/evidence/tts-loopback-2026-09-07-190306.json` (ADR-0080 addendum; `scripts/voice/tts-loopback-qualification.ps1`) |
| 17.9 | The Living Core: `operator.running` / `operator.verifying` / `operator.failed` (UI contract v4, additive) from real task transitions; the Cockpit's "Dijital operatör" panel shows the step, the capability, the observed window and the error class | `PROVEN_AUTOMATED` | web suite 869/869 (`operator-states.test.ts` 29, `digital-operator.test.tsx` 16), `tsc` clean; no live render (the preview is behind the owner login) |
| 17.10 | The Cloud Core half on production: the tools answer `capability_missing` ("Bu bilgisayarda operatör yetkisi yok") until a device advertises the family — the honest production answer with the 0.1.0 agent | `PROVEN_REAL` (release) | harness run 10 (docs/evidence/m18-4-qualification-2026-09-07-205235.json): 16/16 checks; blue/green release of bd21f7f green → blue with no gap (335 probes, 0 dropped, max latency 950 ms), device sessions handed to blue before HTTP (1/1 after 1 s), presence gap 2.22 s, LKG 338ffec exported; runtime: UI contract v4 with operator.running/verifying/failed served, ledger row deployment.cloud_core.released = bd21f7f |
| 17.11 | Companion restart recovery: a task mid-flight fails `dependency_unavailable` with the receipt saying so and the next command works | `PROVEN_AUTOMATED` | `test_operator_task.py` (the port raising mid-step); the real companion restart is the existing Stage 5 proof |

## Stage 18 — M20 File & Document Intelligence (2026-09-08, ADR-0083)

Spec: `docs/M20_FILE_DOCUMENT_INTELLIGENCE_SPEC.md`. The oracle is `services/api/tests/fixtures/documents/` (deterministic fixtures, `truth.json`, `expected/*.extract.json`); the device lab reads real copies of the fixtures under the fixture root through the companion's real dispatcher; the Cloud Core tests consume the same expected extracts through a fake device port.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 18.1 | Six document capabilities (`file.search/locate/inspect/read/compare`, `document.extract`) are interactive-family device commands executed by the companion inside the authorised roots, advertised only with `OperatorEnabled`; every extracted block carries the reference the answer cites; a format the device cannot parse answers `unsupported_format`, never an empty success | `PROVEN_REAL` (mechanisms, this machine + the runner) / `PROVEN_PROXY` (the deployed 0.1.0 agent until item 28) | documents lab 40/40 on this machine (real copies of the fixtures under the fixture root, the real dispatcher); whole agent project 514/514 + audio 135/135; the GitHub windows-latest runner ran the same lab green on the device merge (ce4378d, run 34163771029) and the web merge (f7d632f); after the security fixes: documents lab 54/54 (bomb containers, bounded reads), whole agent project 529/529; `DEVICE_PROTOCOL.md` §6j |
| 18.2 | Extraction equals the oracle for every fixture kind (DOCX paragraphs/headings/tables, XLSX rows + formulas, PPTX slides, PDF pages, MD sections, CSV rows, JSON keys, source chunks + symbols) and the two identities behave (`file_id` = location, `doc_id` = content) | `PROVEN_REAL` | the documents lab against `expected/*.extract.json` and `truth.json` |
| 18.3 | Confinement and privacy: a path outside the roots and a junction pointing outside → `permission_denied`; secret-bearing names never read (present or absent, the same answer); reparse points never followed; bounds truncate loudly (`truncated: true`), never silently; a decompression bomb inside an OOXML container is refused from the central directory before any part is inflated | `PROVEN_REAL` | the documents lab (real junction, real `.env`, a 300 KB and a 60 MiB file, generated bomb containers with the working set measured) |
| 18.4 | Cloud Core answers cite refs chosen deterministically: every `truth.json` question answered with the expected ref and excerpt; the comparison's changed refs exact; common points with one ref per document; two same-title documents named by path; "bulamadım" with what was searched when no block answers | `PROVEN_AUTOMATED` | 10/10 truth questions (`test_truth_question` parametrized: pdf-page-3, xlsx-total, xlsx-formula, pptx-slide-4, pptx-count, docx-clause-3, md-decisions, csv-row-7, json-dil, py-symbol), the comparison changed_refs == [p2, p8] with both clause texts, the common-points entry with one ref per document, the same-title pair named by path; `test_documents_answers.py`, `test_documents_retrieval.py` |
| 18.5 | The index grows only from owner-initiated reads/searches (no crawling); `document_index` migration 0026 expand-only; focus kinds `file` / `document` / `folder` on the M19 stack with current / previous by identity | `PROVEN_AUTOMATED` | `test_documents_index.py`, `test_documents_focus.py`, `test_migration_compatibility.py` |
| 18.6 | The owner's voice: the corpus category `documents` routes through the ONE router with 0 forbidden side effects; "Bunu özetle" with no document → a clarification; "Bu dosyayı sil" reaches no tool and no device capability; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category documents 148 cases; 686/686 total; forbidden side effects 0 |
| 18.7 | Response / TTS: every successful case carries non-empty Turkish speech naming the file and the place; the loopback proxy on a documents sample | `PROVEN_AUTOMATED` (speech structure) / `PROVEN_PROXY` (loopback semantics) | two real OpenAI runs with the same seed (tts-1 → whisper-1, isolated bytes): the first (`docs/evidence/tts-loopback-2026-09-07-230106.json`) 32 cases balanced over eight categories, 26 matched / 5 degraded / 1 mismatched, mean WER 0.082 — the mismatch is whisper dropping two Turkish words after the English brand name in "PowerShell açtım efendim" on a 1.4 s clip (the STT provider already sends language=tr; an STT artefact, recorded), the two degraded documents cases were spoken file extensions — fixed (`spoken_file_name`); the second run (`…-230910.json`) re-sampled the same 32 after the fix: `doc.read.1` matched exactly with the new wording, then OpenAI answered 429 "no credits remaining" for 22 of 32 cases (owner item 29) — the harness now reports PROVIDER_UNAVAILABLE for such a run instead of a regression |
| 18.8 | The Living Core: `document.analysis` (UI contract v5, additive) with the caption from published metadata only; the Cockpit "Belgeler" panel with current / previous document and the last answer's refs, an honest empty state | `PROVEN_AUTOMATED` | web suite 927/927 (+58: document-states.test.ts, documents-panel.test.tsx), tsc clean |
| 18.9 | Security review closed; no authority boundary changed; no delete/move/write capability exists | `PROVEN_REAL` (review) | independent review of the device half: 1 high (an OOXML decompression bomb crashed the companion, verified live) + 2 low (secret-name existence oracle; whole-file reads before the 64 KB cut), all fixed and pinned by real-file lab tests with allocated bytes measured (ADR-0083 addendum 3, residual risks recorded); independent review of the Cloud Core + web half (scope cb35298..d2fccdf): 1 medium (no cloud-side guard on `folder`/`pattern` before the device call — single-layer confinement), 2 low (a model-supplied folder string persisted as the folder focus label; the spoken excerpt unbounded while `refs[].excerpt` was capped) and one functional gap (answer refs never published — the bus drops lists); all closed: `_validate_folder`/`_validate_pattern` refuse absolute, `..`, UNC/device and oversize inputs with a typed `invalid_argument` receipt that never echoes the value and reaches no device (`test_documents_confinement.py`), folder focus only from a known root or the device's `searched_roots`, a ≤ 400-char spoken excerpt cut at a word boundary, and exactly one structured bus key `refs` (≤ 8 `{ref, path}` pairs ≤ 64 chars, excerpt never on the bus; `test_uistate.py`, `test_documents_tools.py`); verified sound: no new endpoint, device selection by capability only, a `permission_denied`/`unsupported_format`/`not_found` result is never indexed, receipts and ledger rows carry no excerpts, focus rows keyed by ids never titles, contract v5 additive, migration expand-only, no `dangerouslySetInnerHTML`, nothing fetched by the browser |
| 18.10 | The Cloud Core half on production through the canonical blue/green path; the tools answer `capability_missing` ("Bu bilgisayarda belge okuma yetkisi yok") until a device advertises the family | `PROVEN_REAL` (release) | harness run 11 (`docs/evidence/m18-4-qualification-2026-09-08-000603.json`, 16/16 checks): blue/green release of 092e56d blue → green through the canonical path with no gap (360 probes, 0 dropped, max latency 1126 ms), the device sessions handed to green before it took HTTP (1/1 after 1 s), presence gap 2.22 s, RELEASE = 092e56d, LAST_KNOWN_GOOD = bd21f7f exported; the release ledger row now reads "blue/green geçişi, api-green" (the M19 wording fix at work); runtime: read back from production after the release with an owner session: `/v1/system/health` release = 092e56d, last_known_good = bd21f7f; `/v1/ui/state/contract` carries `document.analysis` beside the operator states (47 states); `/v1/ui/state` reports contract version 5; device MAIL online with 29 capabilities (the documents family absent, as expected for the 0.1.0 agent — the tools answer `capability_missing`); the ledger row `deployment.cloud_core.released` = 092e56d recorded at 00:08:35Z |

## Stage 19 — M21 Mail & Calendar (2026-09-08, ADR-0084)

Spec: `docs/M21_MAIL_CALENDAR_SPEC.md`. The oracle is `services/api/tests/fixtures/mail_calendar/` (a deterministic mailbox and calendar with computed ground truth); the providers are proven against scripted protocol fakes so the parsing they do is real; nothing is ever sent for real.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 19.1 | Three tiers: READ and PREPARE tools act on the owner's word; `mail.send` / `calendar.commit` refuse without a read-back followed by the owner's confirmation in the same session, refuse when the host flag is off, and never act twice | `PROVEN_AUTOMATED` | `app/actions/confirmation_gate.py`: not_read_back, no_confirmation (a confirmation before the read-back), send_disabled, account_missing, already_sent (a second confirmation after success and after a discard) — each proven for mail and calendar with the fake sender/writer never touched (`test_mail_service.py`, `test_calendar_service.py`, `test_mail_calendar_routes.py`) |
| 19.2 | Providers behind Protocols: IMAP (RFC 2047 headers, UTF-8 search, HTML reduced to text, attachments as metadata), SMTP (reply headers; `send_disabled` when the flag is off), CalDAV/ICS (an in-repo iCalendar parser with RRULE/EXDATE/VTIMEZONE expansion) — each against a scripted fake of its protocol | `PROVEN_AUTOMATED` | `test_mail_imap_provider.py` (a scripted fake IMAP4 server on 127.0.0.1: LOGIN, LIST, SELECT, SEARCH UNSEEN/FROM/TEXT CHARSET UTF-8, FETCH; RFC 2047 subjects; the HTML-only body reduced to the truth`s text), `test_mail_smtp_sender.py` (a scripted fake SMTP server with AUTH; the reply headers exact; send_disabled with the flag off), `test_calendar_caldav_provider.py` (httpx.MockTransport REPORT/PUT), `test_calendar_ics.py` (the 19 occurrences equal to truth.json) |
| 19.3 | Every `truth.json` entry answered: counts, the unread run, the latest from Ali, the thread, the HTML-only body, searches, the reply headers; the 19 occurrences, today's agenda, the EXDATE, the conflict pair, Friday's free windows, the two proposals with their conflicts | `PROVEN_AUTOMATED` | every truth.json entry exact: mail counts, unread run, latest_from_ali, thread_proje_plani, the HTML-only body, search.fatura / search.bütçe, reply_to_latest_ali headers; calendar expanded_events (19), today, ekip_occurrences 9 with the EXDATE skipped, the conflict pair, Friday`s two free windows, both proposals with their conflicts |
| 19.4 | No delete / move / mass action exists; secrets never in logs, receipts or ledger rows; nothing polled; `account_missing` is the honest answer with no account | `PROVEN_AUTOMATED` | API unit suite 5405 passed / 2 skipped; 112 mail/calendar tests incl. test_mail_security_hardening.py; test_identity_enforcement.py covers the six routes; migration 0027 expand-only; ruff clean |
| 19.5 | Focus by identity: `message` / `thread` / `event` / `draft` / `proposal` on the M19 stack; "Gönder." binds the current draft only after its read-back, a bare "Gönder." asks | `PROVEN_AUTOMATED` | `test_mail_calendar_focus.py`, the corpus |
| 19.6 | The owner's voice: the corpus category `mail_calendar` through the ONE router with every external side effect forbidden except in the two confirmation cases, which reach the fake only; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category mail_calendar 120 cases (the two confirmation cases as real two-turn sequences, two no_owner_turn negatives); 806/806 on the merged main (804 corpus cases + the suite's two harness-level checks); forbidden side effects 0 |
| 19.7 | Response / TTS: every successful case carries non-empty Turkish speech (the sender, the subject, the date, the draft read back verbatim); the loopback proxy | `PROVEN_AUTOMATED` (speech structure) / NOT_YET_PROVEN — the loopback proxy could not run for real: OpenAI answers 429 "no credits remaining" (owner item 29); the probe `docs/evidence/tts-loopback-2026-09-08-020232.json` (one case per category incl. mail_calendar) is recorded as PROVIDER_UNAVAILABLE by the harness — the speech-structure checks of every corpus case stand, the loopback semantics for M21 remain unproven until credits return |
| 19.8 | The Living Core: `mail.activity` / `calendar.activity` (UI contract v6, additive) from published metadata only; the Cockpit "Posta" and "Takvim" panels with the approval pair wired to the same gate, honest empty states | `PROVEN_AUTOMATED` | web suite 1018/1018 (+91: mail-calendar-states.test.ts, mail-panel.test.tsx, calendar-panel.test.tsx, approvals-client.test.ts, the exhaustive visual kind table), tsc clean, next build clean |
| 19.9 | Security review closed; no authority boundary changed | `PROVEN_REAL` (review) | independent review of the Cloud Core + web halves (scope c3dfd02..0ac8d67): 2 HIGH — the gate was vacuous (`read_back_at` stamped at PREPARE, `confirmed_at` computed inside `send()`; the router's MAIL_SEND resolution never consulted) and send/commit were read-check-write without a lock (concurrent confirms could double-send); 2 MEDIUM — a 3 000-level nested MIME message broke a whole folder via RecursionError (verified live), RRULE expansion unbounded with `MAX_WINDOW_DAYS` dead; 1 LOW — CRLF in a folded From: header reached a reply draft and the stdlib's refusal was uncaught. All five closed (ADR-0084 addendum 2, migration 0029 `read_back_session_id` / `read_back_turn` / `confirmed_by` / `last_error`): the read-back is an explicit act bound to its session and turn; a voice confirmation must come from the SAME session and a STRICTLY LATER turn resolved by the deterministic router (`confirmation_not_owner` for a model-issued mutation with no owner turn behind it; `not_read_back` for another session); a REST confirmation is the owner's own authenticated act; an atomic compare-and-swap moves the row to `sending`/`committing` before any provider call (two threads confirming the same draft: exactly one send, the other `already_sent`); per-message parse isolation with MIME depth/part bounds (`unparseable` counted and spoken); RRULE occurrences capped and the window clamped; header control characters stripped at parse time, recipients validated, a typed `send_failed` receipt with the row back to `read_back`. Verified sound by the review: no secret in logs/receipts/ledger rows, TLS with verification everywhere, snippets only at rest, attachments never fetched, the fakes bound 127.0.0.1 only, the corpus negatives real, the web posts to exactly the six routes with no HTML rendering of mail content. Regression tests: `test_mail_security_hardening.py`, the extended service/provider/ics/routes/tools suites, the corpus's real two-turn confirmation cases and two negatives. |
| 19.10 | The Cloud Core half on production through the canonical blue/green path; the tools answer `account_missing` until the owner puts an account on the host (item 30) | `PROVEN_REAL` | harness run `m18-4-2026-09-08-080824` (evidence `docs/evidence/m18-4-qualification-2026-09-08-080824.json`): PASS, 16 checks, 0 failed — the blue/green release of 571ddb2 took 149.7 s with no gap (probes=337 dropped=0 gap=0 s max_latency=2533 ms slow>3 s=0), the device sessions handed to api-blue before it took HTTP (1/1 after 0 s), device presence gap 1.13 s (limit 15 s), alarms 1→1 and routines 1→1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanned production signals (2 tracked, 0 opened) and pause/resume proven, final release == HEAD 571ddb2 with contract v12 served and LAST_KNOWN_GOOD exported = 092e56d; runtime read-back: read back on production 2026-09-08 08:16–08:20Z, after the M22 release that superseded it within the hour (c2f1904 contains all of M21): `/v1/system/health` release=c2f1904 last_known_good=571ddb2; `/v1/ui/state/contract` contract_version=7 (a superset of v6) with `mail.activity` and `calendar.activity` among its 50 tokens; `GET /v1/mail/drafts/pending` → 200 `{"drafts":[]}` and `GET /v1/calendar/proposals/pending` → 200 `{"proposals":[]}` with an owner session, 401 without one; the release ledger row `deployment.cloud_core.released` present; the tools' own `account_missing` answer is proven by the unit suite and awaits the owner's account on the host (item 30) for a real-account run |

## Stage 20 — M22 Artifact Factory (2026-09-08, ADR-0085)

Spec: `docs/M22_ARTIFACT_FACTORY_SPEC.md`. The oracle is `services/api/tests/fixtures/artifacts/` (five specs and the refs an independent reader must find per format); every render is reopened by a reader that is not its writer.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 20.1 | A structured `ArtifactSpec` is the only input; every number the owner said is carried literally and validated literally; renderers behind the M13 `Renderer` Protocol for DOCX/PDF/HTML/MD/TXT/XLSX/CSV/PPTX/JSON, deterministic (one spec, one hash) | `PROVEN_AUTOMATED` | API unit suite 5743 passed / 2 skipped on the merged main; ~200 artifact tests (spec 41, renderers 54, validation 41, factory 12, routes 28, wiring 4, identity 3, tools 22, render-fetch tokens 11); ruff clean; determinism proven (two renders, one hash) |
| 20.2 | Validation reopens every render with an independent reader and compares element by element on M20's reference scheme; totals recomputed; a lying renderer is caught with the failing ref; an invalid render is never presented as done | `PROVEN_AUTOMATED` | every fixture spec × format validates ok against truth.json.expect; lying renderers caught with the right failing ref; seven hostile inputs (OOXML bomb, > 10 000 parts, oversized text/JSON, > 200-page PDF, non-zip bytes, malformed JSON) refused as failing validation, never a crash |
| 20.3 | `file.fetch` on the device: the dialled Cloud Core origin only, no off-origin redirect, ≤ 50 MiB while streaming, a sanitised name into the Downloads root, SHA-256 verified before the file is kept, then `file.open`; every refusal leaves nothing behind | `PROVEN_REAL` (this machine + the runner) / `PROVEN_PROXY` (the deployed agent until item 28) | fetch lab 35 facts (FileFetchTests.cs against a local HttpListener origin: foreign origin / scheme / path / traversal / untold companion refused with zero requests; wrong hash and short body leave nothing; 50 MiB bounds before, at the header and on the wire; redirects refused; 22 bad names; a collision suffixed; the pipe handshake carrying the origin proven with a real companion); after the review fixes fetch lab 66, whole agent project 596/596; the runner ran the fetch lab green on every M22 merge (ad3471a, 6293df0, 7c2154d, 4a16428, e6189b7) |
| 20.4 | The owner's voice: the corpus category `artifacts` through the ONE router; the rendered numbers ⊆ the spoken numbers; "Bunu aç" with nothing produced asks; "Sil" reaches no tool; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category artifacts 112 cases (incl. art.neg.invented, art.neg.secret); 918/918 total on the merged main with an OK case failing on any refusal receipt; forbidden side effects 0 |
| 20.5 | Response / TTS: every successful case names the file, the format(s), the verdict and any failing ref; the loopback proxy | `PROVEN_AUTOMATED` (speech structure) / NOT_YET_PROVEN — the loopback proxy cannot run for real while OpenAI has no credits (owner item 29); PROVIDER_UNAVAILABLE recorded by the harness |
| 20.6 | The Living Core: `artifact.factory` (UI contract v7, additive) from published metadata only; the Cockpit "Üretilenler" panel with verdicts, download links on the owner-session-gated render URL and an honest empty state | `PROVEN_AUTOMATED` | web suite 1090/1090 (+70: artifact-states.test.ts, artifacts-panel.test.tsx, artifacts-client.test.ts), tsc clean, next build green |
| 20.7 | Security review closed; readers bounded against hostile inputs; no authority boundary changed | `PROVEN_REAL` (review) | device half (`file.fetch`, scope 2d442b2..m22-device): 1 HIGH — the body was read with the synchronous `Stream.Read`, which ignores the cancellation token, so a peer at the pinned origin sending headers and a few bytes then silence held the companion thread and the `.part` file past the documented 30 s cap (verified live by the reviewer); 2 MEDIUM — a verify-then-rename window a same-user process could exploit, and an executable blocklist without the macro-enabled Office family / `.url` / `.jar` / `.pif` with no Mark-of-the-Web on fetched files; 2 LOW — the system proxy left on, Unicode format-control characters accepted in names. All closed (ADR-0085 addendum 3): async end to end under one token linking the budget and the 30 s cap (a stalled origin answers `timeout` at 30.003 s measured, the `.part` deleted, the slot freed; two concurrent slots, a third `fetch_busy`), the temp handle held across `File.Move` and the final path re-opened exclusively and re-hashed (`sha256_mismatch_after_move` deletes it), 13 extensions added to the shared blocklist (so `file.open` refuses them too), `Zone.Identifier` `ZoneId=3` on every kept file, `UseProxy = false`, Cc/Cf/lone-surrogate names refused; fetch lab 35 → 66, agent project 596/596. Verified sound by the review: origin pinning against IDN/userinfo/ports/IPv6/`..`/`%2e%2e` (empirically), redirects off, no TLS bypass, the handshake fail-closed with an older service, the pipe's four-layer identity unchanged, `OperatorEnabled` on both halves, the Downloads root confined at fetch time and after the write, bounds before/at-header/on-wire, streaming never buffered, the lab clean; Cloud Core + web halves (scope 2d442b2..4a16428): 1 HIGH — a spreadsheet/CSV cell beginning with `=`, `+`, `-` or `@` rendered as a live formula (`data_type == 'f'`, verified live with `=HYPERLINK(…)`), certified valid by the validator and auto-opened by `artifact.open`; 1 MEDIUM/HIGH — python-markdown link syntax let a `javascript:` href through the HTML renderer (verified live); 1 MEDIUM — the never-invented-numbers rule was opt-in (skipped whenever the router found no number in the utterance); 2 LOW — `openpyxl`'s worksheet `iterparse` without `defusedxml`, no secret-reference gate on `artifact.create`; plus the already-tracked device-auth gap (addendum 4 decision 5) closed by the signed render URL. Fixes: formula-trigger cells (`=`, `+`, `-`, `@` after BOM/whitespace) refused at the spec (`Sheet._no_formula_injection`, the dataset validator; the declared `formulas` map exempt) and neutralised by the renderers as defence in depth (`app/artifacts/security.py`), with the validator failing any cell that became a formula outside the declared map even when its text matches; only `http`/`https`/`mailto` hrefs and srcs survive the HTML renderer, a `<script>`/`on*=` surviving conversion trips `UnsafeGeneratedHtmlError`; `artifact_create` always passes `spoken_numbers` (an empty list when the utterance had none) so an invented number is refused naming its ref; `contains_secret_reference` over the title and every text; `defusedxml` declared as a runtime dependency so openpyxl's `iterparse` is protected, and a refused entity-expansion payload reported as a bound; ADR-0085 addendum 6. Verified sound by the review: object-store keys never derive from a title; the device is only ever pointed at this Cloud Core's own render path built server-side; no delete/overwrite tool; capability-only device selection; contract v7 additive; migration expand-only; router-level owner-session gating with the two new routes listed; zip-layer bounds before any library parse; python-docx and python-pptx parse with `resolve_entities=False`; the corpus's `artifact.render` exemption narrow; the web calls exactly three routes, renders text only, and never claims "Açıldı" without the receipt |
| 20.8 | The Cloud Core half on production through the canonical blue/green path | `PROVEN_REAL` | harness run `m18-4-2026-09-08-081308` (evidence `docs/evidence/m18-4-qualification-2026-09-08-081308.json`): PASS, 16 checks, 0 failed — the blue/green release of c2f1904 took 157.7 s with no gap (probes=381 dropped=0 gap=0 s max_latency=1365 ms slow>3 s=0), the device sessions handed to api-green before it took HTTP (1/1 after 1 s), device presence gap 2.26 s (limit 15 s), alarms 1→1 and routines 1→1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanned production signals (2 tracked, 0 opened) and pause/resume proven, final release == HEAD c2f1904 with contract v12 served and LAST_KNOWN_GOOD exported = 571ddb2. CI after c2f1904: run 34202322471 on the documentation-only 486491a failed one fetch-lab fact on the runner (the peer's accepted-socket count was read before the listener's continuation had run, so it read 0 while both stalled fetches were connecting) — a timing assumption in the test, not in the fetch; fixed in 437eb7e so the test waits for the peer to have accepted both stalled sockets before asserting that the refused third one added none; fetch lab 66/66; runtime read-back: read back on production 2026-09-08 08:16–08:20Z: `/v1/system/health` release=c2f1904 last_known_good=571ddb2 started 08:14:40Z; `/v1/ui/state/contract` contract_version=7 with `artifact.factory` present (50 tokens; `document.analysis`, `mail.activity`, `calendar.activity`, `operator.running` all present); `/v1/ui/state` at version 7; `GET /v1/artifacts/renders/fetch/<64 zeros>` and `/…/not-a-token` → 404 without a token, `POST /v1/artifacts/factory` → 401 without a session; the owner's device online through the edge with 29 capabilities (the deployed agent predates the documents family and `file.fetch` — item 28 — so `artifact.open` answers `capability_missing` there until the agent is updated); ledger row `deployment.cloud_core.released` result=c2f1904 at 08:15:59Z "Cloud Core c2f1904 canliya alindi (blue/green gecisi, api-green)" |

## Stage 7 — Real voice

| # | Criterion | Status |
|---|---|---|
| 7.1 | Real STT/TTS providers behind the existing abstraction | `NOT_YET_PROVEN` (blocked on provider keys) |
| 7.2 | Owner speaker enrollment against real samples | `NOT_YET_PROVEN` |
| 7.3 | Turkish STT/TTS quality qualification | `NOT_YET_PROVEN` |

## Stage 8 — Real mobile

| # | Criterion | Status |
|---|---|---|
| 8.1 | Push arrives on a locked phone | `NOT_YET_PROVEN` (server contract is `PROVEN_PROXY`, see M9) |
| 8.2 | Microphone/realtime under OS backgrounding | `NOT_YET_PROVEN` |
| 8.3 | Narration continues across PC and phone | `PROVEN_PROXY` (cross-session cursor resume proven against the real API) |

## Stage 21 — M23 App Factory (2026-09-08, ADR-0086)

Spec: `docs/M23_APP_FACTORY_SPEC.md`. The oracle is `services/api/tests/fixtures/apps/` (the task-tracker's expected file list and its DOM oracle); every generated project is validated by policy on the Cloud Core BEFORE the device is asked and again by the device at scaffold; every run is a child inside a Windows Job Object under the `Projects` root; "the app works" is a DOM oracle satisfied through the browser gateway, never a claim.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 21.1 | A structured `AppSpec` is the only input; three built-in templates render deterministically; `ProjectFiles` policy (≤ 200 files / ≤ 2 MiB / relative text-only paths / the secret scanner / the fixed command allowlist) refuses before the device is asked | `PROVEN_AUTOMATED` | API unit suite on the merged main 5962 passed / 2 skipped (before the review fixes) and 109/109 App Factory facts + 1180 corpus/intents/UI-state facts after them; spec / generator / validation / service / tools through `create_app` / wiring / routes (13) / identity rows; every template renders for its spec and the task-tracker's file list equals the fixture's; determinism; all three templates rerun for real on this machine (tracker `node tests/run.js` 5/5, static page 2/2, CLI `--help`, `selamla dünya`, 3/3); the ProjectFiles policy refuses the path, size, secret, allowlist, alternate-data-stream, trailing-dot/space, empty-segment and repeated-separator shapes; the spec refuses every control character in free text; free text can never reach a code file (splice-point guard + a template scan + `node --check` on every rendered .js with hostile-but-allowed text); ruff clean |
| 21.2 | The device `projects` family: scaffold under the `Projects` root with the marker rule; run/test as a bounded Job Object child (kill-on-close, memory, CPU, breakaway denied, scrubbed environment, bounded log, ≤ 2 running, ≤ 30 min); stop kills only the job; a non-allowlisted command refused before any process | `PROVEN_REAL` (this machine + the runner) / `PROVEN_PROXY` (the deployed agent until item 28) | projects lab 99/99 with real `python -m http.server` and `node` children on this machine (scaffold → files, marker, sha256 map; outside path / marker-less folder / oversize list refused; a real child in a job on a free port answering `GET /`, the log captured, two projects, a third refused; stop → the child gone within 5 s while a sentinel outside the job survived; the runner's counts parsed; 16 non-allowlisted manifest forms and a tampered marker refused before any process; `PAGENTOS_TEST_SECRET`, `PAGENTOS_AGENT_DeviceId`, `MY_API_KEY`, `GITHUB_TOKEN`, `DB_PASSWORD`, `AZURE_CLIENT_SECRET` absent from the child's `process.env`); the Job Object's limits read back from the kernel (`KILL_ON_JOB_CLOSE`, breakaway denied, 512 MiB, 10 min CPU, 8 processes, `UILIMIT_ALL`, `IsProcessInJob`); a 768 MiB allocation ended; the 1 MiB log cut with `truncated: true`; the test bound, the caller's cancel and the lifetime each ended the job (2 s seams); whole agent project 695/695 locally and on the runner; `dotnet format` clean; agent 0.5.0; the deployed agent stays 0.4.0 until item 28 |
| 21.3 | The sample app is exercised for real: the scaffolded task-tracker served by the runner, opened headless, the oracle's DOM facts (add → listed → done → reload → still there) satisfied | `PROVEN_REAL` (this machine) | `scripts/tests/render-app-template.py` + `scripts/tests/exercise-app-oracle.py`: the task-tracker rendered the Cloud Core's way, served with the manifest's own allowlisted command, opened in headless Chromium 151 through the browser worker's Playwright, the oracle run as written — `docs/evidence/m23-dom-exercise-2026-09-08-090711.json` and, after the security fixes changed the templates, `…-093342.json`: PASS, five initial assertions, eight interaction steps (fill "Sütü al", add, listed, toggle done, class `done`, reload, still listed, still done), 0 failures |
| 21.4 | The owner's voice: the corpus category `apps` through the ONE router; "Projeyi sil" reaches no tool; a path in a name refused before the device; nothing scaffolded → clarification; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category apps 116 cases (canonical, paraphrase, ASR noise, no diacritics; the fake device serving the five capabilities); the whole corpus 1032/1032 HEALTHY on the merged main and 1180 corpus/intents/UI-state facts after the review fixes; "Projeyi sil" reaches no tool (no delete tool exists); a path in a name refused at the spec before the device; nothing scaffolded → clarification; forbidden side effects 0 |
| 21.5 | Response / TTS: every receipt names the project, the port when running, the test counts; the loopback proxy | `PROVEN_AUTOMATED` (speech structure: the project, the port while running, the counts, the honest no-op) / NOT_YET_PROVEN for the loopback — OpenAI has no credits (owner item 29): `docs/evidence/tts-loopback-2026-09-08-092123.json`, 25 balanced cases incl. `app.run.para.v3` and `app.run.asr`, every synthesis HTTP 429, summary PROVIDER_UNAVAILABLE recorded by the harness |
| 21.6 | The Living Core: `app.factory` (UI contract v8, additive) from published metadata only; the Cockpit "Uygulamalar" panel with the running link, the test counts, the three controls and an honest empty state | `PROVEN_AUTOMATED` | web suite 1167/1167 (+75: `app-states.test.ts` 37, `apps-panel.test.tsx` + `apps-client.test.ts` 38), `tsc` clean, `oxlint` clean, `next build` green; the link only for a validated port on 127.0.0.1 and only while running, never fetched by the page; each control issues exactly one call; the receipt parser reads the device's own `project.test` shape |
| 21.7 | Security review closed; the generated code, the manifest commands and the runner bounded; no authority boundary changed | `PROVEN_REAL` (review) | independent review of 486491a..6259f61 (`.claude/agent-memory/security-reviewer/m23-app-factory-review.md`): 1 CRITICAL — a newline in `page_title` ended the `// {{PAGE_TITLE}}` comment line of the static page's `app.js` and the rest ran as top-level JavaScript certified by the ProjectFiles policy (verified live by the reviewer); 1 MEDIUM — the Cloud Core's path check accepted an alternate data stream and a reserved name hidden by a trailing space that the device refuses. Both closed on main (f7baf19, ADR-0086 addendum 4): free text never lands in code (the splice point refuses anything but a closed-alphabet token or a generator-made JSON literal into a code file, the template comments fixed, a structural scan + `node --check` tests), the spec refuses every control character and U+2028/U+2029 in free text, the two validators agree (four new shapes). Verified sound by the review: resolve-then-contain on the Projects root against junctions (56 lab facts rerun), the argv-tokenised runtime allowlist with `--bind 127.0.0.1` exact and no shell, the Job Object bounds read back from the kernel, the real-process lab 99/99 rerun, the exercise URL built server-side from the device's own `run_port`, no delete tool, the corpus negatives live, the four REST routes owner-gated, the lab's teardown. The CI secret-hygiene job also caught two key-shaped test literals (now assembled at runtime; the M8 scanner under test unchanged) |
| 21.8 | The Cloud Core half on production through the canonical blue/green path | `PROVEN_REAL` | harness run `m18-4-2026-09-08-094600` (evidence `docs/evidence/m18-4-qualification-2026-09-08-094600.json`): PASS, 16 checks, 0 failed — the blue/green release of f7baf19 took 147.6 s with no gap (probes=322 dropped=0 gap=0 s max_latency=3349 ms, 4 probes over 3 s during the image build), the device sessions handed to api-blue before it took HTTP (1/1 after 0 s), device presence gap 1.12 s (limit 15 s), alarms 1→1 and routines 1→1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanned production signals (2 tracked, 0 opened) and pause/resume proven, final release == HEAD f7baf19 with contract v12 served and LAST_KNOWN_GOOD exported = c2f1904; runtime read-back: read back on production 2026-09-08 09:49–09:52Z: `/v1/system/health` release=f7baf19 last_known_good=c2f1904 started 09:47:24Z; `/v1/ui/state/contract` contract_version=8 with 51 tokens, `app.factory` present and the `appfactory` subsystem listed (every M20–M22 token still present); `/v1/ui/state` at version 8; `GET /v1/apps` → 401 without a session, 200 `{"projects":[]}` with an owner session, `POST /v1/apps/<unknown>/run` → 404; the owner's device online through the edge with 29 capabilities (the deployed agent 0.4.0 predates the `projects` family — item 28 — so `app.create` answers `capability_missing` on production until the agent is updated); ledger row `deployment.cloud_core.released` result=f7baf19 at 09:48:42Z "Cloud Core f7baf19 canliya alindi (blue/green gecisi, api-blue)" |

## Stage 22 — M24 Capability Genesis (2026-09-08, ADR-0087)

Spec: `docs/M24_CAPABILITY_GENESIS_SPEC.md`. The oracle is the pair of fixture applications under `services/api/tests/fixtures/genesis/` (a counter box and a lamp box with different shapes) and the rule that nothing in `app/genesis` may name either of them; "the assistant can now do it" is a value read back from the application itself after the generated adapter was registered through every M7 gate and dispatched as an isolated subprocess — never the adapter's own claim.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 22.1 | The interface description is the research step made concrete: fetched from the application's own `/spec` on loopback only, parsed at one choke point with the strict-token discipline, a closed schema subset, bounded; anything else refused before design | `PROVEN_AUTOMATED` | `test_genesis_interface.py` on the merged main: the closed alphabets and the bounds (16 operations accepted, 17 refused at parse; 16 fields; 8 KiB), loopback-only proven against every bypass an independent review could construct (`127.0.0.2`, `0.0.0.0`, `[::1]`, `127.1`, a decimal IP, userinfo, `localhost.evil.test`, trailing dots, path/query/fragment, `..`), the reserved and privileged ports refused by name (the review's MEDIUM), the `{id}` template refused at parse rather than failing at run time, a read-back that shares no output field with a mutation refused as one that could never witness it, and `fetch_interface` bounded at 5 s / 64 KiB before parsing with redirects and non-JSON refused |
| 22.2 | The adapter is generated behind the existing `SkillGenerator` seam, generic over the description, stdlib-only, one loopback call with a timeout and no redirects; free text never lands in generated source except through `repr`/`json.dumps`; its rendered tests and evals run only against a live base URL | `PROVEN_AUTOMATED` | `test_genesis_adapter.py`: both fixtures rendered through the same code path, deterministic output, one hardcoded loopback URL per generated module, no f-strings and no value spliced outside `repr()`/`json.dumps()` from an already-token-validated field (the description schema carries no free-text field at all, which closes the M23 CRITICAL class by construction), and rendered tests/evals that fail honestly with `dependency_unavailable` without a live base URL rather than passing on a mock |
| 22.3 | The run: capability_missing → researching → designing → building → testing → classifying → [awaiting_approval] → rolling_out → registering → available → used → verified, every transition a row + ledger row + UiState publish; the M7 pipeline, sandbox, rollout runners, registry gates, dispatcher and task resumption REUSED; the counter really increments and the read-back verifies; the lamp box really toggles through the same code; the no-shortcut guard; the failure matrix | `PROVEN_AUTOMATED` | `test_genesis_end_to_end.py` (the counter box: the whole run through `GenesisService` with the fixture live, real subprocess dispatch, the counter really incremented and the read-back verifying it; a second identical request resolves without a new run), `test_genesis_generalisation.py` (the lamp box really toggles through the same code path, with no mention of the counter box anywhere in the file), `test_genesis_no_shortcut_guard.py` (the sources of `app/genesis/*` carry none of the fixtures' names, paths, ports or operation ids; removing `/spec` fails a FRESH run at `researching`, proving nothing was cached), `test_genesis_failure_matrix.py` (the app down, an off-schema response, an unauthorized mutation parking with its cancel leaving no registration, approval by the model's own argument refused, a run unable to trigger another, the ten-minute and rate bounds). INDEPENDENTLY VERIFIED: a test-engineer given only the central claim wrote a third fixture the sources had never seen (a kasa with different paths, field names, types, a zero-input mutation and a non-idempotent one), drove the unmodified service to `verified` and matched an independent HTTP read of that fixture's own state - verdict, the pipeline is generic |
| 22.4 | The owner's voice: the corpus category `genesis` through the ONE router; receipts say exactly one of the six truths; approval only by a session+turn-bound Confirmation; "sil" reaches no tool; a non-local target refused before research; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category `genesis` 109 cases against the REAL fixture applications (a fresh harness per case, each fixture served on a free port for that case), the whole corpus 1141/1141 HEALTHY across 15 categories; "Sayac kutusunu sil." and "Test lambasini sil." reach no tool, a target outside the catalogue is refused before any research, "Onayliyorum." with nothing parked falls through unchanged and produces no side effect; approvals and cancellations run as two turns with the mutating request parked at turn 1; forbidden side effects 0, measured by reading each fixture application after every case |
| 22.5 | Response / TTS: the six truths with numbers read back; the loopback proxy | `PROVEN_AUTOMATED` (speech structure: the six truths, the numbers read back from the application) / NOT_YET_PROVEN for the loopback - OpenAI has no credits (owner item 29): `docs/evidence/tts-loopback-2026-09-08-122520.json`, 27 balanced cases including two genesis ones, every synthesis HTTP 429, summary PROVIDER_UNAVAILABLE |
| 22.6 | The Living Core: `capability.genesis` (UI contract v9, additive) from published metadata only; the Cockpit "Yeni Yetenek" panel with "Onayla" only in `awaiting_approval` and an honest empty state | `PROVEN_AUTOMATED` | web suite 1239/1239 (+70: `genesis-states.test.ts` 35, `genesis-panel.test.tsx` 27, `genesis-client.test.ts` 8), `tsc` clean, `oxlint` clean, `next build` green; an independent review of the branch found no blocker: "kullanilabilir" is reachable only from `available` and "dogrulandi" only from `verified` (no rounding up from a 2xx), "Onayla" only in `awaiting_approval` and "Vazgec" only while active, one call per press under repeated presses, contract v9 append-only with no earlier assertion weakened | **Corrected 2026-09-08 (during M25, ADR-0088 addendum 1 item 9)**: this build's `GENESIS_RUN_STATES` had no `cancelled`, while a run reaches that state whenever the owner says "Vazgeç" and `_transition` publishes it — so `genesisPosture` fell through to `building` and the Core drew a cancelled run as one still being made, in released code. A web test asserted `isGenesisRunState("cancelled")` is false, which is why nothing caught it. Fixed with the word, its Turkish, its place among the settled states and no controls on a cancelled row; `services/api/tests/unit/test_uistate_contract_halves.py` now holds every family's two halves to each other in both directions and was proven to fail by reintroducing this exact drift.
| 22.7 | Security review closed; the description handled as untrusted input; the adapter bounded to its one loopback base URL; no authority boundary of M7/M18.4 loosened | `PROVEN_REAL` (review) | independent review of d051866..5f058a5 (`.claude/agent-memory/security-reviewer/m24-capability-genesis-review.md`), alongside an independent VERIFICATION pass against the milestone's own central claim. **1 HIGH, verified live with a working PoC** - the mutating-approval gate resolved the owner's authorization by the FETCHED DOCUMENT's own self-reported `name` and treated `verify(...) is not None` as authorization without ever calling `AssetAuthorization.covers()`, so an asset enrolled with ZERO grants authorised a brand-new external mutation, and a document could name itself after an asset the owner had enrolled for something else. **2 MEDIUM** - `approve()` had no compare-and-swap, so two racing approvals could both pass and both dispatch (the M21 lesson); and the loopback allowlist accepted this system's own ports and every privileged port while `SandboxPolicy` carries no network policy at all, so the track's single-hardcoded-URL argument confined an adapter to ONE origin without deciding WHICH. **1 LOW** - `{id}` was accepted in an operation path and never substituted, failing at run time instead of at parse. All closed on main in 6bac5e0 (ADR-0087 addendum 1) with nine regression facts: the asset reference is the name the OWNER registered, a description that renames itself is refused outright, authorization is decided by `covers()` against the one narrow network grant the adapter's manifest declares, `approve()` claims the run with a conditional UPDATE before any provider work, ports below 1024 and every port this system's own services use are refused by name, and `{id}` is refused at parse. Verified sound by the review: the description schema carries NO free-text field anywhere, which closes the M23 CRITICAL's whole class by construction rather than by escaping; `_parse_base_url` refused every bypass tried against it live (`127.0.0.2`, `0.0.0.0`, `[::1]`, `127.1`, a decimal IP, userinfo, `localhost.evil.test`, trailing dots, path/query/fragment, `..`); the fetcher's 5 s timeout, its 64 KiB cap before parsing and its redirect refusal; every spliced value `repr()`-ed or `json.dumps()`-ed from an already-token-validated field; registration only after review, shadow and canary with no path around it; approve and cancel resolving the run from durable session-bound state and never from the model's argument; the four REST routes owner-gated and listed in `test_identity_enforcement.py`; the migration expand-only; ledger rows bounded |
| 22.8 | The Cloud Core half on production through the canonical blue/green path, with a genesis run executed on production against a fixture application on the host | `PROVEN_REAL` (the release and the surface) / the genesis run itself `PROVEN_AUTOMATED` | the FIRST attempt failed and rolled back exactly as it should have (run `m18-4-2026-09-08-123921`, FAIL 4/16, evidence committed): api-green never answered health because the container died before uvicorn could load the app with `PermissionError: '/skills'`. Production was untouched throughout - api-blue kept serving M23 with 492 probes, 0 dropped, gap 0 s, presence gap 0 s - and last-known-good was kept. Cause and fix in d6bb9d5 (see BUGS). The SECOND attempt: harness run `m18-4-2026-09-08-130903` (evidence `docs/evidence/m18-4-qualification-2026-09-08-130903.json`): PASS, 16 checks, 0 failed - the blue/green release of d6bb9d5 took 155.6 s with no gap (probes=334 dropped=0 gap=0 s max_latency=3492 ms, one probe over 3 s during the image build), the device sessions handed to api-green before it took HTTP (1/1 after 1 s), device presence gap 2.22 s (limit 15 s), alarms 1->1 and routines 1->1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanning production signals and pause/resume proven, final release == HEAD d6bb9d5 with contract v12 served and LAST_KNOWN_GOOD exported = f7baf19; runtime read-back: read back on production 2026-09-08 13:11-13:14Z: `/v1/system/health` release=d6bb9d5 last_known_good=f7baf19 started 13:10:33Z; `/v1/ui/state/contract` contract_version=9 with 52 tokens, `capability.genesis` present and the `genesis` subsystem listed (every M20-M23 token still there); `GET /v1/genesis/runs` -> 200 `{"runs":[]}` with an owner session and 401 without one; ledger row `deployment.cloud_core.released` result=d6bb9d5 at 13:11:52Z. **Not done, and why**: the spec's own row asks for a genesis run executed on production against a fixture application on the host. It cannot be: the interface catalogue ships EMPTY in production with no registration surface (that is deliberate - ADR-0087 keeps the catalogue a seam until an owner-facing registration milestone), and a run can only start from a target the catalogue resolves. So the production proof is the surface - the contract, the routes, their gating and the ledger - and the end-to-end genesis run itself is PROVEN_AUTOMATED against the real fixture applications (and independently re-proven against a third fixture the sources had never seen), not PROVEN_REAL on production. The milestone that opens catalogue registration is where that row becomes reachable |

## Stage 23 — M25 Unity / Blender / 3D creation (2026-09-08, ADR-0088)

Spec: `docs/M25_CREATIVE_3D_SPEC.md`. The tools were measured before anything was designed (`docs/evidence/m25-tool-detection-2026-09-08.json`): Blender 4.5.4 LTS drives headless here, Unity 6000.5.0f1 refuses batch mode without an editor licence. A scene change is claimed only after the tool's own inspection reads it back and the render is checked by an independent reader; the owner's real Unity project `E:\hologram\HologramVehicleTest` was never opened, read or written by anything in this milestone.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 23.1 | The scene plan is data with a closed operation vocabulary and bounded values; no Python or C# is ever generated from owner or model words; the drivers are fixed repository files with pinned hashes, checked before they are shipped | `PROVEN_AUTOMATED` | `test_scene_spec.py` on the merged main: the closed operation vocabulary (nine tags, anything else refused at parse), the closed name alphabet (a path-shaped project or scene name refused before any device call), the bounded numbers, the fixed Unity script catalogue, and a canonical `plan_json()`; `test_blender_driver.py`'s `test_manifest_pins_match_the_files_on_disk` holds both drivers to their pinned sha256, and `driver_text()` verifies that pin at RUN time before the bytes are ever shipped to the device — a fact tampers with the file and watches the refusal (`test_a_driver_whose_bytes_do_not_match_its_pin_is_never_shipped`). No Python or C# is generated from owner or model words anywhere in the family: the drivers are fixed repository files and the plan is data |
| 23.2 | Blender: a real headless run builds the scene from the plan, saves it, renders, and writes an inspection the Cloud Core reads back — through the Cloud Core's OWN command, not a lab's | `PROVEN_REAL` (this machine) | TWO real labs, both against `C:\Program Files\Blender Foundation\Blender 4.5\blender.exe`. `scripts/tests/blender-scene-lab.py` (`docs/evidence/m25-blender-lab-2026-09-08.json`) drives the pinned driver directly: create -> sphere/cube/camera/sun -> transform -> material -> render 320x240 -> inspect, every fact read from the driver's own `out.json` and an independent read of the PNG. `scripts/tests/blender-service-command-lab.py` (`docs/evidence/m25-service-command-lab-2026-09-08-170441.json`) drives the CLOUD CORE'S OWN command — the string `run_command()` builds, a project laid out the way `project.scaffold` would, the read-back compared through the real `compare()` — in both forms: the creation run (`--factory-startup -b`, no scene to open yet) and the apply run (`--factory-startup -b scene.blend`), each exit 0 and ok. That second lab exists because the M25 security review found the service's path to the device had never been exercised; its FIRST run failed twice and found two more real defects (`docs/evidence/m25-service-command-lab-2026-09-08-150221.json` is that failure, kept) |
| 23.3 | Unity: the provider, the fixed driver and the lab exist and run the real editor; today it answers the licence refusal in the licensing client's own words rather than pretending | `PROVEN_PROXY` (owner item 32) | `docs/evidence/m25-tool-detection-2026-09-08.json`: Unity 6000.5.0f1 (Hub 3.18.3) is installed and `-batchmode -nographics -quit -createProject` exits 198 in 5 s with the licensing client's own line, "No valid Unity Editor license found. Please activate your license." (re-measured 13:24Z, same). The provider, the fixed `SceneDriver.cs`, the device runtime, the argv shape and the lab all exist and run the real editor; what they answer today is `dependency_unavailable` carrying that sentence verbatim — the device reads it from Unity's own `-logFile`, and a fact asserts the receipt says "lisans yok" and never that a scene was made. PROVEN_REAL the moment owner item 32 (sign in to Unity Hub) is done; nothing in the code path changes |
| 23.4 | The closed loop: plan → create → render → inspect → compare → correct, with the comparison naming the object and the axis, never reporting a match over an empty comparison, and never calling an empty comparison a disagreement either | `PROVEN_AUTOMATED` | `test_scene_compare.py` on the merged main: every constraint kind against the inspection rather than the plan, the tolerances, the render read independently by `PIL` (decodable, non-uniform, and within the spec's 1920x1080 bound), and the rule this module was built for — **a match is never claimed over an empty comparison** (`checked == 0` answers `no_constraints`, the M24 defect refused by construction). The camera-aim fold is checked in BOTH directions after the security review found it one-sided: an aim supersedes a rotation stated before it, and a rotation stated after an aim is still the plan's last word. The receipt is where the loop lands, and it now has three outcomes rather than two: the read-back agrees (verified), it disagrees (`mismatch`, unverified, the object and field named, a `scene.mismatch` ledger row), or there was nothing to check (applied, unverified, said plainly). Each has a fact, in the receipt AND in what the Core is told; the third exists because the first fix made an empty scene report a disagreement, and the route test driving "Blender'da yeni sahne aç." through the real application object caught it |
| 23.5 | The device: the two runtimes allowlisted structurally under a 3D root, batch runs bounded in a Job Object, `scene.inspect` returning the bounded inspection and a hash-checked render | `PROVEN_REAL` (this machine + the runner) / `PROVEN_PROXY` (the deployed agent until item 28) | `tests/PagentOS.Agent.Tests/Scenes/` 51 facts, whole agent project 758/758: both argv shapes admitted under the 3D root and stored as argument lists (never a command line, never a shell), 19 hostile variants refused before any process with a counting seam in place of `Process.Start` proving the count stays 0 — a path outside the project, a driver that is not Python, a `.blend` that is not one, `-P` for `--python`, a missing separator, an extra token, another `-executeMethod`, the owner's own Unity project as `-projectPath`, a shell composition, and every misplacement of `--factory-startup` (absent, after the scene, last, and six near-miss spellings in either position); the REAL `blender.exe` building a scene inside the Job Object and `scene.inspect` returning the read-back and the PNG with a matching sha256; the flipped byte and the escaped render path refused; a marker edited into a hostile command refused at RUN time; the real Unity batch attempt answering its licence refusal. PROVEN_PROXY on the deployed agent only because the agent update is owner item 28 |
| 23.6 | The owner's voice: the corpus category `creative3d` through the ONE router; "Sahneyi sil" reaches no tool; the owner's real project is refused; an operation outside the vocabulary asks; every earlier category unchanged | `PROVEN_AUTOMATED` | `test_owner_utterance_corpus.py`: category `creative3d` through the ONE router at the canonical transcription boundary, the whole corpus HEALTHY across 16 categories with forbidden side effects 0, measured by inspecting the fixture project after every case. The directive's own utterances and their paraphrase and ASR-noise variants (no punctuation, lower case, no diacritics); "Sahneyi sil." and "Projeyi sil." reach no tool; the owner's real project is refused at the spec by the slug rule before any device call; an operation outside the closed vocabulary is refused by the plan rather than guessed at; the tool word comes from the utterance, else the `scene` focus, else a clarification. Every earlier category unchanged |
| 23.7 | Response / TTS: every receipt reads the inspection back; the loopback proxy | `PROVEN_AUTOMATED` (the receipt structure: every sentence built from the INSPECTION, never the plan — the object named, the count read back, and the three outcomes each with their own words) / `NOT_YET_PROVEN` for the loopback — OpenAI has no credits (owner item 29): `docs/evidence/tts-loopback-2026-09-08-144253.json`, 29 balanced cases including the 3D ones, every synthesis HTTP 429, summary PROVIDER_UNAVAILABLE |
| 23.8 | The Living Core: `scene.activity` (UI contract v10, additive) from published metadata only; the Cockpit "3B Sahne" panel with the render under the owner session and an honest unavailable row | `PROVEN_AUTOMATED` | web suite 1318/1318 across 72 files, `tsc` clean, `oxlint` clean, `next build` green: contract v10 `scene.activity` (append-only; the nine step words spelled once, a 120 s horizon because a headless render outlasts every earlier activity), `scenes.ts` turning facts into a posture and a caption with "doğrulandı" reachable only from `verified`, a distinct `mismatch` posture that names the object — which the backend now actually emits — and `unavailable` as a settled honest posture rather than an error; the Cockpit "3B Sahne" panel fetching the render under the owner session (a gated route answers a bare `<img>` with 401), controls absent rather than disabled on an unavailable row, and the licensing client's own sentence shown as the reason. The row can actually settle now, and it can actually be driven: the Cloud Core sent the DATABASE ROW's word on every surface this build reads — the bus, the `/v1/scenes` row and the receipt — while the build reads the loop's step words, so five of the seven tokens were unreadable (every successful run and every settled one). The Core drew each finished scene as one still being made, the panel showed NO controls on any row because `rowState()` was null, and `scene.status` read the English token out loud in a Turkish sentence. One vocabulary of nine steps now, spelled once per side, derived for a row by `wire_step()` from the comparison the row kept (and never `verified` over no comparison at all), with `test_scene_activity_vocabulary.py` reading the TypeScript file to hold the two together in both directions and every database state to a word the panel knows. With that guard written, the same question asked of the earlier families found the same defect in RELEASED M24 code — a genesis run the owner cancelled had no word the web could read, so the Core drew it as one still being built; fixed, with Stage 22 row 22.6 carrying the correction and `test_uistate_contract_halves.py` now covering every family, proven to fail by reintroducing that drift and watching it go red |
| 23.9 | Security review closed; no owner project reachable; no code generated from words; the runtimes bounded | `PROVEN_REAL` (review) | independent review of 7c90090..bf791d5 (`.claude/agent-memory/security-reviewer/m25-creative-3d-review.md`). **1 HIGH/CRITICAL, verified live with a PoC against the real service** — `_finish` set `TERMINAL_VERIFIED` and spoke a success sentence whatever the comparison found, so a plan moving an object that had never been created answered "Kup taşındı" while the tool's own inspection proved it absent; the web contract had a `mismatch` posture the backend had never emitted. **1 HIGH** — the manifest carried the bare word `blender` rather than the argv the device matches token for token, and the scaffold never asked for the 3D root, so `project.scaffold` would have refused at the first device call: the entire write path was non-functional against the real device, and nothing caught it because the fake device echoed success without reading the command and the real Blender proof bypassed the service. **1 MEDIUM/HIGH** — the driver pins were asserted only by a test, never checked before the bytes were shipped. **1 MEDIUM** — the camera-aim fold dropped a rotation stated AFTER the aim. **2 LOW** — the Cloud Core's wait was shorter than the device's own bound, and the render's pixel size was never measured. All closed in 007bb01 (ADR-0088 addendum 1). Closing the HIGH meant writing `scripts/tests/blender-service-command-lab.py`, which drives the Cloud Core's own command against the real editor — and its first run found three more real defects: the driver saved the plan's scene word instead of `scene.blend` so the second run had nothing to open; opening a saved `.blend` loaded the OWNER'S installed Blender add-ons and one of them hung the editor with a watchdog thread that never stops (third-party code inside a bounded job, now closed by making `--factory-startup` the first token of every Blender command); and a near-uniform render passed because the lab's own plan never aimed the camera. A ninth defect was mine: the first version of the receipt fix made an empty scene report a disagreement. Verified sound by the review: `ScenePlan`'s closed vocabulary, alphabets and bounds; `SceneInspection`'s bounds-before-read, sha256 verification and resolve-then-contain confinement; `ProjectManifest`'s token-exact argv matching; tool detection by install location, never a PATH search; the Job Object bounds and environment scrub; Unity's honest `dependency_unavailable`; the owner-gated REST routes; the web rendering text and typed tokens only; the migration additive; and the corpus's delete-verb negatives structurally unreachable rather than merely asserted |
| 23.10 | The Cloud Core half on production through the canonical blue/green path | `PROVEN_REAL` (the release and the surface) | released to production 2026-09-08 17:24Z. The Cloud Core now serves 20db267 as api-green behind the edge: the build, the expand-only migration, api-green healthy, **device handoff 1/1 after 1 s**, the edge switched to green, health through the edge settled after 0 retries, api-blue drained 60 s and stopped. The harness's own re-measurement afterwards (`docs/evidence/m18-4-qualification-2026-09-08-172747.json`, 16 checks): blue/green with **no gap — probes=24 dropped=0 gap=0 s max_latency=193 ms, slow(>3 s)=0**, device presence gap 0 s over 9 polls, alarms 1->1 and routines 1->1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanning production signals with pause/resume proven, final release == HEAD. **RUNTIME READ-BACK** (2026-09-08 17:26-17:28Z, through the edge with the owner credential): `/v1/system/health` release=20db267 started 17:24:42Z contracts action=12 **ui_state=10**; `/v1/ui/state/contract` contract_version=10 with 53 tokens, `scene.activity` present and every M20-M24 token still there; `GET /v1/scenes` -> 401 without a session and 200 `{"scenes":[]}` with one; ledger row `deployment.cloud_core.released` result=20db267. The connected Windows agent advertises 29 capabilities and **not** `scene.inspect`, so the 3D family is not reachable from production until the agent update (owner item 28) — stated here rather than implied by the release. **HOW THIS RELEASE WENT, PLAINLY**: the first harness invocation was killed by my own mistake — I started a second command that opened the harness's log file, and the harness died two seconds into its release phase. It had already uploaded the tree and started the idle colour, so the completing run was `release-cloud-core.ps1 -BlueGreen -Force` in the foreground, and the harness then re-ran for its checks. Production was untouched throughout the aborted attempts (measured: the edge kept serving d6bb9d5 at contract v9 until the completing run). One harness check FAILS as a result and is reported as failing: "the device sessions were handed to the new colour BEFORE it took HTTP" looks for a handoff line in the release output, and a verify-only re-run of an already-released HEAD has none — the property itself was measured by the run that performed the release ("device handoff: 1/1 after 1s"). The split also left `LAST_KNOWN_GOOD` equal to `RELEASE`, which would have made a rollback redeploy the same sha; corrected on the host to d6bb9d5 in both the marker file and the environment, and the running container still reports the value it was started with until its next start |

## Stage 24 — M26 Executive Autonomy, with the additive tracks the owner asked for (2026-09-09, ADR-0089 / ADR-0091 / ADR-0092)

Specs: `docs/M26_EXECUTIVE_AUTONOMY_SPEC.md`, `docs/M26_LATEST_NEWS_MODE_SPEC.md`. A multi-step job is a validated `TaskGraph` whose every step maps to ONE existing service call; execution is a durable Temporal workflow the owner can ask about, pause, correct and stop at any moment; a run ends completed, partly done with what is missing named, or stopped — and no step can take an external high-risk action, because no such step kind exists. On top of it, the additive tracks: weather that never guesses where the owner is, a briefing assembled only from real sources, and a news mode that refuses to guess a channel.

Merged main `e6f08ff`; CI run 34301544141 green (all 7 jobs) on the released tree `e6f08ff`; the code-bearing run `34296886021` green before it. Suites: `services/api` unit suite **7312 passed / 2 skipped** (17m; `test_health_endpoint.py` excluded - it makes real db/redis/object-store/temporal connects and hangs in this sandbox, an environmental fault recorded in agent memory since 2026-09-02, not a regression); `services/browser` **405 passed**; `apps/web` **74 files / 1397 tests**, `tsc --noEmit` clean, `oxlint` 0 errors; `ruff check .` clean in both Python services.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 24.1 | The task graph is data with a closed step vocabulary, a real DAG, bounds, and no step kind that can send, pay, delete, publish or change a setting | `PROVEN_AUTOMATED` + `PROVEN_REAL` (read from the deployed code) | `app/executive/plan.py` + `graph.py`: a closed step vocabulary, a real DAG (a reference to the same or a later step is refused, including through `amend`, which re-validates the whole graph), the bounds (<=24 steps, <=900 s, retry <=3 attempts / <=60 s backoff), and `high_risk` DEFINED and mapped to zero kinds - `validate_graph` asserts its absence positively rather than relying on omission. A planner cannot set its own `risk_class`, `compensation` or evidence: they come from the kind's fixed profile. Verified against the DEPLOYED code, not just the source: 15 step kinds, `high_risk_kinds` **[]**, bound 2. `test_executive_graph.py` 17 + `test_executive_spec.py` 12 |
| 24.2 | The planner turns the directive's own shapes into graphs from the ONE router's slots; a model-proposed graph goes through the same validation, never around it | `PROVEN_AUTOMATED` | `app/executive/planner.py`: the directive's three shapes built from the ONE router's own slots, never a second parser. `ClaudeExecutivePlanner` sits behind the same Protocol and its output goes through the SAME `validate_graph`, so a model can propose data and never execute; the planner reads only the owner's own directive text, so fetched web, mail and document content can never decide which steps exist. `test_executive_planner.py` 14 |
| 24.3 | Execution is durable: topological order with bounded fan-out, idempotent activities, no state in the workflow, and a Cloud Core restart mid-run resumes without repeating a verified step | `PROVEN_AUTOMATED` | `app/executive/workflow.py` in Temporal's time-skipping environment: topological order, fan-out bounded at 3, one idempotent activity per step keyed by (run, step, attempt), the workflow holding no state, and a worker restart resuming from history. `test_executive_workflow.py` 11 + `test_executive_activities.py` 17. Honest narrowing: reattachment to a workflow still IN FLIGHT is not proven (ADR-0089 addendum 2 point 3) - the restart test awaits completion first, so what is proven is idempotent replay at the activity layer plus SDK result-by-id |
| 24.4 | The owner's levers: pause, resume, cancel, retry a step, amend a run, and ask status or an explanation — each proven, and cancel bounded with compensation from a closed vocabulary that deletes nothing of the owner's | `PROVEN_AUTOMATED` | pause (finishes the running batch, starts none), resume, cancel (bounded, compensations from a closed vocabulary that deletes nothing of the owner's), retry_step, amend, plus `status`/`explain` queries - each with its own test, and `explain` answering from the row rather than from what the workflow remembers. `test_executive_service.py` 24 + `test_executive_routes.py` 16 |
| 24.5 | A step is `verified` only when its postcondition evidence exists in a row the activity read back; a run that could not finish ends `partial` naming every unverified step and why, and the synthesis still runs over what exists | `PROVEN_AUTOMATED` + `PROVEN_REAL` | A step is `verified` only when its postcondition evidence exists in a row the activity READ BACK; `_evidence_meets_minimum` is never satisfied by an absent or empty comparison. A run that could not finish ends `partial`, naming every unverified step and why - which is now true of a run whose steps merely STOPPED, not only one whose steps reached a terminal state (see 24.6: production told the owner it was working when it had stopped) |
| 24.6 | The failure matrix: a document unavailable, a browser fetch failure, an artifact generator failure, a step timeout, a Cloud Core restart — each ending honestly | `PROVEN_AUTOMATED` + `PROVEN_REAL` (three production runs) | The five-case matrix, proven automated AND then observed on production three times before the fourth run succeeded: a document unavailable -> dependants `skipped` with the reason; a research failure -> retried per the step's own policy then `failed_recoverable`; a generator failure -> `failed` (production: "no upstream content to build an artifact from"); a timeout; a Cloud Core restart. Plus three the security review and the runtime verification added, each with a regression test watched to fail first: an activity dying for a reason nobody classified is settled through the same `_finalize` (it CAUGHT a real Temporal-layer death on production, first encounter); a run whose every step has stopped is `partial` and NAMES what is missing, instead of saying "Çalışıyorum efendim: 3/4 adım tamam" for an hour; and a long step heartbeats while it runs, instead of being killed at 90 s by the liveness bound its own 900 s timeout was never allowed to reach. `test_executive_review_findings.py` 17 |
| 24.7 | Authority: no external high-risk action exists as a step; the M21 confirmation gate is untouched; the owner's session owns the run and the model cannot start one by argument; the bounds hold | `PROVEN_AUTOMATED` + `PROVEN_REAL` | No path reaches a send, payment, delete, publish or settings change - confirmed by an independent review and re-read from the DEPLOYED code (15 kinds, zero high-risk): the mail and calendar handlers call only `draft_reply`/`propose`/`discard`, and `app/executive` never references `MailService.send` or `CalendarService.commit` at all. The M21 confirmation gate is untouched. Every REST route is owner-gated and listed in `test_identity_enforcement.py`. The <=2 active-run bound is a real bound rather than a likely one: the count-then-insert race the review proved live with two threads is closed and regression-tested against a FILE database, after the first attempt at that test turned out to be vacuous on in-memory SQLite |
| 24.8 | The owner's voice, in conversation: the corpus category `executive` with multi-turn sequences through the ONE router; every earlier category unchanged; forbidden side effects 0 | `PROVEN_AUTOMATED` | The corpus category `executive`: 120 multi-turn cases through the ONE router at the canonical transcription boundary, with `preceding_turns` so "Devam et" is tested after "Bu isi durdur" after a start. Negatives included: "Bu maili gonder" inside a run is refused - no such step kind exists. Whole corpus **1636 cases / 19 categories**, every earlier category unchanged, forbidden side effects 0 |
| 24.9 | Response / TTS: every receipt reads the run back; the loopback proxy | `PROVEN_PROXY` | `PROVEN_PROXY` - every receipt is composed from the run READ BACK (production's own: "Tamamlandı efendim. document hazır (artifact dd283204-...)", built from the row, not from what the model remembered saying), and the loopback proxy renders them; real narration audio stays gated on the owner's OpenAI credits (owner item 29) |
| 24.10 | The Living Core: `executive.run` (UI contract v11, additive) from published metadata only; the Cockpit "Görevler" panel with the owner's three controls and an honest partial state | `PROVEN_AUTOMATED` | UI contract v11 `executive.run` (additive, append-only), published from row metadata only, and the Cockpit "Gorevler" panel with the owner's three controls and an honest partial state. Production serves `ui_state` 11 (it served 10 before this release). Both halves are held to each other by tests that read the OTHER side's source (`test_uistate_contract_halves.py` 10, `test_executive_row_shape.py` 4), both confirmed to BITE by breaking them. 74 web test files / 1397 tests |
| 24.11 | Security review closed (Executive Autonomy) | `PROVEN_REAL` (review) | Independent review before the gate (`.claude/agent-memory/security-reviewer/m26-executive-autonomy-review.md`, ADR-0089 addendum 2). One HIGH and one MEDIUM, both proven LIVE against the real objects, both fixed with regression tests watched to fail first; one LOW that narrowed an earlier claim of mine rather than being fixed; and one assertion of mine the review refuted outright. A third defect was found in the HIGH's own first fix - an unbounded retry loop that re-dispatched a failed step for ever - and is recorded with the rest rather than quietly corrected |
| 24.12 | The Cloud Core half on production through the canonical blue/green path, with ONE real executive run executed on production | `PROVEN_REAL` | Released to the Hetzner Cloud Core through the canonical blue/green path on 2026-09-09: `e6f08ff` behind the edge, previous `52d5ddb` kept as last known good, `ui_state` contract 11 served (it was 10 before), the idle colour drained 60 s and stopped. **ONE real executive run on production, shape (a), COMPLETED**: run `ec20975f-a3ee-4bf5-9636-c2696d8fa0c7`, 4/4 steps `verified` - `research.run` through the owner's own device browser worker (research task `affac381`, 2 sources, 2 findings, attempt 2), `research.synthesize` (whose text names its own limit: "Kısa araştırma bütçesinde yalnızca 2 kaynak doğrulanabildi; bulgular sınırlı"), `artifacts.create` (document artifact `dd283204-9208-4463-bb63-458ede89e090`, read back through `/v1/artifacts/{id}` as state READY), and `synthesis`. The owner-facing receipt, read back from the row: "Tamamlandı efendim. document hazır (artifact dd283204-9208-4463-bb63-458ede89e090)". Evidence: `docs/evidence/m26-runtime-2026-09-09.json` |
| 24.13 | Owner Location Context: one resolution order, provenance kept, nothing invented, no continuous coordinate archive | `PROVEN_AUTOMATED` + `PROVEN_REAL` | `app/location/*` (migration `0034_location_weather_briefing`): `LocationContext` is a durable, provenance-keeping row and `LocationService.resolve` is the ONE order the owner specified - explicit request, fresh trusted device fix, owner default, recent-but-aged trusted, coarse IP at LOW confidence with its own spoken caveat, then honestly unresolved. A review proved live across five tier combinations that IP-coarse never overrides a trusted tier. Windows Location is a real seam with an honest writer that always says "no location", because the deployed agent advertises 29 capabilities and none is location - MEASURED. **On production**: `resolved: false`, `reason: unresolved`, no default row - it says it does not know where the owner is rather than guessing. `DEFAULT_WEATHER_LOCATION` stays UNSET (owner item 35). `test_location_service.py` 11 + `test_weather_location_review_findings.py` 13 |
| 24.14 | Live weather from a real provider, never from model memory, always for a resolved location and always able to say which | `PROVEN_REAL` | `app/weather/*`: Open-Meteo is the real default provider - no signup and no key for non-commercial use, the same public-keyless class this repo's research provider already trusts, so shipping it live is not inventing a credential. **PROVEN_REAL on production**: one live call from the deployed Cloud Core answered `executed`/`verified`, provider `open_meteo`, observed_at 2026-09-09T04:15Z, 20.5 C, `location_source` `explicit_owner_request`, speech "İstanbul: genelde açık, 20°C. Bugün en yüksek 26°C, en düşük 20°C. Yağış olasılığı yüzde 0.", with a durable `weather_query_evidence` row so the owner can ask afterwards which location it answered for. `test_weather_providers.py` 12 + `test_weather_service.py` 8 |
| 24.15 | The morning briefing: every dynamic statement from its real provider or an honest absence; the overnight summary never claims work that did not complete | `PROVEN_REAL` | `app/briefing/*`: greeting (time-of-day correct - production said "İyi geceler" at 04:16 local, not a false "Günaydın"), Turkish date and time, live weather, release/version and device presence from the LIVE registries, and an overnight summary from real `ActivityEventRow` evidence. **PROVEN_REAL on production**: it read back the deployed SHA `e6f08ff...`, real uptime, and **13 real overnight autonomous-development events - 13 completed, 0 failed** - counted from the ledger, never claimed. Each section is gated by its own durable preference; `auto_open_news_video` defaults False. Nothing is hardcoded: an unavailable source produces an honest sentence ("Haber özeti şu an bağlı değil efendim"), not a fabricated one. `test_briefing_service.py` 11 + `test_weather_briefing_wiring.py` 5 through the REAL application object |
| 24.16 | Latest News Mode: a channel identity it refuses to guess, "latest" from real timestamps, governed playback in its own context, summary and playback kept separate, nothing bypassed | `PROVEN_AUTOMATED` | `app/news/*` (migration `0035_news_mode`, ADR-0092): durable owner-configurable sources; "latest" decided from real `published_at`, never title similarity or search rank; the three content policies honoured - and, after the review's HIGH, a policy that excludes Shorts REFUSES when it has no evidence to decide rather than selecting one; NEWS SUMMARY and OPEN LATEST VIDEO kept separate operations; managed playback on the browser worker's own THIRD persistent Chrome profile, isolated from both the research and the alarm profiles by directory containment and now enforced media-only in both directions, so news media can never touch the owner's browsing or replace a wake song. No CAPTCHA, consent wall, ad or DRM is ever bypassed - each is a truthful recorded failure. **On production**: 0 sources configured, so "Show Ana Haber" is bound to nothing (owner item 36). Corpus category `news` 64 cases; 164 unit tests across nine files + `test_news_review_findings.py` 19 |
| 24.17 | Security review closed (Location/Weather/Briefing and Latest News Mode) | `PROVEN_REAL` (reviews) | Two further independent reviews before the gate, one per additive track (ADR-0091 addendum 1, ADR-0092 addendum 1; both indexed in the security-reviewer's memory). Ten findings between them - one HIGH each - every one closed with a regression test watched to FAIL first. The location HIGH: an unguarded evidence commit whose failure travels out of the function and takes the whole tool-call turn with it, reachable on every successful answer through an unbounded geocoded place name against a fixed-width column. The news HIGH: the Shorts exclusion was structurally inert on the only provider that ships, so an untagged Short could be played as "the main news" - while all 288 resolver tests passed, because every fixture supplied evidence the real provider cannot produce |

## Stage 25 — M27 Creative Tools Operator (2026-09-09, ADR-0093 / ADR-0094)

Spec: `docs/M27_CREATIVE_TOOLS_SPEC.md`. Report: `docs/reports/m27-creative-tools-report.md`. A creative edit is planned as data, executed through the most structured interface the installed application really offers, exported, reopened by an INDEPENDENT reader and compared with what was asked — and an application that is not installed or not licensed is named, never imitated.

The milestone turns on one decision: **Paint has no scripting surface, so its document model IS the interface.** The edit is performed on the bitmap with Pillow — deterministic, inspectable, testable — and Paint is opened on the result through M19 so the owner sees it where they asked for it. That is the browser rule (`API > DOM > accessibility > UI Automation > vision > coordinates`) applied to a creative application: the file is the API. Pointer drawing is never used and no script is ever generated from owner or model text.

Merged main `65ab820`; CI run `34321233803` green on it. Suites, run as CI runs them (random ordering, no `-p no:randomly`): `services/api` unit suite **7570 passed / 3 skipped** (20m; `tests/integration/*` needs real Postgres/Redis/MinIO/Temporal and is CI's own separate job, not run in this sandbox); `services/browser` **412 passed**; `apps/web` **79 files / 1509 tests**, `tsc --noEmit` clean, `oxlint` 0 errors on every changed file; `ruff check` clean. Voice corpus **1719 cases across 20 categories** (`creative` 83) through the ONE router. Creative unit suites: spec 23, intents 20, review findings 17, compare 14, service 14, execute 11, providers 11, routes 7, activity vocabulary 7, tools 8 — **132**.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 25.1 | What is installed is MEASURED, and an application that is not there is named rather than imitated | `PROVEN_REAL` | `docs/evidence/m27-m28-tool-detection-2026-09-08.json`, measured before anything was designed and again against the finished code: `paint installed=True C:\Windows\System32\mspaint.exe`; `photoshop installed=False "registry key present but no Photoshop.exe found"`; `illustrator installed=False`; `figma installed=False "no owner Figma token configured"`. That second line is the criterion doing its job: the provider originally read the bare `HKLM\SOFTWARE\Adobe\Photoshop` key as proof, and Creative Cloud writes that key with no Photoshop behind it — on this machine it reported Photoshop "installed", the exact failure ADR-0093 decision 3 forbids. Caught and fixed before it shipped. `test_creative_providers.py` 11 |
| 25.2 | The plan is data with a closed operation vocabulary and bounds; the owner's original is never overwritten | `PROVEN_AUTOMATED` | `app/creative/spec.py`: `CreativePlan` over a closed operation vocabulary (≤ 64 operations), bounded colours, sizes and positions, sources confined to authorised roots, and every output a NEW file beside the original (`<name>-pagentos-<n>.<ext>`) — the original is never opened for writing. `MAX_DIMENSION` bounds the canvas and, after the security review, `_open_source` bounds the DECODED size too. `test_creative_spec.py` 23 |
| 25.3 | Execution goes through the most structured interface the installed provider offers — never pointer drawing, never a generated script | `PROVEN_AUTOMATED` + `PROVEN_REAL` | `app/creative/execute.py` performs each operation on the bitmap with Pillow. No pointer path exists to be reached; no `.jsx` or Figma driver is ever generated from owner or model text (the drivers are fixed files that read a JSON plan). `test_creative_execute.py` 11 |
| 25.4 | Inspection and comparison are done by an INDEPENDENT reader against what was asked, with objective defects named | `PROVEN_AUTOMATED` + `PROVEN_REAL` | `app/creative/compare.py`: dimensions, per-tile colour distance, and the presence of asked-for shapes and text by masks. The aggregate is **not called SSIM anywhere** — the spec's draft promised "SSIM via PIL+numpy", numpy is not a dependency of this repository, and naming a different measure by a known name is the class of overclaim three M26 reviews existed to catch (ADR-0093 decision 4). Four wire defects: `wrong_size`, `missing_region`, `color_drift`, `empty_output`. `test_creative_compare.py` 14 |
| 25.5 | The Paint round trip on this machine: a known canvas produced, saved, reopened by an independent reader, and verified | `PROVEN_REAL` | `scripts/tests/creative-paint-lab.py` against the real service with no fake in the path (`docs/evidence/m27-paint-lab-2026-09-09.json`): a real **3,703-byte PNG**, reopened independently at **320×240 PNG**, red inside the rectangle `(220,30,40,255)`, blue inside the ellipse `(40,70,210,255)`, white background, **969 ink pixels** where the text was planned, and the comparison agreeing on **4 checks** |
| 25.6 | A deliberate discrepancy is DETECTED and corrected in one round, not smoothed over | `PROVEN_REAL` | Same lab, same run: a deliberately wrong colour was **caught** — *"no ink within tolerance of (20, 200, 60, 255) near the planned position"*, wire defect `missing_region` — and the corrected round agreed again on 4 checks. The failure is a positive detection with a named defect, not an absence of evidence |
| 25.7 | Photoshop, Illustrator and Figma: the provider architecture ships, and says the truth when the application is absent | `PROVEN_PROXY` | Each provider ships with its detection, its capability list and its lab; with none installed or configured, each answers `dependency_unavailable` **with the detection facts**, and the router refuses with the installed alternative named ("Photoshop bu bilgisayarda kurulu değil; Paint'te açayım mı?"). No licence bypass and no authentication bypass exists to be reached. `PROVEN_PROXY` by construction and stated as such: the same lab runs the real application the day the owner installs it, with no code change |
| 25.8 | Voice: the corpus category `creative` through the ONE router, zero wrong-app routings, forbidden side effects 0 | `PROVEN_AUTOMATED` | Corpus category `creative` **83 cases** through the ONE router at the canonical transcription boundary; whole corpus **1719 cases / 20 categories**, every earlier category unchanged. `test_creative_intents.py` 20. It caught the fourth intent collision of this build: `CREATIVE_EXPORT` matched "dışa" + an export verb, which is every "… dışa aktar." sentence in Turkish — **including M25's own "Sahneyi dışa aktar."**, whose expected answer is *none*. M27 would have told the owner Paint was exporting a Blender scene. Fixed by NARROWING, not reordering — a reordering only moves the collision to whichever family loses the race — and found by another milestone's negative case, which is the argument for one shared corpus |
| 25.9 | The Living Core: UI contract v12 `creative.activity`, additive, published from real row state, with the Cockpit panel | `PROVEN_AUTOMATED` | Contract v12 `creative.activity` (additive, append-only), 12 wire words mapped from 7 row states by `wire_step()`, which RAISES on an unmapped state rather than inventing one. Both halves are held to each other by tests that read the OTHER side's source, and the guard's own blind spot was fixed on the way: `FAMILIES` was hand-maintained, so a family present on one side was invisible — it now discovers every run vocabulary in the TypeScript and demands a row for each, which surfaced **two more, M21's mail-draft and calendar-proposal vocabularies, never compared with anything since M21 shipped**. Seven families watched now, not four. `test_creative_activity_vocabulary.py` 7 |
| 25.10 | Security review closed | `PROVEN_REAL` (review) | One independent review before the gate, three findings, each closed with a regression watched to FAIL first. **HIGH**: `_apply_background_remove`'s threshold branch — the *only* method the shipped voice tools request — was an unbudgeted per-pixel Python loop; two ordinary turns (an 8192×8192 canvas, then "arka planını kaldır") reach roughly **eighty CPU-seconds** of synchronous work on the request thread, and `spec.py`'s own comment claimed a runaway request was refused "well before any pixel is ever touched" when `MAX_DIMENSION` bounded the canvas and never the cost of walking it. Vectorised with Pillow's own arithmetic: **0.094 s at 2048×2048 against ~5 s for the loop**, proven pixel-for-pixel unchanged including at the tolerance boundary. **MEDIUM**: the same shape in the comparison's region scan, and `_open_source` bounding encoded bytes but never decoded size. **LOW, and the one that mattered most**: `creative_redraw` named a source and never handed over its bytes, so "Bu resmi Paint'te yeniden çiz" — the sentence the capability is named for — always fell through to a blank canvas. `test_creative_review_findings.py` 17 |
| 25.11 | Regression review: nothing earlier weakened | `PROVEN_AUTOMATED` + `PROVEN_REAL` | Beyond the review, five defects found and fixed in other milestones' code while building this one, each with its own regression: M18.3's greeting-token test read a different clock from the one it set up and could only pass between 04:00 and 04:06 UTC on one day (it failed CI at 04:31Z); a `turkish_casefold` mismatch meant "Illustrator'da" never routed, the proper noun being spelled with an ordinary Latin `I` that casefolds to dotless `ı`; `creative.activity` was deferred while the halves were separate branches and so was never PUBLISHED, making every posture the panel can draw unreachable with no vocabulary guard noticing; and a `.panel li` layout defect on **already-released** M23/M24/M25/M26 panels rendered a refusal run into a vendor sentence as one garbled claim |
| 25.12 | The Cloud Core half on production through the canonical blue/green path | `PROVEN_REAL` (release) / `NOT_YET_PROVEN` (the Paint round trip on production) | Released to the Hetzner Cloud Core through the canonical blue/green path: `65ab820` behind the edge, the previous release kept as last known good, the idle colour drained and stopped. The **production Paint round trip stays `NOT_YET_PROVEN`, measured rather than assumed**: `DEVICE_PROTOCOL.md` §6 puts `desktop.open_application`'s allowlist at `notepad`/`calc`, and §6a puts `desktop.open_artifact`'s extension allowlist at `.pdf .docx .html .htm .txt .md` — no image format at all. Opening a PNG in `mspaint.exe` needs a device-side change carried by the elevated agent update (**owner item 28**). Spec §6 named this gate in its own words ("item 28 permitting"), so it is a stated boundary, not a discovered gap. The same is true of §4's device-side `creative.export_check`. **The device-side change is made (2026-09-09, ADR-0096):** `mspaint` is on BOTH launch allowlists — `desktop.open_application`'s (`AppLauncher.DefaultAllowlist`) and `app.launch`'s (`OperatorCapabilities.DefaultApplications`), the latter with notepad's argument policy, at most one absolute path that must RESOLVE inside the authorised roots. It reaches this machine with item 28, and `scripts/core/qualify-item28-unlocked.ps1` exercises the round trip the moment it does. `creative.export_check` is still not an implemented capability anywhere in the repo; that script says so rather than omitting it |
| 25.13 | Response / TTS | `PROVEN_PROXY` | Every receipt is composed from the run READ BACK, with the comparison metrics on it, and the loopback proxy renders them. Real narration audio stays gated on the owner's OpenAI credits (owner item 29) — the same single reason it is `NOT_YET_PROVEN` in four earlier gates |

## Stage 26 — M28 Native Application Factory (2026-09-09, ADR-0095)

Spec: `docs/M28_NATIVE_APP_FACTORY_SPEC.md`. Report: `docs/reports/m28-native-app-factory-report.md`. The last milestone: a distributable application exists when a real build produced a real artefact on this machine, something INDEPENDENT read that artefact back, the application was launched and driven through the operator, its logs were read — and every platform the toolchain cannot reach is classified honestly as not built.

The toolchain was measured before any of it was designed against and again on the finished code (`docs/evidence/m28-toolchain-2026-09-09.json`), which is what killed two of the draft spec's assumptions before either could shape code: there is **no MAUI workload**, so MAUI is not a stack this milestone has; and **`pefile` is not importable**, so the PE reader is standard library only — the same trade ADR-0093 refused for numpy in M27.

Merged main `7527877`; CI run `34341441268`. Suites: `services/api` unit suite **7883 passed / 16 skipped** (CI, 13m19s); `services/browser` **412**; `apps/web` **81 files / 1576 tests**, `tsc --noEmit` clean, `oxlint` clean; `devices/windows-agent` **973** (135 + 838); `ruff check .` clean in both Python services; the voice corpus **1813 cases across 21 categories** (`nativeapps` **94**), every earlier category unchanged case for case. M28's own unit tests: **117** across eight files, plus **78** voice ones across two.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 26.1 | What the toolchain allows is MEASURED, not assumed, and a platform it cannot reach is named rather than attempted | `PROVEN_REAL` | `docs/evidence/m28-toolchain-2026-09-09.json`: .NET SDK 10.0.400, WPF template, `makeappx` and `signtool` under Windows Kits 10.0.26100.0, the whole Android SDK (adb, emulator, build-tools 30.0.3/33.0.0, platforms 32/33, `aapt2`) — and **no Java, no MAUI workload, no macOS**. Re-measured on the finished code, not trusted from the day before: the M27 lesson, where a registry key with no executable behind it was about to be reported as an installed Photoshop |
| 26.2 | The plan is data with a closed vocabulary and bounds; free text never reaches code | `PROVEN_AUTOMATED` | `app/nativefactory/spec.py`: closed targets, stacks, templates and features; version `^\d+\.\d+\.\d+$` because it must be an assembly version, an MSIX `Version` and a semver at once; a display name may carry `&` (a name, not an attack) but never markup structure. A test asserts the display name appears in **no `.cs` file**. `test_nativefactory_spec.py` 25 |
| 26.3 | The generated project passes M23's `ProjectFiles` policy UNCHANGED — one policy, not a laxer second one written for native | `PROVEN_AUTOMATED` | `app/nativefactory/generator.py` renders template FILES with `{{SLOT}}` markers, every slot filled from a validated field, and both drift directions are refused: an unfilled slot and a slot the renderer does not know. `validate_files` accepts the result as-is |
| 26.4 | A native project contains only source, and a build happens only inside the root that authorises it | `PROVEN_AUTOMATED` | `app/nativefactory/roots.py`: resolve-then-contain (symlinks, `..` and absolute paths resolved FIRST), refused **before a single file is written** — a test asserts the directory does not exist afterwards, not merely that the row says failed. Plus an extension allowlist, because a native project is COMPILED and RUN and MSBuild will execute a `.ps1` a project file points at; an assert holds that the allowlist and the executable list can never overlap. Containment is judged against the root IN FORCE, injected by the caller. `test_nativefactory_roots.py` 25 |
| 26.5 | A real EXE, built by the real toolchain from an owner-shaped request | `PROVEN_REAL` | `scripts/tests/native-windows-lab.py` (`docs/evidence/m28-native-windows-lab-2026-09-09-102001.json`): request → spec → 9 files → policy → `dotnet build` 0 → `dotnet test` **6/6** (including persistence across a restart and the data directory's own location) → `dotnet publish` 0 → **`notlarim.exe`, 162,304 bytes**. Re-running after the containment and extension guards landed produced a **byte-identical sha256**: the build is reproducible |
| 26.6 | The artefact is read back by something that did NOT build it, and the version is the one the spec asked for | `PROVEN_REAL` | `app/nativefactory/artifacts.py`, standard library only: x64, `windows_gui`, version `0.1.0`, sha256 `175164a9e06475da…`, verdict **AGREES**. Qualified against binaries it did not build — Windows' own `mspaint.exe`, the .NET host (`windows_console`, the distinction the validation depends on) and this repository's installed agent, **whose version string it reproduced exactly** — and one test compares its answer with Windows' own version API, so a reader agreeing only with itself would fail. `test_nativefactory_artifacts.py` 15 |
| 26.7 | A portable package and an MSIX, made by the platform's own tool and opened independently | `PROVEN_REAL` | `windows_portable` 59,574,644 bytes; `windows_msix` 60,592,458 bytes built by **Windows' own `makeappx.exe`**, read back as identity `PagentOS.notlarim`, version `0.1.0.0`, x64, display name `Notlarım`, 250 payload entries. `makeappx` refused the first manifest (error 80080204) until it declared `runFullTrust` — fixed by declaring the capability the application actually uses, not by an entry point that would misdescribe the package. `test_nativefactory_packaging.py` 9 |
| 26.8 | NOT signed, by decision, and the package says so about itself | `PROVEN_REAL` | Signing needs a certificate and the owner's real signing identity is theirs — not something an autonomous build reaches for. Publisher `CN=PagentOS Unsigned Build`, display publisher `PagentOS (imzasız)`, and the receipt tells the owner an unsigned MSIX installs only where its publisher is trusted. A test asserts no path reaches `signtool` with an owner certificate store |
| 26.9 | A row may not say `verified` unless a reader read the file; every other outcome is named and distinct | `PROVEN_AUTOMATED` | `native_builds` (migration 0037, one row per application AND TARGET, so one can fail without the other): nothing produced → `failed`; produced but unreadable → `unverified` ("Hazır demiyorum."); produced and disagreeing → `mismatch`, with what was READ kept even though it disagrees; a target this machine cannot reach → `unavailable`, **which is not a failure at all**. The failure matrix is proven with a fake runner, because a real build exercises only the happy path. `test_nativefactory_service.py` 17 |
| 26.10 | Honest classification per platform | `PROVEN_REAL` (Windows) / `WAITING_OWNER_JDK` (Android) / `NOT_BUILT` (iOS) | Android's SDK, emulator, adb, build-tools and system images are ALL present and there is no Java — so the router says exactly that, naming owner item 33, because an owner told only "Android unavailable" would go looking for the wrong thing. The Android template refuses by name rather than shipping one whose build can only fail. iOS is **not a target value at all** and offers no owner action, because none exists. `test_nativefactory_stacks.py` 15 |
| 26.11 | Voice: the `nativeapps` family through the ONE router, with every earlier category unchanged | `PROVEN_AUTOMATED` | Nine `NATIVE_*` intents and the `native.*` tools; corpus **1719 → 1813** across **20 → 21** categories, `nativeapps` **94**, and not one earlier category changed — checked case by case, because this router has had four intent collisions and the fourth was caught by another milestone's negative case. `test_voice_native_intents.py` 58 + `test_voice_native_tools.py` 20 |
| 26.12 | The Living Core: UI contract v13 `native.build`, additive, published from real row state | `PROVEN_AUTOMATED` | Contract v13, append-only — held by a test that freezes the v12 vocabulary as an ordered prefix, which caught the web track's own first attempt. `native.build` publishes from `_touch`, the one place every transition goes through, so the wire cannot drift from the row; `verdict_ok` comes from the reader's verdict and never from `state == "verified"`, and both halves read it that way on purpose. The "Yerel Uygulamalar" panel has **no controls at all**: a build is a twenty-minute compiler plus a package install, and a button would be a second authority surface |
| 26.13 | The REST surface is read-only and reports only what the row holds | `PROVEN_AUTOMATED` | Asserted from the app's OWN published OpenAPI contract — so a route added and never called still fails — and again from a 405. 404 / 409 / 410 are kept apart on purpose: "I never made that", "I made it and it produced nothing", "I made it and the file is gone" send an owner in three different directions. `test_nativefactory_routes.py` 7 + `test_nativefactory_web_route.py` 4 |
| 26.14 | The device half: a compiler runs under exactly one root, through fixed argv, adding no capability name | `PROVEN_AUTOMATED` | `DEVICE_PROTOCOL.md` §6n: the four `dotnet`/`makeappx` shapes ride the existing `project.scaffold(root:"native")` + `project.run`, matched token for token against a FIXED list, never a prefix; the three web runtimes are REFUSED under the native root. No new capability name, deliberately — adding one would have moved the advertised manifest from 85 to 89 and invalidated the arithmetic both qualification scripts assert. The two halves' root constants are held together by a test that READS the other side's source |
| 26.15 | The EXE launched, driven, relaunched with its state intact, and its own log read | `NOT_YET_PROVEN` | Launching is M19 through the device runtime, and the installed runtime is `1.0.0+a3cb04e` with 29 capabilities. `scripts/core/qualify-item28-unlocked.ps1` runs it automatically the moment owner item 28 lands — build, launch through `file.open` from the authorised root, type a note through UI Automation, read the count back from the application's OWN status line, close, relaunch, require the note to still be there, read its log. Dry run against the old runtime: 86 planned calls, 0 sent, **the refusal is the result** |
| 26.16 | A build startable from PRODUCTION | `NOT_YET_PROVEN` | The voice family, the rows, the channel, the panel and the device half are all real; what does not exist is the Cloud Core object that drives the device's `project.*` for a native build. Nothing supplies `native_runner` outside the corpus harness, so production answers `dependency_unavailable` and says so. Honest, not silent — and gated on item 28 regardless, since the deployed agent advertises no `project.*` at all |
| 26.17 | Response / TTS | `PROVIDER_UNAVAILABLE` | Receipts are composed from the ROW and speak the file name, not the path — a Linux Cloud Core reading a Windows path with `pathlib.Path` returns the whole string, which CI caught and three sites carried. Real narration audio is blocked on OpenAI credits (owner item 29), rechecked once on 2026-09-09: *"You have no credits remaining"* |
