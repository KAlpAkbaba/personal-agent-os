# M25 Unity / Blender / 3D creation — milestone report (2026-09-08)

Owner directive: master directive "CLOSE M18.4 AND COMPLETE M19 -> M28" (M25 section: "add structured 3D creative development capabilities; detect actual installed tools; if software is unavailable, implement and test the provider architecture honestly; do not claim real app control without the app"). Decision record: ADR-0088 (+ addendum 1). Spec: `docs/M25_CREATIVE_3D_SPEC.md`. QUALIFICATION Stage 23. Merged main: 20db267.

**THE TOOLS WERE MEASURED BEFORE ANYTHING WAS DESIGNED** (`docs/evidence/m25-tool-detection-2026-09-08.json`, 2026-09-08 09:55Z): Blender 4.5.4 LTS at `C:\Program Files\Blender Foundation\Blender 4.5\blender.exe` builds a scene from Python headlessly and renders a still — real here. Unity 6000.5.0f1 (Hub 3.18.3) is installed, and `-batchmode -nographics -quit -createProject` exits **198** in 5 s: "No valid Unity Editor license found. Please activate your license." (the licensing client answering 404 for the entitlement; re-measured at 13:24Z with the same result). So the Unity half is built as the directive's honest form and its marks say PROXY until the owner signs in to Unity Hub (item 32). The owner's real Unity project `E:\hologram\HologramVehicleTest` was never opened, read or written by anything in this milestone.

**IMPLEMENTED**
- Cloud Core (`services/api/app/creative3d/`): `spec.py` — `ScenePlan` with a closed operation vocabulary (`create_scene`, `add_primitive`, `transform`, `set_material`, `set_camera`, `set_light`, `attach_script`, `render`, `inspect`), closed-alphabet names, bounded numbers, a fixed Unity script catalogue, and a canonical `plan_json()`; a path-shaped project or scene name is refused by the slug rule before any device call. `drivers/blender_driver.py` and `drivers/SceneDriver.cs` — the two FIXED drivers with their sha256 pinned in `drivers/manifest.json` and verified before the bytes are shipped, so no Python or C# is ever generated from owner or model words. `compare.py` — the closed loop's comparison, folding per-object expected state across the whole plan. `models.py` + `service.py` + `routes.py` + migration `20260908_0032_scenes.py` (expand-only) — `SceneService` (create, apply, render, inspect, status, list) on the SAME device port every family holds, with the Unity licence answered as `dependency_unavailable` carrying the licensing client's own words; the owner-gated REST surface `GET /v1/scenes`, `GET /v1/scenes/{id}`, `GET /v1/scenes/{id}/render`, `POST /v1/scenes/{id}/render|inspect`. `tools_scene.py` and the `SCENE_*` intents through the ONE router, focus kind `scene`, ledger rows `scene.*`, UI contract v10 `scene.activity`.
- Device (`PagentOS.SessionCompanion/Scenes/`; agent 0.6.0): `SceneCapabilityNames.Inspect` advertised with `OperatorEnabled`; a 3D root (`%USERPROFILE%\Documents\PagentOS Projects\3d`) resolved-then-contained beside `Projects`; `ProjectManifest` gaining exactly two argv shapes matched structurally against editors found by detection (the registry and Program Files, never a PATH search); `ProjectRunner.RunBatchAsync` waiting out a batch run inside the same Job Object (Unity ≤ 10 min / 4 GiB, Blender ≤ 5 min / 2 GiB, kill-on-close, breakaway denied, the environment scrubbed, the log bounded), with a Unity exit 198 turned into `dependency_unavailable` carrying the licence line from `-logFile`; `SceneInspection` returning the bounded `out.json` and the render PNG as base64 with its sha256 checked against what the inspection declares; `DEVICE_PROTOCOL.md` §6m.
- Web: contract v10 (`scene.activity` with `{tool, scene, state, objects}`, the nine step words spelled once, a 120 s horizon because a headless render outlasts every earlier one), `scenes.ts` (facts → posture → caption; "doğrulandı" reachable only from `verified`), a making posture, a distinct rendering posture, a reading posture that draws inward, a `mismatch` posture that names the object, and `unavailable` as a settled honest posture rather than an error; the Cockpit "3B Sahne" panel with the render fetched under the owner session (a gated route answers a bare `<img>` with 401, and a broken picture reads as "no render"), controls absent rather than disabled on an unavailable row, and the licensing client's own sentence shown.

**THE ONE COMMAND, SETTLED BY MEASUREMENT** — `blender --factory-startup -b [<scene.blend>] --python <driver.py> -- <plan.json> <out.json>`. The factory settings are the first token and are never optional: opening a saved `.blend` without them loads the OWNER'S installed Blender add-ons into the run, and on this machine one of them starts a watchdog thread that never stops, so the editor never exits and the run burns its whole bound. The scene file is what is optional, and only there: a project's first run has none to open, because a `.blend` that does not exist cannot be opened at all and `project.scaffold` writes text only. Eight tokens without the scene, nine with it. The Cloud Core builds that string in one function, the device matches it token for token, and a real lab runs both forms against the real editor.

**TESTED** — Cloud Core: `test_scene_spec.py` (the closed vocabulary, the alphabets, the bounds, the canonical plan JSON, a path-shaped name refused before any device call), `test_scene_compare.py` (every constraint kind, the tolerances, the camera-aim fold in BOTH directions, the render's own independent reader including its size bound, and the rule that an empty comparison is never a match), `test_blender_driver.py` (the sha256 pin, the argv tail of both real command forms, the look-at trigonometry cross-checked against an independent recovery in `compare.py`, every operation kind against a fake `bpy`, the bounded `out.json`, and that the scene is always saved as `scene.blend`), `test_scene_service.py` (the whole service against a fake device that runs the REAL driver logic on a fake `bpy` and now READS the manifest the way the real device does), `test_scene_routes.py` (the REST surface through the real application object and the corpus harness), `test_identity_enforcement.py` (the routes owner-gated). Whole API unit suite green (the count is in the gate). Device: `tests/PagentOS.Agent.Tests/Scenes/` 51 facts inside a project of 758/758. Web: 1318 facts across 72 files. Real Blender: `scripts/tests/blender-scene-lab.py` (the driver directly) and `scripts/tests/blender-service-command-lab.py` (the Cloud Core's OWN command, both forms, compared through the real `compare()`).

**SYNTHETIC OWNER COMMANDS** — the corpus category `creative3d` through the ONE router at the canonical transcription boundary: the directive's own utterances "Unity'de boş bir sahne oluştur.", "Bir küp ekle.", "Işığı ayarla.", "Blender'da küre oluştur.", "Kamerayı nesneye çevir.", "Render al." and their paraphrase and ASR-noise variants (no punctuation, lower case, no diacritics).

**VOICE ROUTES** — `SCENE_CREATE / SCENE_ADD / SCENE_TRANSFORM / SCENE_LIGHT / SCENE_CAMERA / SCENE_RENDER / SCENE_INSPECT` to the `scene.*` tools; the tool word (`unity` / `blender`) comes from the utterance, else the `scene` focus, else a clarification; every earlier category unchanged, the whole corpus HEALTHY across 16 categories, forbidden side effects 0.

**FORBIDDEN ROUTES** — "Sahneyi sil" and "Projeyi sil" reach no tool; the owner's real project is refused at the spec by the slug rule before any device call; an operation outside the vocabulary is refused by the plan's closed tag set; forbidden side effects 0, measured by inspecting the fixture project after every case.

**TTS OUTPUT** — every receipt is built from the INSPECTION rather than the plan ("Küre eklendi: Kure, (0, 0, 0); sahnede 3 nesne"), the Unity path says "lisans yok" instead of claiming a scene, a disagreement says "Uyuşmazlık efendim" and names the object and the field, and a run with nothing to check says so ("Doğrulanacak bir şey yoktu efendim") rather than claiming verification.

**LOOPBACK-STT** — `docs/evidence/tts-loopback-2026-09-08-144253.json`: 29 balanced cases including the 3D ones, every synthesis HTTP 429, summary PROVIDER_UNAVAILABLE. OpenAI has no credits (owner item 29), so the loopback stays NOT_YET_PROVEN and is not claimed otherwise.

**BUGS FOUND / FIXED (regression tests for each)**
1. **The receipt said "verified" whatever the comparison found** (security review, HIGH/CRITICAL, verified live with a PoC against the real service): a plan moving an object that had never been created answered "Kup taşındı" while the tool's own inspection proved it absent. The comparison decides the state now.
2. **A run with nothing to check was then called a disagreement** (mine, found by `test_scene_routes.py` driving "Blender'da yeni sahne aç." through the real application object): the first version of that fix made every `ok=False` a mismatch, and an empty comparison is deliberately `ok=False`, so creating an empty scene told the owner "Uyuşmazlık efendim" about a run that did exactly what was asked. Three outcomes now, one fact each.
3. **The write path could never have worked against the real device** (security review, HIGH): the manifest carried the bare word `blender` instead of the argv the device matches token for token, and the scaffold never asked for the 3D root. Fixed; the fake device now reads the manifest, so the blind spot is closed for every test rather than one.
4. **The driver saved the plan's own scene word, not `scene.blend`** (mine, found by the new service-command lab against the real editor): the second run had nothing to open and Blender exited 1.
5. **Opening a saved `.blend` loaded the owner's installed Blender add-ons** (mine, same lab): one of them starts a watchdog thread that never stops, so the editor never exited and the run burned its whole bound — a hang, and third-party code inside a job this milestone exists to bound.
6. **A near-uniform render passed the independent reader** (mine, same lab): the lab's own plan never aimed the camera. The plan was fixed, not the reader.
7. **The driver hashes were pinned and never checked** (security review, MEDIUM/HIGH).
8. **The camera-aim fold could hide a dropped instruction** (security review, MEDIUM): it dropped the rotation constraint for any object ever aimed, including one stated after the aim.
9. **The Cloud Core's wait was shorter than the device's own bound** (security review, LOW).
10. **The render's pixel size was never measured** (security review, LOW): only its bytes and non-uniformity were.
11. **The Cockpit could not read a single successful scene run** (mine, found by reading the two halves of the UI contract against each other): the Cloud Core published the database row's word in `scene.activity`, and the web build can only read the loop's step words. Five of the seven overlapped with nothing, so the "3B Sahne" row could never say "doğrulandı" and never settled, and the Unity licence path published nothing at all. The REST surface had the same drift and worse: `/v1/scenes` also sent the database's word, and the panel reads a row's state through the same vocabulary, so `rowState()` was null for every real scene and the row showed NO controls at all. And `scene.status` read the English token out loud in the middle of a Turkish sentence. One vocabulary now on every surface the owner or the web can see, and a test that reads the other side's source file to keep them together.
12. **A cancelled genesis run was drawn as one still being built** (mine, found by asking the same question of every earlier family): `GenesisRun` reaches `cancelled` whenever the owner says "Vazgeç" and the Cloud Core publishes it, and the web build had no such word, so the Core drew a run the owner had given up on as one still being made. In released M24 code. A web test asserted that `cancelled` was NOT a genesis state, which is why nothing caught it. Fixed, with QUALIFICATION Stage 22 row 22.6 carrying the correction, and the guard generalised to every family and proven to fail by reintroducing the drift.

**REGRESSION TESTS** — every bug above has a named test on main; the corpus category `creative3d` and the Scenes lab are permanent coverage; both Blender labs are rerunnable and write their own evidence, and the failing run that found defects 4 and 5 is kept beside the passing one.

**SECURITY GATE** — `PROVEN_REAL` (review). Independent review of 7c90090..bf791d5 recorded at `.claude/agent-memory/security-reviewer/m25-creative-3d-review.md`; six findings, all closed in 007bb01 with ADR-0088 addendum 1, plus the four found while closing them. Verified sound by the review: `ScenePlan`'s closed vocabulary and bounds; `SceneInspection`'s bounds-before-read, sha256 verification and resolve-then-contain confinement; `ProjectManifest`'s token-exact argv matching; tool detection by install location; the Job Object bounds and environment scrub; Unity's honest `dependency_unavailable`; the owner-gated REST routes; the web rendering text and typed tokens only; the migration additive; the corpus's delete-verb negatives structurally unreachable rather than merely asserted.

**CI** — run 34254950816 on 20db267: 7/7 jobs green (API lint + unit, API integration, Windows agent build + tests, browser agent, web shell build, recovery supervisor, secret hygiene); green on 007bb01 (run 34247048976) as well — the runs on cb048b9, 41afaaf and 5e3e834 were superseded and cancelled by the next push

**DEPLOYMENT** — released to production 2026-09-08 17:24Z. The Cloud Core now serves 20db267 as api-green behind the edge: the build, the expand-only migration, api-green healthy, **device handoff 1/1 after 1 s**, the edge switched to green, health through the edge settled after 0 retries, api-blue drained 60 s and stopped. The harness's own re-measurement afterwards (`docs/evidence/m18-4-qualification-2026-09-08-172747.json`, 16 checks): blue/green with **no gap — probes=24 dropped=0 gap=0 s max_latency=193 ms, slow(>3 s)=0**, device presence gap 0 s over 9 polls, alarms 1->1 and routines 1->1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanning production signals with pause/resume proven, final release == HEAD. **RUNTIME READ-BACK** (2026-09-08 17:26-17:28Z, through the edge with the owner credential): `/v1/system/health` release=20db267 started 17:24:42Z contracts action=12 **ui_state=10**; `/v1/ui/state/contract` contract_version=10 with 53 tokens, `scene.activity` present and every M20-M24 token still there; `GET /v1/scenes` -> 401 without a session and 200 `{"scenes":[]}` with one; ledger row `deployment.cloud_core.released` result=20db267. The connected Windows agent advertises 29 capabilities and **not** `scene.inspect`, so the 3D family is not reachable from production until the agent update (owner item 28) — stated here rather than implied by the release. **HOW THIS RELEASE WENT, PLAINLY**: the first harness invocation was killed by my own mistake — I started a second command that opened the harness's log file, and the harness died two seconds into its release phase. It had already uploaded the tree and started the idle colour, so the completing run was `release-cloud-core.ps1 -BlueGreen -Force` in the foreground, and the harness then re-ran for its checks. Production was untouched throughout the aborted attempts (measured: the edge kept serving d6bb9d5 at contract v9 until the completing run). One harness check FAILS as a result and is reported as failing: "the device sessions were handed to the new colour BEFORE it took HTTP" looks for a handoff line in the release output, and a verify-only re-run of an already-released HEAD has none — the property itself was measured by the run that performed the release ("device handoff: 1/1 after 1s"). The split also left `LAST_KNOWN_GOOD` equal to `RELEASE`, which would have made a rollback redeploy the same sha; corrected on the host to d6bb9d5 in both the marker file and the environment, and the running container still reports the value it was started with until its next start

**RUNTIME VERIFICATION** — read back on production 2026-09-08 17:26-17:28Z through the edge with the owner credential: release 20db267 started 17:24:42Z, UI contract v10 with 53 tokens and `scene.activity` present beside every earlier token, `/v1/scenes` owner-gated (401 anonymous, 200 with a session, no scenes yet), and the ledger's own `deployment.cloud_core.released` row naming the sha. The deployed Windows agent does not yet advertise `scene.inspect`, so a 3D run cannot be started from production until owner item 28

**PROOF CLASS** — the scene plan and the fixed drivers PROVEN_AUTOMATED; the Blender round trip PROVEN_REAL on this machine, through the Cloud Core's own command as well as the driver lab; Unity PROVEN_PROXY (owner item 32, the editor licence); the closed-loop comparison PROVEN_AUTOMATED; the device's 3D runtimes PROVEN_REAL on this machine and the runner (PROVEN_PROXY on the deployed agent until item 28); voice PROVEN_AUTOMATED; TTS structure PROVEN_AUTOMATED and the loopback NOT_YET_PROVEN (owner item 29); the Living Core PROVEN_AUTOMATED; the Cloud Core on production `PROVEN_REAL` (the release and the surface).

**REMAINING MACHINE-UNVERIFIABLE ITEMS** — owner item 32 (sign in to Unity Hub) is what turns the Unity half from PROVEN_PROXY to PROVEN_REAL; nothing in the code path changes when it is done. Owner item 29 (OpenAI credits) is what the TTS loopback waits on. Owner item 28 (the agent update, which needs UAC) is why the deployed agent is PROVEN_PROXY while this machine and the runner are PROVEN_REAL. None of the three is reachable without the owner, and none is claimed as proven.

**WHAT THIS MILESTONE COST, AND WHAT IT TAUGHT** — the security review found a receipt that said "verified" whatever the tool reported, which is the one rule the whole project rests on. Closing the second finding required admitting that the milestone's real proof had never touched the service: the Blender lab drove the driver, the unit suite drove a fake, and the path between them was untested. The lab written to close that gap found three more defects in its first run. Then reading the two halves of the UI contract against each other found a fifth: the Cockpit could not read a single successful run.

With the guard for that written, the same question asked of the earlier families found a sixth, in released M24 code: a genesis run the owner cancelled was drawn as one still being built.

Every one of those had the same shape. Two halves, each with its own passing tests, never driven against each other — a fake device that never read the command, a lab that never called the service, a web suite fed its own vocabulary, and a test that asserted the drift was correct. The rule this milestone earned, and it is M24's rule one layer up: **prove a claim against something the code has never seen, and make the two sides of every contract read each other.** Three permanent guards now do that here: the fake device reads the manifest, the service-command lab runs the Cloud Core's own argv against the real editor, and `test_uistate_contract_halves.py` reads the other side's source for every UI family — proven to fail by reintroducing a real drift and watching it go red.
