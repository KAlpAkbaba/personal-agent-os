# M20 File & Document Intelligence — milestone report (2026-09-08)

Owner directive: master directive "CLOSE M18.4 AND COMPLETE M19 -> M28" (M20 section). Decision record: ADR-0083 (+ addenda 1 device, 2 web, 3 security). Spec: `docs/M20_FILE_DOCUMENT_INTELLIGENCE_SPEC.md`. QUALIFICATION Stage 18.

**IMPLEMENTED**
- Oracle: deterministic fixtures `services/api/tests/fixtures/documents/` (DOCX ×2 same title / two paths / one changed clause, XLSX with a total and a formula, PPTX 7 slides, PDF 5 pages, MD, CSV, JSON, PY) generated by `scripts/tests/make-document-fixtures.py` (byte-stable re-runs; LF pinned), `truth.json` (reference scheme, ten questions, one comparison, one common-points entry) and `expected/*.extract.json` — the single seam: the device must produce them, the Cloud Core consumes them.
- Device (Session Companion `Documents/`): `FileIdentity` (`file_id` = a location: the hashed volume serial + casefolded path; `doc_id` = a content version: the SHA-256 of the bytes; `FileRecord` with `sha256` only ≤ 8 MiB and never from a search), `FileKinds` + `SecretNames` + `TextFold` (diacritics-insensitive matching), `TextFileReader` (BOM/UTF-8/UTF-16, bounded), `IDocumentExtractor` with `OpenXmlExtractor` (DOCX paragraphs/headings/tables, XLSX rows + formulas, PPTX slides; DocumentFormat.OpenXml 3.5.1, MIT), `PdfPigExtractor` (PdfPig 0.1.16, Apache-2.0), `TextLikeExtractor` (MD sections, CSV rows, JSON keys, source chunks + symbols, TXT paragraphs), `FileSearch` (own queue, reparse points never followed, 200 / 20 000 / 10 s bounds), `DocumentCompare` (blocks by ref), `DocumentCapabilities` (six names dispatched behind the one `OperatorEnabled` gate; every path resolve-then-contained before any I/O; `unsupported_format` and `not_found` added to the taxonomy, schema and broker validator; `AgentInfo.SoftwareVersion` 0.3.0; `DEVICE_PROTOCOL.md` §6j). After the security review: `DocumentBounds` (every cap in one place), `ContainerGuard` (the OOXML central directory read with `ZipArchive`, nothing inflated: 64 MiB summed uncompressed, 10 000 parts, ratio > 100:1 above 1 MiB → `unsupported_format` / `decompression_bound`), `BoundedFilterProvider` (PdfPig's filter source wrapped: streaming length counters for Flate/LZW/RunLength, 32 MiB per stream, 256 MiB per document; `page_bound` over 200 pages unless a `page_range` ≤ 200 is named; 256 KiB of text per page), lexical secret-name refusal before any lookup, one-handle bounded text reads (4 MiB prefix, the cut moved back to a whole character; JSON whole ≤ 8 MiB else `too_large`).
- Cloud Core (`app/documents/`): `document_index` (migration 0026, expand-only; unique per device + file id; `blocks` ≤ 64 KB flagged when cut), `DocumentIndex` (grows only from owner-initiated reads/searches), deterministic `retrieval` (Turkish casefold both ways, diacritics-insensitive token overlap, literal numbers and cell/page refs, top-k), `answers` (kind-aware summaries; answers naming the file and the place in the owner's words; comparisons; common points; an honest "bulamadım" with what was searched), `DocumentService` (search / read / inspect / summarize / answer / compare / common_points / previous with deixis over the M19 focus stack — kinds `file`, `document`, `folder`; two same-title or same-named documents answered by path, never guessed; `document.inspect` through `file.inspect`, never a full extract), eight voice tools (`file.search`, `document.read/summarize/answer/compare/inspect/common_points/previous`) registered through the real relay, eight intents with the `documents` context flags, ledger rows `document.*`, receipts subsystem `documents`, UI contract v5 `document.analysis` with `{file, part}`; device selection by `document.extract` with the honest `capability_missing` answer when no device advertises it.
- Web: UI contract v5 (`document.analysis`), `documents.ts` (facts → view → caption from published metadata only; the place phrased exactly as the Core speaks it: "3. sayfa" / "3. paragraf", "4. slayt", "Ozet sayfası, 5. satır", "Kararlar bölümü", "7. satır", "ses anahtarı", "1-40. satırlar"; "Belge inceleniyor" when no file is published), the reading posture in `visual.ts` and the M18.1 table, the Core's readout, the Cockpit `DocumentsPanel` (current / previous document, the last answer's refs, the honest empty state).

**TESTED** — the documents lab on real files under the fixture root through the companion's real dispatcher: 40/40 on this machine (every fixture's extraction equals its expected extract ref for ref; `doc_id` equals the truth's sha256, so the copies are byte-identical; search diacritics-insensitive with `max` truncating loudly; inspect headers; read with offset/length and `unsupported_format` for a DOCX; compare of the two contracts `changed_refs == [p2, p8]`; identities stable and distinct; a junction and an outside path refused; `.env` refused as `secret_bearing_name`; a 300 KB file truncated at 65 536 chars, a 60 MiB file refused before any read); whole agent project 514/514 (operator lab included) + audio 135/135; `dotnet format` clean; the GitHub runner ran the same lab green (device merge ce4378d, web merge f7d632f); web 927/927 (+58: `document-states.test.ts`, `documents-panel.test.tsx`) + `tsc` clean; after the security fixes the documents lab is 54/54 (15 new: generated DOCX/XLSX/PPTX bombs of 200 MiB inflated in ~205 KB, a 300-page PDF bomb sharing one 200 MiB stream — each refused on inspect, extract and both compare orders with +0.0 MiB working set and a counting extractor never entered; a 6 MiB CSV read returns the first 65 536 chars with `total_chars` 4 194 304 and `truncated: true`; a 40 MiB text read allocates ~12 MB instead of ~120; a 9 MiB JSON → `too_large`; `.env` present, absent inside a root and absent outside all answer `secret_bearing_name`) and the whole agent project 529/529 twice; Cloud Core: 65 documents-specific unit tests green (fixtures 3, index 10, retrieval 13, answers 19, tools 10 through `create_app`, wiring 3, focus 5, search ambiguity 2) inside the full API unit suite 5124 passed / 2 skipped (pre-existing) / 0 failed, `ruff` clean; PowerShell suites unchanged.

**SYNTHETIC OWNER COMMANDS TESTED** — the corpus category `documents`: 148 cases (canonical, paraphrase, ASR noise, no diacritics; deixis "bunu / bu dosyayı / bu belgeyi / bir önceki / az önceki", folder and extension searches, the ten truth questions spoken, inspect, common points, previous; negatives: no document focus → clarification, "Bu dosyayı sil" reaches no tool and no device capability, "Şifre dosyamı oku" → the device's `permission_denied` spoken honestly with no text leaked, "Bunu teknik anlat." unchanged). **VOICE ROUTES PASSED**: 686/686 (538 earlier cases unchanged + 148). **FORBIDDEN ROUTES / SIDE EFFECTS**: 0.

**TTS OUTPUT TEST** — every successful case: non-empty Turkish speech naming the file and the place + the audible-turn record (PROVEN_AUTOMATED). **LOOPBACK/STT** — two real OpenAI runs with the same seed (tts-1 → whisper-1, isolated bytes): the first (`docs/evidence/tts-loopback-2026-09-07-230106.json`) 32 cases balanced over eight categories, 26 matched / 5 degraded / 1 mismatched, mean WER 0.082 — the mismatch is whisper dropping two Turkish words after the English brand name in "PowerShell açtım efendim" on a 1.4 s clip (the STT provider already sends language=tr; an STT artefact, recorded), the two degraded documents cases were spoken file extensions — fixed (`spoken_file_name`); the second run (`…-230910.json`) re-sampled the same 32 after the fix: `doc.read.1` matched exactly with the new wording, then OpenAI answered 429 "no credits remaining" for 22 of 32 cases (owner item 29) — the harness now reports PROVIDER_UNAVAILABLE for such a run instead of a regression (PROVEN_PROXY; physical hearing not claimed).

**BUGS FOUND / FIXED (regression tests for each)**
1. The fixture generator wrote the expected extracts with CRLF on Windows and the fixture directory did not pin LF for its text files — a Windows checkout would have hashed differently from the runner; LF pinned (`.gitattributes`), the generator writes LF everywhere, `truth.json` sha256 verified against the checkout (kickoff, cb35298).
2. The M19 typing-guard lab test and the Explorer selection test kept failing only on the GitHub runner (three shapes of focus loss, Windows Server's Explorer never reporting the `/select` item selected) — fixed before M20 began (see the M19 report, bugs 7 and 17); the M20 CI runs inherited green.
3. Security review, HIGH: a 2 MiB OOXML container inflating to ~2 GiB crashed the companion (`OutOfMemoryException` in ~3 s) before any bound or cancellation could run — the container's central directory is read before any SDK open (`ContainerGuard`), PdfPig's filters are bounded (`BoundedFilterProvider`), page counts checked first; regression: `DecompressionBoundTests` (real bomb containers, allocated bytes measured).
4. Security review, LOW: a missing `.env` answered `not_found` while a present one answered `permission_denied` (an existence oracle on secret-bearing names) — the secret-name check is lexical on the requested leaf before the exists branch; regression: `BoundedReadTests`/`DecompressionBoundTests` (present, absent-inside, absent-outside).
5. Security review, LOW: text-like reads materialised the whole file before the 64 KB cut and trusted a stat snapshot for the 50 MiB bound — one `FileStream` handle, its `Length` re-checked on the handle, a 4 MiB prefix decoded with the cut moved back to a whole character, JSON whole to 8 MiB; on the way a pre-existing bug: `structure.keys` of a JSON stopped at the block budget — now every key; regression: `BoundedReadTests`.
6. An import-order `NameError` in the interrupted core work made `create_app()` unable to construct at all — fixed on recovery (regression: `test_documents_wiring.py`).
7. "Az önceki" resolved to the research-only "just finished" reading even with a document in focus — the router prefers the document when one is focused (corpus deixis cases).
8. The UI-state exact-vocabulary tripwire and the realtime-session tool-name tripwire did not know v5 / the eight tools — updated deliberately, both tripwires still exact.
9. The fake device served `file.search` records with a `sha256` and a compare without `mtime_delta_s` — brought to the wire shape of `DEVICE_PROTOCOL.md` §6j (two commits), so the Cloud Core tests consume exactly what the device sends.
10. `document.inspect` ran a full `document.extract` — now `file.inspect` (headers only), as the spec's table says.
11. The core track's report named two `search` branches without a dedicated test (same-named hits; folder focus) — added on main (`test_documents_search_ambiguity.py`).
12. Loopback finding (first documents sample, evidence `tts-loopback-2026-09-07-230106.json`): file names were SAID with their extension — whisper heard "butce-2026.xlsx" as "Bütçe 2026'ın" and "sunum-q3.pptx belgesine" as "Sunum Gülçpege'sine" (two degraded cases) — `spoken_file_name` drops the extension and reads hyphens as spaces in speech only; receipts keep the exact name; the three tool tests now assert the extension is never spoken.
13. The loopback harness called a provider outage a regression (`REGRESSION_FOUND` when OpenAI ran out of credits mid-run, posted to the ledger as such) — `PROVIDER_UNAVAILABLE` when every error is `dependency_unavailable`, a mismatch still a regression; regression: `test_voice_loopback.py` (three cases).
14. Security review (Cloud Core), medium + 2 low + the refs gap — see SECURITY GATE; regression tests `test_documents_confinement.py`, `test_documents_answers.py` (3 000-char block), `test_uistate.py`, `test_documents_tools.py`.
15. The full unit suite reproduced a 1-in-3 flake in `test_voice_research_followup` (the spoken focus losing to the completion's row on a row-id tiebreak when both writers' moments tie on Windows' coarse clock) — the research focus stamps every act strictly after the newest row (db-monotonic) and nudges the default clock like the operator focus does; regression: `test_research_focus.py` (two tests); 6/6 green after.

**SECURITY GATE** — independent review of the device half (scope `cb35298..ce4378d`): 1 high (OOXML decompression bomb, verified live), 2 low; all fixed with real-file regression tests (working set measured); verified sound: resolve-then-contain on every path including search roots, memo lookups and both compare targets; identities from the resolved path only; secret names checked on the resolved leaf and filtered from listings; every bound answers `truncated: true`; the family double-gated by `OperatorEnabled` on both halves; no write/delete/move anywhere; the taxonomy additions purely additive; every result through the forbidden-key scan; the lab leaves nothing behind. independent review of the Cloud Core + web half (scope cb35298..d2fccdf): 1 medium (no cloud-side guard on `folder`/`pattern` before the device call — single-layer confinement), 2 low (a model-supplied folder string persisted as the folder focus label; the spoken excerpt unbounded while `refs[].excerpt` was capped) and one functional gap (answer refs never published — the bus drops lists); all closed: `_validate_folder`/`_validate_pattern` refuse absolute, `..`, UNC/device and oversize inputs with a typed `invalid_argument` receipt that never echoes the value and reaches no device (`test_documents_confinement.py`), folder focus only from a known root or the device's `searched_roots`, a ≤ 400-char spoken excerpt cut at a word boundary, and exactly one structured bus key `refs` (≤ 8 `{ref, path}` pairs ≤ 64 chars, excerpt never on the bus; `test_uistate.py`, `test_documents_tools.py`); verified sound: no new endpoint, device selection by capability only, a `permission_denied`/`unsupported_format`/`not_found` result is never indexed, receipts and ledger rows carry no excerpts, focus rows keyed by ids never titles, contract v5 additive, migration expand-only, no `dangerouslySetInnerHTML`, nothing fetched by the browser. Residual risk recorded in ADR-0083 addendum 3: a per-page PDF stream bomb not empirically tried; a job-object-bounded extraction child process deferred.

**CI STATUS** — run 34171734557 on 092e56d: 7/7 jobs green (API lint + unit 5145 passed / 2 skipped, API integration, Windows agent build + tests 529/529 + audio 135/135 with the bomb-container lab on the runner, browser agent, web shell build 927/927, recovery supervisor, secret hygiene); every M20 merge before it green as well (ce4378d device, f7d632f web, 4ee5756 device security fixes, d2fccdf core, b7c8714 core security fixes superseded by 092e56d). **DEPLOYMENT STATE** — the M20 Cloud Core (092e56d) is LIVE on production: harness run 11 (`docs/evidence/m18-4-qualification-2026-09-08-000603.json`, 16/16 checks): blue/green release of 092e56d blue → green through the canonical path with no gap (360 probes, 0 dropped, max latency 1126 ms), the device sessions handed to green before it took HTTP (1/1 after 1 s), presence gap 2.22 s, RELEASE = 092e56d, LAST_KNOWN_GOOD = bd21f7f exported; the release ledger row now reads "blue/green geçişi, api-green" (the M19 wording fix at work); the Windows agent stays 0.1.0 until the owner's elevated update (item 28). **RUNTIME VERIFICATION** — read back from production after the release with an owner session: `/v1/system/health` release = 092e56d, last_known_good = bd21f7f; `/v1/ui/state/contract` carries `document.analysis` beside the operator states (47 states); `/v1/ui/state` reports contract version 5; device MAIL online with 29 capabilities (the documents family absent, as expected for the 0.1.0 agent — the tools answer `capability_missing`); the ledger row `deployment.cloud_core.released` = 092e56d recorded at 00:08:35Z.

**PROOF CLASS**
| Item | Class |
|---|---|
| Six document capabilities through the companion inside the roots, refs on every block | PROVEN_REAL (mechanisms: this machine + the GitHub runner) / PROVEN_PROXY (the deployed 0.1.0 agent until item 28) |
| Extraction equals the oracle; identities; confinement; bounds; bomb containers refused | PROVEN_REAL |
| Answers cite deterministic refs; index; focus by identity | PROVEN_AUTOMATED |
| Voice routing (corpus `documents`) | PROVEN_AUTOMATED |
| TTS output structure | PROVEN_AUTOMATED |
| Loopback speech semantics | PROVEN_PROXY |
| Living Core `document.analysis`, Cockpit panel | PROVEN_AUTOMATED |
| Cloud Core half on production | PROVEN_REAL (release) |

**REMAINING MACHINE-UNVERIFIABLE ITEMS** — the owner's elevated agent update with `-Operator` (item 28; UAC) before the deployed agent reads documents; a live render of the Core's document posture (the preview is behind the owner login); physical audibility; the cognitive backend's prose (never asserted; refs are); PdfPig on PDFs from other producers (only the fpdf2 fixture and the generated bombs were exercised).

**M20 ENGINEERING CLOSED** — M21 Mail & Calendar begins.
