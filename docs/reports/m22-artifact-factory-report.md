# M22 Artifact Factory — milestone report (2026-09-08)

Owner directive: master directive "CLOSE M18.4 AND COMPLETE M19 -> M28" (M22 section). Decision record: ADR-0085. Spec: `docs/M22_ARTIFACT_FACTORY_SPEC.md`. QUALIFICATION Stage 20.

**IMPLEMENTED**
- Oracle: `services/api/tests/fixtures/artifacts/` (`scripts/tests/make-artifact-fixtures.py`): five specs — the budget spreadsheet (three rows, the total, a formula), a five-section document with a table, a three-slide presentation, a dataset, a page — and `truth.json` with the refs an independent reader must find per format and the numbers the owner "said".
- Cloud Core: `app/artifacts/spec.py` (`ArtifactSpec`: five kinds with their structures, bounds, `spoken_numbers` and the never-invented rule), the renderers extended behind the M13 `Renderer` Protocol (`XlsxRenderer`, `CsvRenderer`, `PptxRenderer`, `JsonRenderer`, `MdRenderer` + table support; openpyxl and python-pptx moved to runtime; deterministic — the shared OOXML zip normaliser pins `docProps/core.xml` because openpyxl restamps `modified` at save time), `validation.py` (independent readers: python-docx, openpyxl, python-pptx, pypdf — a new BSD runtime dependency — and the standard library; element-by-element checks on M20's reference scheme; totals recomputed; resource bounds before any library inflates: OOXML bombs, > 10 000 parts, oversized text/JSON, > 200-page PDFs, non-zip bytes, malformed JSON all refused as failing validation, never a crash), migration 0028 (`validation_json`, `state valid|invalid` on renders), `factory.py` (create → renders → validations, an invalid render kept and named; `render_format`, `revalidate_all`; the UI publish), `open_service.py` (picks a valid render, builds the device's `file.fetch` payload with the render's hash and size, translates device errors), routes on the existing artifacts router incl. `POST /v1/artifacts/factory` and `POST /v1/artifacts/{id}/open` (owner-session gated), `FOCUS_KIND_ARTIFACT`, five voice tools (`tools_artifacts.py`: `artifact.create/render/validate/open/list`), four intents (`ARTIFACT_CREATE` with kind words, the title and `spoken_numbers`; `ARTIFACT_OPEN`, `ARTIFACT_LIST`, `ARTIFACT_VALIDATE`; "Bunu PDF yap" resolves through the deictic `artifact_ref`), UI contract v7 `artifact.factory`; ADR-0085 addenda 4 and 5.
- Device: `Documents/FileFetch.cs` in the Session Companion: the URL must be on exactly the origin the Device Service dialled (carried to the companion over the pipe handshake as `service_challenge.broker_origin`; the service refuses a foreign origin before the pipe; a companion never told refuses everything and never guesses from its own voice URL), path under `/v1/artifacts/`, no redirects followed (`dependency_unavailable`), `size` and `Content-Length` checked against 50 MiB and the stream counted and aborted past it, the name validated (plain file name ≤ 120 chars; no separators, `..`, reserved names, control characters, trailing dots; executable and secret-bearing names refused), streamed into a `CreateNew` temp file in the Downloads root with the SHA-256 computed on the wire bytes and compared before the rename (a mismatch deletes the temp file: `postcondition_failed`), a collision suffixed ` (n)`, resolve-then-contain on the kept path, `open: true` through the operator's real `file.open`; `DEVICE_PROTOCOL.md` §6k; `AgentInfo.SoftwareVersion` 0.4.0 (ADR-0085 addendum 1).
- Web: UI contract v7 (`artifact.factory`, verdicts `rendering | valid | invalid`, `{title, format, verdict, failing_ref}` typed; captions spelled once in `contract.ts`: "Dosya üretiliyor", "Bütçe 2026 · XLSX · doğrulandı", "… · doğrulanamadı (sheet:Ozet!B5)"), `artifacts.ts` (facts → view → caption from published metadata only; an unreadable verdict word → the plain state name, never "doğrulandı"), the making posture (paths carry traffic while rendering, stop on a verdict; invalid held under restraint), the Cockpit "Üretilenler" panel (the rows of `GET /v1/artifacts` with each render's verdict and failing ref; a session-gated download link per VALID render only, fetched as a blob through `apiFetch`; unvalidated M13 renders say "doğrulama bildirilmedi"; "Aç" enabled only with a valid render and nothing in flight, calling `POST /v1/artifacts/{id}/open` through the `ArtifactClient` double — the route the Cloud Core provides; a bare 404 reported honestly as the route absent); ADR-0085 addendum 1 (web).

**TESTED** — the fetch lab (`FileFetchTests.cs`, a local `HttpListener` standing in for the Cloud Core origin, the real dispatcher, the Downloads root under the fixture root): 35 facts — a 200 KB DOCX fetched, verified and inspected, a `.txt` variant opened in Notepad with the window observed; a foreign origin, another scheme, a path off `/v1/artifacts/`, a `..` traversal and an untold companion refused with zero requests; a wrong hash and a short chunked body → `postcondition_failed` with nothing left; `size` over 50 MiB refused before any request, `Content-Length` 50 MiB + 1 refused unread, an 80 MiB chunked stream aborted between 50 and 80 MiB served; 302 off- and on-origin → `dependency_unavailable` with the target seeing no request; 410/503/a dead port typed; 22 bad names refused before any request; a collision suffixed; the family advertised with `OperatorEnabled` only; the real pipe handshake carrying the origin proven with a real companion; whole agent project 565/565, `dotnet format` clean; Cloud Core: 103 spec/renderer/validation tests + 12 factory + 14 routes + 4 wiring + 3 identity + 14 tool tests inside the full unit suite 5675 passed / 2 skipped (5405 on main before M22); every fixture spec × format validates `ok` and satisfies every `truth.json.expect` entry (butce-tablosu xlsx+csv, hosgeldin-sayfasi html+md, musteri-listesi csv+json, q3-sunum pptx, toplanti-notlari docx+pdf+html+md+txt); lying renderers caught with the right failing ref (a dropped row → `sheet:Ozet!A3`/`B3`, a dropped slide → `s3` + `structure:slide_count`, a corrupted heading → `h1:Giriş`, a missing paragraph → its `h2:` ref); seven hostile inputs refused; `ruff` clean; web 1090/1090 (+70: `artifact-states.test.ts`, `artifacts-panel.test.tsx`, `artifacts-client.test.ts`, the exhaustive visual kind table) + `tsc` clean + `next build` green

**SYNTHETIC OWNER COMMANDS TESTED** — the corpus category `artifacts`: 110 cases (a budget spreadsheet from spoken kalem/tutar pairs, a Word document from notes, a three-slide presentation, a dataset, a page, "Bunu PDF yap" on the focused artifact, open/list/validate, paraphrase / ASR noise / no diacritics; negatives: "Bunu aç" with nothing produced → clarification, the rendered numbers ⊆ the spoken numbers asserted on the produced spec, "Sil" reaches no tool, "Bunu teknik anlat." unchanged). **VOICE ROUTES PASSED**: 918/918 on the merged main with the strengthened harness (806 earlier cases unchanged + 112: 110 + the two security negatives). **FORBIDDEN ROUTES / SIDE EFFECTS**: 0.

**TTS OUTPUT TEST** — every successful case: non-empty Turkish speech naming the file, the format(s), the verdict and any failing ref + the audible-turn record (PROVEN_AUTOMATED). **LOOPBACK/STT** — not run for real: OpenAI still answers 429 "no credits remaining" (owner item 29); the harness would record PROVIDER_UNAVAILABLE; the speech-structure checks of every corpus case stand, the loopback semantics for M22 remain unproven until credits return.

**BUGS FOUND / FIXED (regression tests for each)**
1. Security review (device), HIGH — see SECURITY GATE; regression: the stalled-origin, two-slot and re-verify tests in `FileFetchTests.cs`.
2. Security review (device), MEDIUM ×2 and LOW ×2 — see SECURITY GATE; regressions: the new-extension refusals, the `Zone.Identifier` read-back, the proxy property, the U+202E name.
3. openpyxl restamps `docProps/core.xml` `modified` at save time regardless of the workbook's properties — invisible within one process, a guaranteed drift in production; the shared OOXML zip normaliser now pins it (regression: the determinism test, two renders one hash).
4. pydantic's `ValidationError.errors()` crashed the routes' JSON error handler on a custom validator failure — `include_context=False` (regression in `test_artifact_routes.py`).
5. An exact-vocabulary tripwire test enumerating every registered tool name did not know the five artifact tools — updated deliberately, the tripwire still exact.
6. A real production gap flagged by the core track (ADR-0085 addendum 4, decision 5): the render download route is owner-bearer-gated while the device's `file.fetch` sends no token — closed by `render_fetch_store.py` (ADR-0085 addendum 5): the open service mints a single-use, expiring (≤ 10 min), random token bound to one render (artifact id + format + content hash, stored hashed like the alarm audio tokens) and hands the device `GET /v1/artifacts/renders/fetch/{token}` on the origin the device dialled, with the render's exact `size` and `sha256`; the route serves the bytes once with exact `Content-Length`/`Content-Type`, then answers 404 for a used, expired, tampered or unknown token with no detail; it is listed as a deliberate unauthenticated endpoint in `test_identity_enforcement.py`; the owner's bearer-gated render download is unchanged; the token never appears in receipts, ledger rows or UI metadata (`test_render_fetch_store.py`, `test_artifact_routes.py`, `test_artifact_tools.py`).
7. Security review (Cloud Core), HIGH formula injection, MEDIUM/HIGH `javascript:` links, MEDIUM opt-in numbers rule, LOW ×2 — see SECURITY GATE; regression tests: `test_artifact_spec.py` (+14), `test_artifact_renderers.py` (+15 incl. the tripwire), `test_artifact_validation.py` (+4 incl. entity expansion), `test_artifact_tools.py` (+7 end to end), corpus negatives `art.neg.invented` and `art.neg.secret`; live proofs: a hand-built `=HYPERLINK(…)` cell reopens as `data_type == "s"`, `javascript:`/`JAVASCRIPT:`/`data:`/`vbscript:` hrefs and srcs stripped, `https:`/`mailto:` kept.
8. On the way, two self-found bugs on the fix branch: the new HTML tripwire regex false-positived on the M3 escaper's own safe output (anchored to a real unescaped tag opening); `_validate_xlsx` caught only its own bound error around `load_workbook()`, so a defusedxml-refused payload would have crashed `validate()` (now `bound:xml_parse_refused`).
9. The corpus harness's persona-generated spreadsheet spec carried digits in its own labels ("Kalem 1") and a `0` fallback, tripping the always-on never-invented rule — and the harness's OK assertion accepted a refusal receipt as success (a refusal is a succeeded CALL under the M18 contract), so several `art.create.*` cases passed while refused. Fixed on main: letter labels, no zero fallback, and an OK case now fails on any refusal receipt (`expected an executed receipt, got a refusal`) — which immediately exposed the seven `art.render.*` PDF cases running against a spreadsheet fixture ("Bu dosya PDF olamaz" is the honest answer): a `CTX_DOCUMENT_ARTIFACT_FOCUSED` context now focuses a document for the PDF cases and the spreadsheet stays for the Excel case; 918/918.

**SECURITY GATE** — device half (`file.fetch`, scope 2d442b2..m22-device): 1 HIGH — the body was read with the synchronous `Stream.Read`, which ignores the cancellation token, so a peer at the pinned origin sending headers and a few bytes then silence held the companion thread and the `.part` file past the documented 30 s cap (verified live by the reviewer); 2 MEDIUM — a verify-then-rename window a same-user process could exploit, and an executable blocklist without the macro-enabled Office family / `.url` / `.jar` / `.pif` with no Mark-of-the-Web on fetched files; 2 LOW — the system proxy left on, Unicode format-control characters accepted in names. All closed (ADR-0085 addendum 3): async end to end under one token linking the budget and the 30 s cap (a stalled origin answers `timeout` at 30.003 s measured, the `.part` deleted, the slot freed; two concurrent slots, a third `fetch_busy`), the temp handle held across `File.Move` and the final path re-opened exclusively and re-hashed (`sha256_mismatch_after_move` deletes it), 13 extensions added to the shared blocklist (so `file.open` refuses them too), `Zone.Identifier` `ZoneId=3` on every kept file, `UseProxy = false`, Cc/Cf/lone-surrogate names refused; fetch lab 35 → 66, agent project 596/596. Verified sound by the review: origin pinning against IDN/userinfo/ports/IPv6/`..`/`%2e%2e` (empirically), redirects off, no TLS bypass, the handshake fail-closed with an older service, the pipe's four-layer identity unchanged, `OperatorEnabled` on both halves, the Downloads root confined at fetch time and after the write, bounds before/at-header/on-wire, streaming never buffered, the lab clean; Cloud Core + web halves (scope 2d442b2..4a16428): 1 HIGH — a spreadsheet/CSV cell beginning with `=`, `+`, `-` or `@` rendered as a live formula (`data_type == 'f'`, verified live with `=HYPERLINK(…)`), certified valid by the validator and auto-opened by `artifact.open`; 1 MEDIUM/HIGH — python-markdown link syntax let a `javascript:` href through the HTML renderer (verified live); 1 MEDIUM — the never-invented-numbers rule was opt-in (skipped whenever the router found no number in the utterance); 2 LOW — `openpyxl`'s worksheet `iterparse` without `defusedxml`, no secret-reference gate on `artifact.create`; plus the already-tracked device-auth gap (addendum 4 decision 5) closed by the signed render URL. Fixes: formula-trigger cells (`=`, `+`, `-`, `@` after BOM/whitespace) refused at the spec (`Sheet._no_formula_injection`, the dataset validator; the declared `formulas` map exempt) and neutralised by the renderers as defence in depth (`app/artifacts/security.py`), with the validator failing any cell that became a formula outside the declared map even when its text matches; only `http`/`https`/`mailto` hrefs and srcs survive the HTML renderer, a `<script>`/`on*=` surviving conversion trips `UnsafeGeneratedHtmlError`; `artifact_create` always passes `spoken_numbers` (an empty list when the utterance had none) so an invented number is refused naming its ref; `contains_secret_reference` over the title and every text; `defusedxml` declared as a runtime dependency so openpyxl's `iterparse` is protected, and a refused entity-expansion payload reported as a bound; ADR-0085 addendum 6. Verified sound by the review: object-store keys never derive from a title; the device is only ever pointed at this Cloud Core's own render path built server-side; no delete/overwrite tool; capability-only device selection; contract v7 additive; migration expand-only; router-level owner-session gating with the two new routes listed; zip-layer bounds before any library parse; python-docx and python-pptx parse with `resolve_entities=False`; the corpus's `artifact.render` exemption narrow; the web calls exactly three routes, renders text only, and never claims "Açıldı" without the receipt

**CI STATUS** — run 34201274734 on c2f1904: 7/7 jobs green (API lint + unit 5743 passed / 2 skipped, API integration, Windows agent build + tests 596/596 + audio 135/135 with the fetch lab on the runner, browser agent, web shell build 1090/1090, recovery supervisor, secret hygiene); every M22 merge before it green (ad3471a device, 6293df0 web, 7c2154d fetch fixes, 4a16428 core, e6189b7 signed render token, ed18214 core security fixes). **DEPLOYMENT STATE** — RELEASED 2026-09-08 08:13Z through the canonical blue/green path from a frozen checkout of c2f1904 (the last commit with a fully green CI; the two commits after it are documentation only), right after the M21 release on the same fresh Tailscale check: harness run `m18-4-2026-09-08-081308` (evidence `docs/evidence/m18-4-qualification-2026-09-08-081308.json`): PASS, 16 checks, 0 failed — the blue/green release of c2f1904 took 157.7 s with no gap (probes=381 dropped=0 gap=0 s max_latency=1365 ms slow>3 s=0), the device sessions handed to api-green before it took HTTP (1/1 after 1 s), device presence gap 2.26 s (limit 15 s), alarms 1→1 and routines 1→1 unchanged, the Windows agent online through the edge after 0 s, the supervisor scanned production signals (2 tracked, 0 opened) and pause/resume proven, final release == HEAD c2f1904 with contract v12 served and LAST_KNOWN_GOOD exported = 571ddb2. CI after c2f1904: run 34202322471 on the documentation-only 486491a failed one fetch-lab fact on the runner (the peer's accepted-socket count was read before the listener's continuation had run, so it read 0 while both stalled fetches were connecting) — a timing assumption in the test, not in the fetch; fixed in 437eb7e so the test waits for the peer to have accepted both stalled sockets before asserting that the refused third one added none; fetch lab 66/66. **RUNTIME VERIFICATION** — read back on production 2026-09-08 08:16–08:20Z: `/v1/system/health` release=c2f1904 last_known_good=571ddb2 started 08:14:40Z; `/v1/ui/state/contract` contract_version=7 with `artifact.factory` present (50 tokens; `document.analysis`, `mail.activity`, `calendar.activity`, `operator.running` all present); `/v1/ui/state` at version 7; `GET /v1/artifacts/renders/fetch/<64 zeros>` and `/…/not-a-token` → 404 without a token, `POST /v1/artifacts/factory` → 401 without a session; the owner's device online through the edge with 29 capabilities (the deployed agent predates the documents family and `file.fetch` — item 28 — so `artifact.open` answers `capability_missing` there until the agent is updated); ledger row `deployment.cloud_core.released` result=c2f1904 at 08:15:59Z "Cloud Core c2f1904 canliya alindi (blue/green gecisi, api-green)"

**PROOF CLASS**
| Item | Class |
|---|---|
| Renderers deterministic; validation by independent readers; a lying renderer caught | PROVEN_AUTOMATED |
| `file.fetch` origin-pinned, hash-verified, bounded; then `file.open` | PROVEN_REAL (this machine + the runner) / PROVEN_PROXY (the deployed agent until item 28) |
| Voice routing (corpus `artifacts`), the spoken-numbers rule | PROVEN_AUTOMATED |
| TTS output structure | PROVEN_AUTOMATED |
| Loopback speech semantics | NOT_YET_PROVEN (PROVIDER_UNAVAILABLE; item 29) |
| Living Core `artifact.factory`, Üretilenler panel | PROVEN_AUTOMATED |
| Cloud Core half on production | NOT_YET_PROVEN — release pending the owner's Tailscale SSH check (item 31), queued behind the armed M21 release |

**REMAINING MACHINE-UNVERIFIABLE ITEMS** — the Tailscale SSH check (item 31) before any release; the owner's elevated agent update with `-Operator` (item 28) before the deployed agent fetches and opens artifacts; Office actually honouring the Mark-of-the-Web (Protected View) on the owner's machine; the loopback proxy while OpenAI has no credits (item 29); a live render of the Üretilenler panel (behind the owner login); end-to-end redemption of a render-fetch token by the real companion (the lab's origin is a local listener).

**M22 ENGINEERING CLOSED** — M23 App Factory begins.
