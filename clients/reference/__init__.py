"""Headless reference clients.

`mobile_client` is the M9 stand-in for the native mobile app: it exercises the
full mobile contract over real HTTP, exactly as a native client would, on a
machine that has no mobile toolchain and no device.
"""

__all__: list[str] = []
